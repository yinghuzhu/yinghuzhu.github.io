<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>yhzhu的随手记</title>
  
  
  <link href="https://www.yzhu.name/atom.xml" rel="self"/>
  
  <link href="https://www.yzhu.name/"/>
  <updated>2026-09-13T02:00:00.000Z</updated>
  <id>https://www.yzhu.name/</id>
  
  <author>
    <name>yhzhu</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>从 Figma 到编码 Agent：设计上下文的结构化、缓存与校验</title>
    <link href="https://www.yzhu.name/2026/09/13/design-api-context-architecture/"/>
    <id>https://www.yzhu.name/2026/09/13/design-api-context-architecture/</id>
    <published>2026-09-13T02:00:00.000Z</published>
    <updated>2026-09-13T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>将设计稿交给编码 Agent，需要先解决设计数据从哪里来。官方集成提供了现成入口；直接调用设计平台 API，则把数据获取和请求调度的控制权留在自己的工具中。</p><p>选择从成本约束开始：如果需求集中在读取节点、导出截图和资产，已有 API 权限与额度能够覆盖这些操作，就有机会在不增加设计平台订阅支出的情况下完成设计获取。接下来需要衡量的是，为此承担多少集成和维护工作。</p><p>本文以 Figma 为例，讨论一种将设计获取与页面实现分开的架构：通过可调度的 API 获取层准备本地上下文包，再由多模态 Agent 完成实现与验证。这里的获取层指承担数据获取与请求调度的模块，CLI 是本地调用入口；工具则包含获取层、包管理和校验等模块。成本、请求复用、凭据分配和输入质量共同决定这层工具的边界。</p><span id="more"></span><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>直接接 API 的收益来自两个方面：在已有权限和额度内控制新增费用，以及自行决定请求如何合并、缓存、分配和重试。相应代价是维护获取层与数据契约。</p><p>确定性 CLI 负责获取、归一化、缓存和结构校验，多模态 Agent 负责理解设计、结合现有工程实现页面，并在真实浏览器中验证。两者通过本地上下文包交接。</p><p>这套划分让输入失败有明确的状态和恢复路径，也保留了几个必要边界：包完整不代表选对了设计范围，缓存命中不代表远端设计没有变化，视觉通过也不代表业务行为通过。</p><h2 id="已有-Figma-MCP，为什么选择直接接-API"><a href="#已有-Figma-MCP，为什么选择直接接-API" class="headerlink" title="已有 Figma MCP，为什么选择直接接 API"></a>已有 Figma MCP，为什么选择直接接 API</h2><p>官方 Figma MCP 提供<a href="https://developers.figma.com/docs/figma-mcp-server/tools-and-prompts/">设计上下文、截图等工具</a>，并能结合 <a href="https://developers.figma.com/docs/figma-mcp-server/code-connect-integration/">Code Connect</a> 提供设计组件对应的代码实现信息。对于已经具备合适套餐和席位、需要这些集成能力的场景，直接使用官方工具可以减少自建维护。</p><p>当需求集中在设计读取时，可以把成本拆成平台访问、编码 Agent 和工具维护三部分。API 方案主要改变第一部分，并增加第三部分；它不会消除模型调用费用。在已有权限和额度足够的场景下，设计获取可以接近零新增费用，但这是一种有条件的成本结构。</p><p>MCP 本身是连接协议，费用和访问条件由具体服务决定。Figma 官方 MCP 提供<a href="https://developers.figma.com/docs/figma-mcp-server/rate-limits-access/">有限额度的访问</a>，<a href="https://developers.figma.com/docs/rest-api/rate-limits/">REST API 的限流规则</a>也与套餐、席位和端点等因素有关，入口选择取决于这些条件能否覆盖实际工作量。</p><table><thead><tr><th>决策维度</th><th>官方集成</th><th>自建 API 获取层</th></tr></thead><tbody><tr><td>使用成本</td><td>使用现有套餐、席位与服务额度</td><td>利用已有 API 额度，承担工具维护投入</td></tr><tr><td>数据组织</td><td>使用集成提供的上下文</td><td>自定上下文包和校验规则</td></tr><tr><td>请求策略</td><td>使用现成工具，再配套外围流程</td><td>控制缓存、批处理、凭据选择和重试</td></tr><tr><td>组件关联</td><td>可利用 Code Connect 等集成</td><td>按需求决定是否承担额外关联工作</td></tr><tr><td>演进责任</td><td>跟随官方服务</td><td>维护接口适配和格式兼容</td></tr></tbody></table><p>直接接 API 的价值在于把获取策略变成可以设计的工程模块。缓存减少请求量，调度管理可用额度，本地包支持跨会话复用。这些机制同样可以放在 MCP 调用之外；差异在于选择哪一层承担这些责任，以及相应的维护投入是否与收益匹配。</p><h2 id="先明确工具要解决哪一段问题"><a href="#先明确工具要解决哪一段问题" class="headerlink" title="先明确工具要解决哪一段问题"></a>先明确工具要解决哪一段问题</h2><p>设计稿进入代码仓库之前，需要回答三个问题：拿到了什么、这些内容是否足够、它们对应哪个实现目标。</p><p>拿到了什么，可以通过截图、节点结构和资产清单记录下来。内容是否足够，需要检查文件和引用关系，也需要结合目标看图判断。对应哪个实现目标，则依赖目标仓库、页面或路由，以及已有项目的业务约束。</p><p>三类信息的来源不同。设计平台能够提供节点与导出结果，却不能替项目决定路由和接口；CLI 能检查文件，却不能仅凭文件存在判断一个弹窗是否包含了全部内容；Agent 能理解图片，但下载失败、缓存替换这些操作没有必要每次重新组织。</p><p>因此，工具的目标收敛为：为 Agent 准备一份来源明确、内容可检查、失败可解释的设计输入。页面应该采用什么组件、如何适配现有布局、哪些行为必须保留，仍放在目标项目中判断。</p><p>将范围收敛到输入准备，可以避免工具同时承担完整代码生成平台的复杂度。输入检查可以跨页面复用，页面实现则依赖各自的组件和业务约束，因此两者分开处理。</p><h2 id="用本地上下文包划分职责"><a href="#用本地上下文包划分职责" class="headerlink" title="用本地上下文包划分职责"></a>用本地上下文包划分职责</h2><p>从责任分配看，有三种可选方式。</p><table><thead><tr><th>方式</th><th>适用之处</th><th>需要承担的问题</th></tr></thead><tbody><tr><td>Agent 直接获取设计并实现</td><td>一次性探索，流程短</td><td>获取、重试、输入检查依赖每次执行过程</td></tr><tr><td>工具获取设计并直接生成页面</td><td>输出范围固定、生成规则明确</td><td>工具需要承担组件选择、框架适配和业务集成</td></tr><tr><td>工具准备上下文包，Agent 实现</td><td>输入处理需要复用，目标工程存在差异</td><td>需要维护包格式，以及工具与 Agent 之间的约定</td></tr></tbody></table><p>这里选择第三种职责划分。CLI 接收设计节点，下载资源，转换为统一结构；Agent 读取结果，再在目标仓库里工作。</p><pre class="mermaid">flowchart TD    A["Figma 选定节点"] --> B["CLI：获取、归一化、校验"]    B --> C["本地上下文包"]    C --> D["Agent：查看截图、确认范围"]    E["目标项目与受保护业务行为"] --> F["页面实现"]    D --> F    F --> G["真实浏览器：视觉比较与业务验证"]    G --> H["人工验收"]</pre><p>这里有意保留了一次明确的交接。输入准备结束后，可以检查包的内容、重新运行校验，也可以在后续任务中复用同一份本地输入。页面实现失败时，排查起点也更清晰：先判断输入是否正确，再看实现是否偏离。</p><p>本地流程可以采用 CLI 和文件系统作为载体，无需额外常驻服务。对本地编码流程来说，这减少了服务启动、连接状态和部署方面的维护。代价是状态主要保存在本机；跨机器共享、多人同步和缓存清理需要另行处理。</p><h2 id="将多凭据放进请求调度层"><a href="#将多凭据放进请求调度层" class="headerlink" title="将多凭据放进请求调度层"></a>将多凭据放进请求调度层</h2><p>直接接 API 的另一个考虑，是请求不必固定绑定一把 key。获取层可以接收多组凭据，将具体凭据的选择从业务操作中抽离。上层只表达“获取这个节点和关联资产”，下层负责决定由哪个可用凭据执行、是否等待，以及何时重试。</p><p>多凭据调度需要先识别额度归属。key 是认证材料，额度池是容量边界，两者未必一一对应。例如，按 <a href="https://developers.figma.com/docs/rest-api/rate-limits/">Figma 的额度计量规则</a>，personal access token 按用户和套餐计量，同一用户生成多把 token 不会得到多份独立额度；plan access token 则按 token 和套餐计量。调度器应依据上游规则归组，避免把共享额度误认为可用的新容量。</p><pre class="mermaid">flowchart TD    A["节点与资产获取任务"] --> B{"有效缓存是否命中"}    B -->|是| C["复用本地上下文"]    B -->|否| D["请求合并与等待队列"]    D --> E["按访问权限筛选凭据"]    E --> F["按额度池与冷却状态调度"]    F --> G["设计平台 API"]    G -->|成功| H["校验并保存上下文"]    G -->|限流| I["记录冷却时间与重试预算"]    I --> D</pre><p>在权限和额度均可用的候选集合中，可以采用不同的分配策略。</p><table><thead><tr><th>策略</th><th>选择理由</th><th>需要处理的代价</th></tr></thead><tbody><tr><td>随机或轮询</td><td>凭据容量接近，调度逻辑简单</td><td>不能保证负载均衡，需要排除冷却中的额度池</td></tr><tr><td>按文档分片</td><td>同一文档保持相对稳定的访问路径</td><td>热点文档可能集中，需要允许重新分配</td></tr><tr><td>按容量加权</td><td>不同额度池的容量存在差异</td><td>需要维护容量估计和运行状态</td></tr></tbody></table><p>选择策略之前先过滤权限，能够避免一次随机分配把任务交给无权读取该文件的凭据。缓存隔离也应沿用相同的访问边界，不能因为某个凭据获取过数据，就让其他调用者自动获得读取资格。</p><p>遇到 429 时，应按上游返回的 <code>Retry-After</code> 冷却相应额度池。存在具有访问权限、额度独立且仍可用的其他候选时，可以重新调度；候选全部不可用时进入等待，并受最大等待时间和重试次数约束。认证失败、权限不足和临时网络错误需要分别处理，避免把所有错误都变成无休止的换 key 重试。</p><p>这一设计将限流影响收敛到获取层：页面实现无需知道凭据如何分配，也无需处理冷却与重试。多凭据利用可用容量，缓存和批处理减少所需容量，两者配合才能稳定降低远端请求压力。多进程共享凭据时，还需要共享额度池状态，否则各进程分别控制并发，合计请求仍可能超过上游限制。</p><p>凭据由运行环境或秘密管理设施注入，上下文包只保留来源和结果，运行日志通过不包含密钥的内部标识定位请求。这样可以调整调度策略，而不把认证材料传播到 Agent 上下文和业务仓库。</p><h2 id="截图、结构和资产分别提供什么"><a href="#截图、结构和资产分别提供什么" class="headerlink" title="截图、结构和资产分别提供什么"></a>截图、结构和资产分别提供什么</h2><p>上下文包的核心内容很少：一张设计截图、一份归一化节点数据、一组本地资产，以及描述这些文件的 manifest。</p><p>截图提供整体视觉依据。层次、留白、对齐和颜色关系，最终都要回到图片上判断。节点数据补充图片难以准确表达的信息，例如文字、几何尺寸和组件关系。资产则保留需要直接使用的图标与图片，减少实现时重新猜测或绘制的空间。</p><p>manifest 记录来源节点、导出参数、文件位置、资产映射和诊断信息，让这些内容形成一份可以检查的输入。</p><p>这里接受了一定冗余。截图和结构可能描述同一个元素，归一化数据之外还保留清理敏感字段后的平台原始数据。它们服务于不同目的：常规读取使用统一结构；遇到转换差异时，可以回查来源数据。Agent 也不需要一次读完所有内容，可以先看截图，再定向读取相关节点和资产。</p><p>归一化还有一个边界：统一结构不可能完整表达所有平台特性。可以通过来源适配层隔离平台字段，使核心包格式保持稳定。适配层负责解释各平台语义，无法无损转换的部分保留来源信息，避免把不相同的概念强行映射为同一字段。</p><h2 id="完整性校验之后，还要确认设计范围"><a href="#完整性校验之后，还要确认设计范围" class="headerlink" title="完整性校验之后，还要确认设计范围"></a>完整性校验之后，还要确认设计范围</h2><p>一个背景矩形可以是合法节点，也可以正常导出截图。假设目标是实现一个支付弹窗，但链接只选中了背景矩形，那么请求成功、文件可读、JSON 合法都无法证明输入满足任务要求。</p><p>包状态可以分为三类：</p><table><thead><tr><th>状态</th><th>含义与处理</th></tr></thead><tbody><tr><td><code>complete</code></td><td>通过当前包校验，可以进入设计范围确认</td></tr><tr><td><code>partial</code></td><td>存在缺失或诊断信息，保留结果供检查与恢复</td></tr><tr><td><code>invalid</code></td><td>核心结构或必需文件不满足要求，不能作为有效输入</td></tr></tbody></table><p>这里需要区分“结果允许保存”和“允许继续完整复刻”。非关键资产失败时，CLI 可以保留 partial 包，便于诊断；完整复刻流程仍要求输入达到 complete，并确认截图范围正确。</p><p>结构检查可以采用较窄的启发式规则：根节点属于基础图形，没有子节点、有效文字和可导出资产时，标记为设计范围可疑。它用于发现明显可疑的输入，不负责判断所有设计是否完整。一个有文字但缺少主体内容的节点，仍然可能通过结构检查。</p><p>因此，Agent 必须在实现前查看截图，将其与用户描述的目标对照。发现主要内容缺失，就重新确认来源节点。工具不会静默向上寻找父节点，因为父节点也可能包含多个页面、旧版本或无关区域，扩大范围同样是在改变任务输入。</p><p>这种处理会增加一次人工沟通，但减少了输入未确认时的实现投入。尤其当根因是选错节点时，重复下载同一个链接没有修复作用。</p><h2 id="缓存解决复用，刷新解决新鲜度"><a href="#缓存解决复用，刷新解决新鲜度" class="headerlink" title="缓存解决复用，刷新解决新鲜度"></a>缓存解决复用，刷新解决新鲜度</h2><p>设计截图和资产适合保存在本地。持续调整同一个页面时，可以复用已有输入，也避免把页面实现绑定在每一次远端访问上。</p><p>容易混淆的是缓存标识的含义。一种缓存键设计是对来源平台、文档 ID、节点 ID、导出格式和缩放参数生成 fingerprint。它标识同一组获取参数，没有包含远端内容版本。</p><p>例如，生成 fingerprint 的输入可以是下面这组参数，标识均为示意值：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;provider&quot;</span><span class="punctuation">:</span> <span class="string">&quot;figma&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;documentId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;example-document&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;nodeId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1:2&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;format&quot;</span><span class="punctuation">:</span> <span class="string">&quot;png&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;scale&quot;</span><span class="punctuation">:</span> <span class="number">2</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>对参数做稳定序列化后再计算哈希，可以得到缓存标识。这里没有设计版本或内容摘要字段；导出参数发生变化会改变标识，远端内容变化则不会自动反映进来。</p><p>因此，同一个链接命中缓存，只能说明本地存在通过校验、参数匹配的包。设计内容发生变化而节点 ID 不变时，这个 fingerprint 不会自动改变。</p><p>可以选择显式刷新：新任务或已知设计更新时重新获取；确认设计来源未变化的持续任务可以复用。这样保留了离线复用能力，也把新鲜度判断的责任暴露出来。若业务要求每次自动追踪最新版，就需要增加远端版本检查，并接受相应的请求、延迟和失败处理。</p><p>刷新本身还涉及旧数据保护。一种做法是先在同级临时目录准备新包，校验后再切换到正式目录；替换过程中保留旧目录备份，捕获到发布失败时尝试恢复。</p><p>这样可以避免下载到一半直接破坏旧包，但这套目录操作不等于完整的事务系统。多步重命名之间存在切换窗口，也不能据此声称已经解决并发读写或进程崩溃恢复。若需要并发读取或崩溃恢复，可以进一步采用不可变版本目录、指针切换和恢复记录；这些机制也会增加状态管理成本，应由实际要求驱动。</p><h2 id="工具状态与项目代码分开存放"><a href="#工具状态与项目代码分开存放" class="headerlink" title="工具状态与项目代码分开存放"></a>工具状态与项目代码分开存放</h2><p>截图、原始 JSON、缓存和浏览器证据有自己的生命周期。它们可能体积较大，也会随着设计变化而重复生成。如果默认放进业务工作树，每个目标项目都要处理忽略规则、临时文件和误提交问题。</p><p>状态和缓存可以放在工作树外，通过稳定的工作空间标识关联目标仓库。标识设计需要考虑路径变化，避免一次目录改名就让原有缓存和任务状态失去关联。</p><p>这个决策减少了工具对业务仓库的侵入，同时也带来可发现性成本：查看和清理文件时，需要通过 CLI 找到实际路径。另一台机器重新克隆仓库，也不会自动获得这台机器的本地状态。</p><p>这里区分的是文件用途。设计来源包和验证证据由工具管理；页面运行确实需要的图片，则仍应进入目标项目约定的资产目录，并接受正常代码审查。</p><h2 id="页面验收需要两组证据"><a href="#页面验收需要两组证据" class="headerlink" title="页面验收需要两组证据"></a>页面验收需要两组证据</h2><p>上下文准备完成，只是实现的起点。</p><p>迁移已有页面时，还需要明确哪些业务行为受保护，例如路由、API 调用、状态流转、表单校验和错误提示。设计截图通常不包含这些约束，它们必须从任务说明和既有实现中获得。</p><p>验收因此分成两组证据：视觉比较关注布局、字号、间距和资产使用；业务验证关注操作之后是否仍发生正确的请求、状态变化和错误处理。静态截图无法证明提交行为正确，接口请求成功也不能证明页面符合设计。</p><p>由多模态 Agent 对照原稿与真实应用截图，按需要迭代，再运行相关业务验证。CLI 只提供输入与状态支持，不输出视觉评分，也不负责最终验收判断。</p><p>这仍然存在主观性。图片比较的结论受视口、字体、页面状态和判断标准影响，所以需要保存具体证据，并保留人工验收。验收应保留差异项及其依据，单一的“还原度百分比”很难表达业务行为和视觉差异。</p><h2 id="这层工具适用于什么范围"><a href="#这层工具适用于什么范围" class="headerlink" title="这层工具适用于什么范围"></a>这层工具适用于什么范围</h2><p>当同一设计需要多轮实现、任务经常跨会话继续、已有页面还带着业务约束时，本地上下文包能提供一个稳定的检查起点。排查时可以分别处理来源、输入质量、实现和验收，减少不同阶段之间的猜测。</p><p>如果只是根据一张截图做一次性原型，维护包格式、workspace 和状态可能比任务本身还重。引入这层工具的前提，是输入处理已经成为重复工作，或者错误输入的代价足够高。</p><p>这套职责划分提供了可检查的输入格式、明确的失败状态和工具分工。缓存新鲜度仍需要显式管理，设计范围仍需要看图确认，跨机器协作也不在本地状态方案的承诺之内。保留这些边界，后续才容易判断新增能力应该进入 CLI、Agent 工作流程，还是独立的协作系统。</p>]]></content>
    
    
    <summary type="html">从成本约束出发，讨论设计 API 接入、凭据池调度、本地上下文缓存与校验，以及编码 Agent 的职责和验收边界。</summary>
    
    
    
    <category term="架构设计" scheme="https://www.yzhu.name/categories/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    
    
    <category term="Agent" scheme="https://www.yzhu.name/tags/Agent/"/>
    
    <category term="架构设计" scheme="https://www.yzhu.name/tags/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    
    <category term="上下文工程" scheme="https://www.yzhu.name/tags/%E4%B8%8A%E4%B8%8B%E6%96%87%E5%B7%A5%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>PostgreSQL Index Bloat: Why Small Tables Can Have Huge Indexes</title>
    <link href="https://www.yzhu.name/2026/07/06/postgresql-index-bloat-remediation-en/"/>
    <id>https://www.yzhu.name/2026/07/06/postgresql-index-bloat-remediation-en/</id>
    <published>2026-07-06T02:56:16.000Z</published>
    <updated>2026-07-06T02:56:16.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/07/06/postgresql-index-bloat-remediation/">中文版</a></p></blockquote><p>During a production database capacity review, we ran into a typical PostgreSQL index bloat case: several tables did not contain much live data, and one table had no live rows at all, yet their index files still occupied a large amount of disk space. Ordinary <code>VACUUM</code> had already run. <code>n_dead_tup</code> did not look alarming. The database files, however, did not shrink.<span id="more"></span></p><p>This kind of issue is easy to misread as “the database needs cleanup.” The real question is more specific: is the space pressure coming from heap bloat or index bloat? This article focuses on the diagnosis path: why the issue happens, why <code>VACUUM</code> did not fix it, what can stop the bleeding in the short term, and what should change in the write model over time.</p><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><ul><li>Ordinary PostgreSQL <code>VACUUM</code> can clean dead tuples in heap pages and make that space reusable, but it does not automatically shrink already-bloated B-tree index files.</li><li>When table data is small but indexes are huge, first separate heap bloat from index bloat. Looking only at <code>n_dead_tup</code> is not enough.</li><li>Frequently deleted, rebuilt, and batch-updated derived tables can create long-term index write amplification when they are stored as heavily indexed primary business tables.</li><li>Short-term remediation usually means dropping confirmed-unused indexes and rebuilding bloated ones with <code>REINDEX CONCURRENTLY</code>.</li><li>Long-term remediation must change the write model: use staging + diff instead of broad <code>DELETE</code> + <code>bulk_create</code>, and make index lifecycle review part of engineering practice.</li></ul><h2 id="1-The-Symptom-Small-Tables-Huge-Indexes"><a href="#1-The-Symptom-Small-Tables-Huge-Indexes" class="headerlink" title="1. The Symptom: Small Tables, Huge Indexes"></a>1. The Symptom: Small Tables, Huge Indexes</h2><p>The first step in a capacity investigation is not to run a cleanup script. It is to split table data size from index size.</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname <span class="keyword">AS</span> table_name,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid)) <span class="keyword">AS</span> total_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.oid)) <span class="keyword">AS</span> table_data,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid) <span class="operator">-</span> pg_relation_size(c.oid)) <span class="keyword">AS</span> indexes,</span><br><span class="line">    c.reltuples::<span class="type">bigint</span> <span class="keyword">AS</span> approx_rows</span><br><span class="line"><span class="keyword">FROM</span> pg_class c</span><br><span class="line"><span class="keyword">JOIN</span> pg_namespace n <span class="keyword">ON</span> n.oid <span class="operator">=</span> c.relnamespace</span><br><span class="line"><span class="keyword">WHERE</span> c.relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> n.nspname <span class="operator">=</span> <span class="string">&#x27;public&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_total_relation_size(c.oid) <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>The shape of the problem looked roughly like this:</p><table><thead><tr><th>Table type</th><th align="right">Total size</th><th align="right">Table data</th><th align="right">Indexes</th><th align="right">Estimated rows</th></tr></thead><tbody><tr><td>Periodic derived table</td><td align="right">50+ GB</td><td align="right">A few MB</td><td align="right">50+ GB</td><td align="right">Tens of thousands</td></tr><tr><td>Rule detail table</td><td align="right">Several GB</td><td align="right">Hundreds of KB</td><td align="right">Several GB</td><td align="right">Thousands</td></tr><tr><td>Execution record table</td><td align="right">Hundreds of MB</td><td align="right">0 bytes</td><td align="right">Hundreds of MB</td><td align="right">0</td></tr><tr><td>Extended attribute table</td><td align="right">Hundreds of MB</td><td align="right">Hundreds of KB</td><td align="right">Hundreds of MB</td><td align="right">Thousands</td></tr></tbody></table><p>That shape tells us the issue is not “too much live business data.” Historical write behavior has inflated index files.</p><p>Then check autovacuum statistics:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname,</span><br><span class="line">    n_live_tup,</span><br><span class="line">    n_dead_tup,</span><br><span class="line">    last_autovacuum,</span><br><span class="line">    last_autoanalyze,</span><br><span class="line">    autovacuum_count,</span><br><span class="line">    autoanalyze_count,</span><br><span class="line">    vacuum_count</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_user_tables</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> n_dead_tup <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>This is where misdiagnosis often starts. Some tables may already have <code>n_dead_tup = 0</code>, so it is tempting to conclude that autovacuum has handled the bloat. But if disk usage is still high, the main issue is probably not the heap. It is the indexes.</p><p>Next, inspect index size and usage:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname <span class="keyword">AS</span> table_name,</span><br><span class="line">    indexrelname <span class="keyword">AS</span> index_name,</span><br><span class="line">    pg_size_pretty(pg_relation_size(indexrelid)) <span class="keyword">AS</span> index_size,</span><br><span class="line">    idx_scan,</span><br><span class="line">    idx_tup_read,</span><br><span class="line">    idx_tup_fetch</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_user_indexes</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_relation_size(indexrelid) <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">30</span>;</span><br></pre></td></tr></table></figure><p>Common findings:</p><ul><li>A single B-tree index can grow to tens of GB.</li><li>Some indexes have <code>idx_scan = 0</code> but still occupy significant disk space.</li><li>Some tables may be empty while their indexes still exist on disk.</li><li>Primary key indexes, time-based indexes, and ORM-generated foreign-key indexes can all be inflated by historical churn.</li></ul><p>At this point, continuing to run ordinary <code>VACUUM (ANALYZE)</code> usually will not produce the expected result.</p><h2 id="2-Why-Ordinary-VACUUM-Did-Not-Shrink-the-Indexes"><a href="#2-Why-Ordinary-VACUUM-Did-Not-Shrink-the-Indexes" class="headerlink" title="2. Why Ordinary VACUUM Did Not Shrink the Indexes"></a>2. Why Ordinary VACUUM Did Not Shrink the Indexes</h2><p>PostgreSQL uses MVCC. An <code>UPDATE</code> does not overwrite a row in place. It writes a new row version and marks the old version as no longer visible. A <code>DELETE</code> also does not immediately return physical space to the operating system. It first creates reclaimable space.</p><p>The main job of ordinary <code>VACUUM</code> is to clean dead tuples in heap pages and make that space reusable inside the same table file. It can also remove index entries pointing to dead tuples. What it does not do is automatically shrink an already-expanded B-tree index file back to a small file.</p><p>So the database can reach this state:</p><ul><li><code>n_dead_tup = 0</code></li><li>table data is only a few MB</li><li>indexes are still tens of GB</li><li>repeated <code>VACUUM</code> runs do not materially reduce disk usage</li></ul><p>That does not mean <code>VACUUM</code> failed. It means <code>VACUUM</code> solved a different problem from the one we were facing.</p><p>If the goal is to shrink bloated index files, typical options are:</p><ul><li><code>REINDEX INDEX CONCURRENTLY index_name</code></li><li><code>REINDEX TABLE CONCURRENTLY table_name</code></li><li>a table&#x2F;index rewrite tool such as <code>pg_repack</code></li></ul><p><code>VACUUM FULL</code> can also rewrite objects and reclaim space, but it requires heavier locking and is usually not the first choice for online systems. Since PostgreSQL 12, <code>REINDEX CONCURRENTLY</code> is a practical option for online index remediation. It still consumes IO, and the old and new indexes coexist for a period of time, so disk headroom must be checked before execution.</p><h2 id="3-Root-Cause-High-Churn-Derived-Data-Stored-Like-Stable-Primary-Data"><a href="#3-Root-Cause-High-Churn-Derived-Data-Stored-Like-Stable-Primary-Data" class="headerlink" title="3. Root Cause: High-Churn Derived Data Stored Like Stable Primary Data"></a>3. Root Cause: High-Churn Derived Data Stored Like Stable Primary Data</h2><p>The root cause is rarely one database parameter. It is usually a mismatch between the write model and the storage model.</p><p>Consider derived data such as reporting results, rule-hit details, execution windows, and temporary computation outputs. Their characteristics are often similar:</p><ul><li>upstream data refreshes periodically</li><li>configuration changes can trigger broad recalculation</li><li>historical periods expire</li><li>calculation rules may change and require recomputing a future range</li></ul><p>This data is naturally high churn. But in many systems it is stored as long-lived primary data, with many indexes added for read paths. The write model then becomes:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Delete derived records for a target object over a future range</span><br><span class="line">Bulk create new period records</span><br><span class="line">Bulk create detail records</span><br><span class="line">Bulk create associated state records</span><br></pre></td></tr></table></figure><p>Or:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Calculation rule changes</span><br><span class="line">Clear the old derived structure</span><br><span class="line">Regenerate according to the new configuration</span><br></pre></td></tr></table></figure><p>At a small scale this is simple and usually correct. Under PostgreSQL MVCC, however, running this pattern for a long time continuously creates dead tuples and index garbage. The more important point is write amplification: every extra B-tree index must be maintained during deletes, inserts, and updates. The application thinks it is only rebuilding a derived result set. The database is maintaining multiple indexes for every rewrite.</p><p>This is why simply making autovacuum more aggressive is not enough. Autovacuum can reduce accumulation. It cannot remove the root cause of storing high-churn derived data as heavily indexed long-lived primary data.</p><h2 id="4-Another-Root-Cause-ORM-Defaults-Plus-Accumulated-Business-Indexes"><a href="#4-Another-Root-Cause-ORM-Defaults-Plus-Accumulated-Business-Indexes" class="headerlink" title="4. Another Root Cause: ORM Defaults Plus Accumulated Business Indexes"></a>4. Another Root Cause: ORM Defaults Plus Accumulated Business Indexes</h2><p>Another common source is that indexes are not designed as a whole. They accumulate over time.</p><p>For example:</p><ul><li>an ORM creates a single-column index for a foreign-key field</li><li>a business query adds a composite index</li><li>an ordering column gets <code>db_index=True</code></li><li>a unique constraint creates its own index</li><li>a historical LIKE query adds another index</li><li>the query pattern changes later, but the old index is never removed</li></ul><p>A high-write table may end up with indexes like:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">(entity_id)</span><br><span class="line">(rule_id)</span><br><span class="line">(effective_date)</span><br><span class="line">(entity_id, rule_id)</span><br><span class="line">(entity_id, effective_date)</span><br><span class="line">(entity_id, status, effective_date)</span><br><span class="line">PRIMARY KEY (id)</span><br><span class="line">UNIQUE (entity_id, attribute_id, value_id)</span><br></pre></td></tr></table></figure><p>Some of these indexes may still serve hot queries. Some may be historical leftovers. Some may be covered by the prefix of a composite index.</p><p>Do not drop an index only because <code>idx_scan = 0</code>. At minimum, check whether statistics have just been reset:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> datname, stats_reset</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_database</span><br><span class="line"><span class="keyword">WHERE</span> datname <span class="operator">=</span> current_database();</span><br></pre></td></tr></table></figure><p>Also confirm:</p><ul><li>whether the index backs a unique constraint or primary key</li><li>whether it is required by foreign-key maintenance</li><li>whether low-frequency but critical background jobs use it</li><li>whether ORM-generated query paths depend on it</li><li>whether an existing composite index already covers it</li><li>whether key queries regress under <code>EXPLAIN</code></li></ul><p>A safer workflow is to observe candidate indexes over a meaningful period, combine <code>pg_stat_user_indexes</code> with slow-query logs and <code>EXPLAIN</code>, and only then remove confirmed-unused indexes with <code>DROP INDEX CONCURRENTLY</code>.</p><h2 id="5-Short-Term-Remediation-Drop-Unused-Indexes-and-Rebuild-Bloated-Ones"><a href="#5-Short-Term-Remediation-Drop-Unused-Indexes-and-Rebuild-Bloated-Ones" class="headerlink" title="5. Short-Term Remediation: Drop Unused Indexes and Rebuild Bloated Ones"></a>5. Short-Term Remediation: Drop Unused Indexes and Rebuild Bloated Ones</h2><p>Short-term remediation has three parts.</p><p>First, record the before state.</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(oid)) <span class="keyword">AS</span> total_before</span><br><span class="line"><span class="keyword">FROM</span> pg_class</span><br><span class="line"><span class="keyword">WHERE</span> relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_total_relation_size(oid) <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>Also check long-running transactions and invalid indexes:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    pid,</span><br><span class="line">    usename,</span><br><span class="line">    state,</span><br><span class="line">    xact_start,</span><br><span class="line">    now() <span class="operator">-</span> xact_start <span class="keyword">AS</span> xact_age,</span><br><span class="line">    query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> xact_start <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> xact_start;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname <span class="keyword">AS</span> index_name,</span><br><span class="line">    i.indisvalid,</span><br><span class="line">    i.indisready</span><br><span class="line"><span class="keyword">FROM</span> pg_index i</span><br><span class="line"><span class="keyword">JOIN</span> pg_class c <span class="keyword">ON</span> c.oid <span class="operator">=</span> i.indexrelid</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">NOT</span> i.indisvalid <span class="keyword">OR</span> <span class="keyword">NOT</span> i.indisready;</span><br></pre></td></tr></table></figure><p>Second, drop confirmed-unused indexes:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> INDEX CONCURRENTLY IF <span class="keyword">EXISTS</span> idx_unused_column;</span><br></pre></td></tr></table></figure><p><code>CONCURRENTLY</code> reduces the impact on normal reads and writes, but it still consumes database resources. Process a small number of indexes at a time and watch database IO and application metrics.</p><p>Third, rebuild bloated indexes online:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">REINDEX INDEX CONCURRENTLY idx_large_active_index;</span><br></pre></td></tr></table></figure><p>If multiple indexes on the same table are bloated, evaluate:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">REINDEX <span class="keyword">TABLE</span> CONCURRENTLY high_churn_table;</span><br></pre></td></tr></table></figure><p>Operational notes:</p><ul><li><code>REINDEX CONCURRENTLY</code> cannot run inside a transaction block.</li><li>If it fails, it may leave an invalid <code>_ccnew</code> index that must be cleaned up before retrying.</li><li>The old and new indexes coexist during the rebuild, so disk headroom is required.</li><li>Reindexing creates IO pressure and should be batched during low-traffic windows.</li><li>After rebuilding, compare object sizes and key query plans again.</li></ul><p>Use the same query for before&#x2F;after validation. Do not change the measurement after remediation:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname <span class="keyword">AS</span> table_name,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid)) <span class="keyword">AS</span> total_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.oid)) <span class="keyword">AS</span> table_data,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid) <span class="operator">-</span> pg_relation_size(c.oid)) <span class="keyword">AS</span> indexes</span><br><span class="line"><span class="keyword">FROM</span> pg_class c</span><br><span class="line"><span class="keyword">JOIN</span> pg_namespace n <span class="keyword">ON</span> n.oid <span class="operator">=</span> c.relnamespace</span><br><span class="line"><span class="keyword">WHERE</span> c.relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> n.nspname <span class="operator">=</span> <span class="string">&#x27;public&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_total_relation_size(c.oid) <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>Then sample core queries with <code>EXPLAIN ANALYZE</code> to ensure that index cleanup did not introduce obvious query regressions.</p><h2 id="6-Medium-Term-Governance-Give-Indexes-a-Lifecycle"><a href="#6-Medium-Term-Governance-Give-Indexes-a-Lifecycle" class="headerlink" title="6. Medium-Term Governance: Give Indexes a Lifecycle"></a>6. Medium-Term Governance: Give Indexes a Lifecycle</h2><p>Index cleanup should not start only after disk alerts. A useful medium-term practice has four parts.</p><p>First, monitor index size and usage.</p><p>Track:</p><ul><li>individual index size</li><li>table-data-to-index-size ratio</li><li><code>idx_scan</code> growth</li><li><code>idx_tup_read</code> &#x2F; <code>idx_tup_fetch</code></li><li><code>pg_stat_database.stats_reset</code></li></ul><p>If statistics were reset recently, <code>idx_scan = 0</code> only means “not used since the reset.” It does not prove long-term non-use.</p><p>Second, tune autovacuum per high-churn table.</p><p>Global defaults are often conservative. For frequently updated tables, table-level reloptions can be more appropriate:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> high_churn_table <span class="keyword">SET</span> (</span><br><span class="line">    autovacuum_vacuum_scale_factor <span class="operator">=</span> <span class="number">0.05</span>,</span><br><span class="line">    autovacuum_vacuum_threshold <span class="operator">=</span> <span class="number">1000</span>,</span><br><span class="line">    autovacuum_analyze_scale_factor <span class="operator">=</span> <span class="number">0.02</span>,</span><br><span class="line">    autovacuum_vacuum_cost_limit <span class="operator">=</span> <span class="number">1000</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>If updated columns are not indexed, consider <code>fillfactor</code> to leave room for HOT updates:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> high_churn_table <span class="keyword">SET</span> (</span><br><span class="line">    fillfactor <span class="operator">=</span> <span class="number">90</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>Third, create a low-traffic REINDEX procedure.</p><p>Not every index needs scheduled rebuilding. But indexes on known high-churn tables should have a review rule:</p><ul><li>bloat ratio crosses a threshold</li><li>an individual index crosses a size threshold</li><li>write&#x2F;delete volume remains high</li><li>a code change is planned and the table already shows bloat symptoms</li></ul><p>Fourth, clean up temporary backup tables.</p><p>Many databases accumulate <code>_backup</code>, <code>_bak</code>, <code>_copy</code>, or date-suffixed tables. They may not cause index bloat directly, but they consume capacity and add noise to investigations. If a temporary backup table enters the main database, it needs a retention window, an owner, and a cleanup rule.</p><h2 id="7-Long-Term-Remediation-Reduce-Write-Amplification"><a href="#7-Long-Term-Remediation-Reduce-Write-Amplification" class="headerlink" title="7. Long-Term Remediation: Reduce Write Amplification"></a>7. Long-Term Remediation: Reduce Write Amplification</h2><p>Database-level <code>REINDEX</code> can fix already-bloated files. It cannot prevent recurrence. Long-term remediation has to change the write model.</p><p>For periodic derived data, staging + diff is usually a better pattern:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Upstream raw data</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">staging table or in-memory structure</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">Diff against current effective rows by business key</span><br><span class="line">        |</span><br><span class="line">        +-- New: insert</span><br><span class="line">        +-- Changed: update</span><br><span class="line">        +-- Missing: soft disable or small-batch cleanup</span><br><span class="line">        +-- Same: no write</span><br></pre></td></tr></table></figure><p>The key is to stop deleting and rebuilding a broad future range on every run. Only rows that actually changed should be written.</p><p>Configuration screens should follow the same idea. Saving configuration should distinguish:</p><ul><li>normal state, threshold, or priority changes: update by primary key or business key</li><li>incompatible structural changes: clear and rebuild only when necessary</li><li>expired historical data: archive or partition cleanup, not unbounded deletes on the primary table</li></ul><p>If data has a natural time lifecycle, partitioning may also be worth evaluating:</p><ul><li>current and future periods stay in active partitions</li><li>historical partitions become read-only or archived</li><li>data beyond retention is removed with <code>DROP PARTITION</code></li><li>broad deletes on a large primary table are avoided</li></ul><p>Finally, index design should enter engineering review. Each new index should answer:</p><ul><li>which interface or SQL does it serve</li><li>what are the typical <code>WHERE</code>, <code>JOIN</code>, and <code>ORDER BY</code> clauses</li><li>is it covered by the prefix of an existing composite index</li><li>is it on a high-churn table</li><li>how will it be verified after release with <code>pg_stat_user_indexes</code> and <code>EXPLAIN</code></li><li>who owns its removal if the query path disappears</li></ul><p>Indexes are not free. They trade space and write cost for read performance. More indexes may be acceptable on read-heavy stable tables. On high-churn derived tables, every additional index is a persistent write-amplification multiplier.</p><h2 id="8-Takeaways"><a href="#8-Takeaways" class="headerlink" title="8. Takeaways"></a>8. Takeaways</h2><p>The visible problem was PostgreSQL index bloat. The deeper issue was the combination of write-model design, ORM-generated defaults, accumulated business indexes, and missing operational lifecycle.</p><p>The short-term path is:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Find large objects</span><br><span class="line">Separate heap size from index size</span><br><span class="line">Confirm the boundary of ordinary VACUUM</span><br><span class="line">Drop confirmed-unused indexes</span><br><span class="line">REINDEX bloated indexes concurrently</span><br><span class="line">Validate capacity and query plans</span><br></pre></td></tr></table></figure><p>The long-term changes are more important:</p><ul><li>Do not store frequently rebuilt derived data as if it were stable primary data.</li><li>Do not allow ORM defaults and business indexes to accumulate without review.</li><li>Do not treat <code>idx_scan = 0</code> as the only signal for dropping an index.</li><li>Do not wait for disk alerts before maintaining database objects.</li><li>Do not use broad <code>DELETE</code> + <code>bulk_create</code> when a diff-based write model would work.</li></ul><p>When a table with only a few MB of data carries tens of GB of indexes, the issue is no longer just “clean up the database.” It is evidence that the data model, write path, index design, and maintenance process need to be designed together.</p>]]></content>
    
    
    <summary type="html">A PostgreSQL index bloat remediation note: why tables with little data can still carry huge B-tree indexes, why ordinary VACUUM does not shrink them, and how to combine REINDEX, redundant-index cleanup, and write-model changes.</summary>
    
    
    
    <category term="Database" scheme="https://www.yzhu.name/categories/Database/"/>
    
    
    <category term="PostgreSQL" scheme="https://www.yzhu.name/tags/PostgreSQL/"/>
    
    <category term="index-bloat" scheme="https://www.yzhu.name/tags/index-bloat/"/>
    
    <category term="autovacuum" scheme="https://www.yzhu.name/tags/autovacuum/"/>
    
    <category term="REINDEX" scheme="https://www.yzhu.name/tags/REINDEX/"/>
    
    <category term="database-architecture" scheme="https://www.yzhu.name/tags/database-architecture/"/>
    
  </entry>
  
  <entry>
    <title>PostgreSQL 索引膨胀复盘：为什么表很小，索引却很大</title>
    <link href="https://www.yzhu.name/2026/07/06/postgresql-index-bloat-remediation/"/>
    <id>https://www.yzhu.name/2026/07/06/postgresql-index-bloat-remediation/</id>
    <published>2026-07-06T02:56:16.000Z</published>
    <updated>2026-07-06T02:56:16.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <a href="/2026/07/06/postgresql-index-bloat-remediation-en/">English Version</a> | <strong>中文版</strong></p></blockquote><p>上周生产环境触发了一次数据库容量报警。第一眼看上去，像是某几张表突然变大了；但真正开始排查以后，发现事情没有那么直接。几张业务表的数据量并不大，有的表甚至已经没有有效数据，但索引文件仍然占用了大量磁盘空间。普通 <code>VACUUM</code> 已经运行过，<code>n_dead_tup</code> 看起来也不高，但库文件大小没有明显下降。<span id="more"></span></p><p>这类问题很容易被误判为“数据库该清理了”，但真正的关键不在清理命令本身，而在判断问题到底发生在 heap 还是 index。本文重点整理判断过程：为什么会发生、为什么 <code>VACUUM</code> 没解决、短期怎么止血，以及长期应该改哪些架构和流程。</p><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><ul><li>PostgreSQL 的普通 <code>VACUUM</code> 可以清理 heap 中的 dead tuple，让空间可复用，但不会自动把已经膨胀的 B-tree 索引文件缩小。</li><li>表数据很小、索引很大时，要优先区分是 heap 膨胀还是 index bloat。只看 <code>n_dead_tup</code> 容易误判。</li><li>高频删除、重建、批量更新的“运行态派生表”，如果长期承载在多索引主业务表上，会持续制造索引写入放大。</li><li>短期治理通常是删除确认无用的索引，再用 <code>REINDEX CONCURRENTLY</code> 在线重建膨胀索引。</li><li>长期治理必须回到写入模型：用 staging + diff 替代整片 <code>DELETE</code> + <code>bulk_create</code>，并建立索引生命周期评审和监控。</li></ul><h2 id="1-现象：表数据很小，索引却很大"><a href="#1-现象：表数据很小，索引却很大" class="headerlink" title="1. 现象：表数据很小，索引却很大"></a>1. 现象：表数据很小，索引却很大</h2><p>排查的入口通常是库容量异常。第一步不是直接执行清理脚本，而是拆分表数据和索引空间。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname <span class="keyword">AS</span> table_name,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid)) <span class="keyword">AS</span> total_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.oid)) <span class="keyword">AS</span> table_data,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid) <span class="operator">-</span> pg_relation_size(c.oid)) <span class="keyword">AS</span> indexes,</span><br><span class="line">    c.reltuples::<span class="type">bigint</span> <span class="keyword">AS</span> approx_rows</span><br><span class="line"><span class="keyword">FROM</span> pg_class c</span><br><span class="line"><span class="keyword">JOIN</span> pg_namespace n <span class="keyword">ON</span> n.oid <span class="operator">=</span> c.relnamespace</span><br><span class="line"><span class="keyword">WHERE</span> c.relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> n.nspname <span class="operator">=</span> <span class="string">&#x27;public&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_total_relation_size(c.oid) <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>问题大致长这样：</p><table><thead><tr><th>表类型</th><th align="right">总大小</th><th align="right">表数据</th><th align="right">索引</th><th align="right">估算行数</th></tr></thead><tbody><tr><td>周期派生类表</td><td align="right">50+ GB</td><td align="right">几 MB</td><td align="right">50+ GB</td><td align="right">几万</td></tr><tr><td>规则明细类表</td><td align="right">数 GB</td><td align="right">几百 KB</td><td align="right">数 GB</td><td align="right">几千</td></tr><tr><td>执行记录类表</td><td align="right">数百 MB</td><td align="right">0 bytes</td><td align="right">数百 MB</td><td align="right">0</td></tr><tr><td>扩展属性类表</td><td align="right">数百 MB</td><td align="right">几百 KB</td><td align="right">数百 MB</td><td align="right">几千</td></tr></tbody></table><p>这个形态已经说明问题不在“业务数据本身很多”，而在历史写入行为把索引文件撑大了。</p><p>继续看 autovacuum 统计：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname,</span><br><span class="line">    n_live_tup,</span><br><span class="line">    n_dead_tup,</span><br><span class="line">    last_autovacuum,</span><br><span class="line">    last_autoanalyze,</span><br><span class="line">    autovacuum_count,</span><br><span class="line">    autoanalyze_count,</span><br><span class="line">    vacuum_count</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_user_tables</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> n_dead_tup <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>这里容易出现一个误判：有些表的 <code>n_dead_tup</code> 已经是 0，于是认为膨胀已经被 autovacuum 处理掉了。但空间仍然没有下降，说明问题主要不在 heap，而在索引。</p><p>再看索引大小和使用情况：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname <span class="keyword">AS</span> table_name,</span><br><span class="line">    indexrelname <span class="keyword">AS</span> index_name,</span><br><span class="line">    pg_size_pretty(pg_relation_size(indexrelid)) <span class="keyword">AS</span> index_size,</span><br><span class="line">    idx_scan,</span><br><span class="line">    idx_tup_read,</span><br><span class="line">    idx_tup_fetch</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_user_indexes</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_relation_size(indexrelid) <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">30</span>;</span><br></pre></td></tr></table></figure><p>常见现象是：</p><ul><li>单个 B-tree 索引膨胀到十几 GB；</li><li>某些索引 <code>idx_scan = 0</code>，但仍占用大量空间；</li><li>一些表已经空了，索引文件还在；</li><li>主键索引、时间索引、外键默认索引都可能被历史 churn 放大。</li></ul><p>这时再继续跑普通 <code>VACUUM (ANALYZE)</code>，通常不会得到预期效果。</p><h2 id="2-为什么普通-VACUUM-没有缩小索引"><a href="#2-为什么普通-VACUUM-没有缩小索引" class="headerlink" title="2. 为什么普通 VACUUM 没有缩小索引"></a>2. 为什么普通 VACUUM 没有缩小索引</h2><p>PostgreSQL 使用 MVCC。一次 <code>UPDATE</code> 不是原地覆盖，而是写入新行版本，再把旧行版本标记为不可见；<code>DELETE</code> 也不是立即把物理空间归还给操作系统，而是先产生可回收空间。</p><p>普通 <code>VACUUM</code> 的核心作用是清理 heap 里的 dead tuple，让这些空间在同一个表文件里复用。它也会清理索引中指向 dead tuple 的条目，但它不会把已经扩张的 B-tree 文件自动缩回很小。</p><p>所以会出现这种看起来矛盾的状态：</p><ul><li><code>n_dead_tup = 0</code>；</li><li>表数据只有几 MB；</li><li>索引仍然几十 GB；</li><li>继续 <code>VACUUM</code>，磁盘空间基本不变。</li></ul><p>这不是 <code>VACUUM</code> 没执行，而是它解决的问题和我们当前遇到的问题不是同一个。</p><p>如果目标是缩小已经膨胀的索引文件，通常需要：</p><ul><li><code>REINDEX INDEX CONCURRENTLY index_name</code>；</li><li><code>REINDEX TABLE CONCURRENTLY table_name</code>；</li><li>或用 <code>pg_repack</code> 一类工具重建对象。</li></ul><p><code>VACUUM FULL</code> 也能重写表并回收空间，但它锁表重，在线业务通常不作为首选。PostgreSQL 12 之后支持 <code>REINDEX CONCURRENTLY</code>，对在线治理会友好很多，但仍然会消耗 IO，并且新旧索引会短时间共存，需要提前评估磁盘余量。</p><h2 id="3-根因一：高频变化数据被当成长生命周期主表"><a href="#3-根因一：高频变化数据被当成长生命周期主表" class="headerlink" title="3. 根因一：高频变化数据被当成长生命周期主表"></a>3. 根因一：高频变化数据被当成长生命周期主表</h2><p>这类问题的根本原因通常不是一个数据库参数，而是业务写入模型和存储模型错配。</p><p>以报表结果、规则命中明细、任务执行窗口、临时计算结果这类派生数据为例，它们的业务特征是：</p><ul><li>上游数据会定期刷新；</li><li>配置后台会批量调整；</li><li>历史周期会过期；</li><li>某些计算规则变化后，未来一段时间的数据需要重算。</li></ul><p>这些数据天然是高 churn 的运行态派生数据。但在很多系统里，它们被设计成长期主业务表，并叠加大量索引，最后写入模式变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">删除某个对象未来一段时间的派生记录</span><br><span class="line">批量创建新的周期记录</span><br><span class="line">批量创建明细记录</span><br><span class="line">批量创建关联状态记录</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">计算规则变化</span><br><span class="line">清空旧的派生结构</span><br><span class="line">按新配置重新生成</span><br></pre></td></tr></table></figure><p>在小数据量阶段，这种做法简单直接，也容易保证业务正确。但在 PostgreSQL MVCC 下，长期运行后会持续制造 dead tuple 和索引垃圾。更麻烦的是，每多一棵索引，删除、插入和更新都要多维护一次 B-tree。业务层以为自己只是在重建一片派生结果，数据库层实际承受的是多倍写入放大。</p><p>这也是为什么单纯调高 autovacuum 不够。autovacuum 可以缓解堆积，但它不能消除“把高 churn 派生数据写成多索引长期主表”的根因。</p><h2 id="4-根因二：ORM-默认索引和业务索引叠加"><a href="#4-根因二：ORM-默认索引和业务索引叠加" class="headerlink" title="4. 根因二：ORM 默认索引和业务索引叠加"></a>4. 根因二：ORM 默认索引和业务索引叠加</h2><p>另一个常见来源是索引不是统一设计出来的，而是逐步叠加出来的。</p><p>例如：</p><ul><li>外键字段被 ORM 默认建了单列索引；</li><li>业务查询又加了复合索引；</li><li>排序字段加了 <code>db_index=True</code>；</li><li>唯一约束自带索引；</li><li>为了某个历史接口加过 LIKE 查询索引；</li><li>后来查询逻辑变了，但索引没有下线。</li></ul><p>最终一张高写入表可能同时存在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">(entity_id)</span><br><span class="line">(rule_id)</span><br><span class="line">(effective_date)</span><br><span class="line">(entity_id, rule_id)</span><br><span class="line">(entity_id, effective_date)</span><br><span class="line">(entity_id, status, effective_date)</span><br><span class="line">PRIMARY KEY (id)</span><br><span class="line">UNIQUE (entity_id, attribute_id, value_id)</span><br></pre></td></tr></table></figure><p>其中有些索引确实在服务高频查询，有些只是历史遗留，有些已经被复合索引前缀覆盖。</p><p>判断冗余索引时，不能只看 <code>idx_scan = 0</code> 就马上删除。至少要同时检查：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 统计信息是否刚被重置</span></span><br><span class="line"><span class="keyword">SELECT</span> datname, stats_reset</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_database</span><br><span class="line"><span class="keyword">WHERE</span> datname <span class="operator">=</span> current_database();</span><br></pre></td></tr></table></figure><p>还要确认：</p><ul><li>是否是唯一约束或主键约束背后的索引；</li><li>是否被外键维护依赖；</li><li>是否有低频但关键的后台任务使用；</li><li>是否被 ORM 某些隐式查询路径使用；</li><li>是否能被已有复合索引覆盖；</li><li>删除后关键 SQL 的 <code>EXPLAIN</code> 是否劣化。</li></ul><p>更稳妥的做法是把候选索引观察一段时间，结合慢查询、接口访问路径、<code>EXPLAIN</code> 和业务任务周期判断。确认无用后，再用 <code>DROP INDEX CONCURRENTLY</code> 分批删除。</p><h2 id="5-短期止血：删除无用索引和在线重建"><a href="#5-短期止血：删除无用索引和在线重建" class="headerlink" title="5. 短期止血：删除无用索引和在线重建"></a>5. 短期止血：删除无用索引和在线重建</h2><p>短期治理可以分成三步。</p><p>第一步，记录执行前状态。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(oid)) <span class="keyword">AS</span> total_before</span><br><span class="line"><span class="keyword">FROM</span> pg_class</span><br><span class="line"><span class="keyword">WHERE</span> relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_total_relation_size(oid) <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>同时检查长事务和无效索引：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    pid,</span><br><span class="line">    usename,</span><br><span class="line">    state,</span><br><span class="line">    xact_start,</span><br><span class="line">    now() <span class="operator">-</span> xact_start <span class="keyword">AS</span> xact_age,</span><br><span class="line">    query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> xact_start <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> xact_start;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname <span class="keyword">AS</span> index_name,</span><br><span class="line">    i.indisvalid,</span><br><span class="line">    i.indisready</span><br><span class="line"><span class="keyword">FROM</span> pg_index i</span><br><span class="line"><span class="keyword">JOIN</span> pg_class c <span class="keyword">ON</span> c.oid <span class="operator">=</span> i.indexrelid</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">NOT</span> i.indisvalid <span class="keyword">OR</span> <span class="keyword">NOT</span> i.indisready;</span><br></pre></td></tr></table></figure><p>第二步，删除确认无用的索引。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> INDEX CONCURRENTLY IF <span class="keyword">EXISTS</span> idx_unused_column;</span><br></pre></td></tr></table></figure><p><code>CONCURRENTLY</code> 可以降低对线上读写的影响，但仍然会消耗资源。每次只处理少量索引，执行后观察业务指标和数据库 IO。</p><p>第三步，在线重建膨胀索引。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">REINDEX INDEX CONCURRENTLY idx_large_active_index;</span><br></pre></td></tr></table></figure><p>如果一张表多个索引都膨胀，也可以评估：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">REINDEX <span class="keyword">TABLE</span> CONCURRENTLY high_churn_table;</span><br></pre></td></tr></table></figure><p>需要注意几点：</p><ul><li><code>REINDEX CONCURRENTLY</code> 不能放在事务块里执行；</li><li>失败时可能留下 <code>_ccnew</code> 之类无效索引，需要清理后重试；</li><li>重建期间新旧索引共存，要预留额外磁盘空间；</li><li>重建会带来 IO 压力，应该放在低峰窗口分批执行；</li><li>重建后要重新对比对象大小和关键查询计划。</li></ul><p>治理后验证仍然使用同一组 SQL，不要换口径：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname <span class="keyword">AS</span> table_name,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid)) <span class="keyword">AS</span> total_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.oid)) <span class="keyword">AS</span> table_data,</span><br><span class="line">    pg_size_pretty(pg_total_relation_size(c.oid) <span class="operator">-</span> pg_relation_size(c.oid)) <span class="keyword">AS</span> indexes</span><br><span class="line"><span class="keyword">FROM</span> pg_class c</span><br><span class="line"><span class="keyword">JOIN</span> pg_namespace n <span class="keyword">ON</span> n.oid <span class="operator">=</span> c.relnamespace</span><br><span class="line"><span class="keyword">WHERE</span> c.relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> n.nspname <span class="operator">=</span> <span class="string">&#x27;public&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_total_relation_size(c.oid) <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>再用 <code>EXPLAIN ANALYZE</code> 抽样验证核心查询，确保删除索引没有换来明显的查询退化。</p><h2 id="6-中期治理：把索引纳入生命周期"><a href="#6-中期治理：把索引纳入生命周期" class="headerlink" title="6. 中期治理：把索引纳入生命周期"></a>6. 中期治理：把索引纳入生命周期</h2><p>索引治理不应该只在磁盘告警后发生。比较有效的中期措施包括四类。</p><p>第一，监控索引大小和使用率。</p><p>要长期记录：</p><ul><li>单索引大小；</li><li>表数据大小与索引大小比例；</li><li><code>idx_scan</code> 增长情况；</li><li><code>idx_tup_read</code> &#x2F; <code>idx_tup_fetch</code>；</li><li><code>pg_stat_database.stats_reset</code> 时间。</li></ul><p>如果统计被重置，<code>idx_scan = 0</code> 只能说明“重置后未使用”，不能说明“长期未使用”。</p><p>第二，对高 churn 表单独调 autovacuum。</p><p>全局默认值通常偏保守。对频繁更新的表，可以使用表级 reloptions：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> high_churn_table <span class="keyword">SET</span> (</span><br><span class="line">    autovacuum_vacuum_scale_factor <span class="operator">=</span> <span class="number">0.05</span>,</span><br><span class="line">    autovacuum_vacuum_threshold <span class="operator">=</span> <span class="number">1000</span>,</span><br><span class="line">    autovacuum_analyze_scale_factor <span class="operator">=</span> <span class="number">0.02</span>,</span><br><span class="line">    autovacuum_vacuum_cost_limit <span class="operator">=</span> <span class="number">1000</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>如果更新的列不在索引中，还可以评估 <code>fillfactor</code>，给 HOT update 留空间：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> high_churn_table <span class="keyword">SET</span> (</span><br><span class="line">    fillfactor <span class="operator">=</span> <span class="number">90</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>第三，建立低峰 REINDEX 流程。</p><p>对明确高 churn 且历史上膨胀过的索引，可以建立定期巡检和维护流程。不是所有索引都需要固定重建，但应该有一套判断标准：</p><ul><li>膨胀比例超过阈值；</li><li>单索引超过容量阈值；</li><li>写入量和删除量持续高；</li><li>最近业务改造前需要先止血。</li></ul><p>第四，清理临时备份表。</p><p>很多库里都会残留 <code>_backup</code>、<code>_bak</code>、<code>_copy</code>、日期后缀表。它们不一定导致索引膨胀，但会长期占用容量，也会干扰排查。临时备份表进入主库后，应该有保留期、负责人和清理机制。</p><h2 id="7-长期治理：减少写入放大"><a href="#7-长期治理：减少写入放大" class="headerlink" title="7. 长期治理：减少写入放大"></a>7. 长期治理：减少写入放大</h2><p>数据库层的 <code>REINDEX</code> 只能修复已经膨胀的文件，不能阻止问题复发。长期治理要回到写入模型。</p><p>对于周期性派生数据，更合理的模式是 staging + diff。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">上游原始数据</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">staging 表或内存结构</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">与线上有效数据按业务键 diff</span><br><span class="line">        |</span><br><span class="line">        +-- 新增：insert</span><br><span class="line">        +-- 变化：update</span><br><span class="line">        +-- 消失：soft disable 或小批量清理</span><br><span class="line">        +-- 不变：不写</span><br></pre></td></tr></table></figure><p>关键点是：不要每次都删除未来一整片派生记录再重建。只有真正变化的记录才写入数据库。</p><p>配置后台也一样。保存配置时应该区分：</p><ul><li>普通状态、阈值、优先级调整：按主键或业务唯一键增量更新；</li><li>不兼容结构变化：才允许清空并重建；</li><li>历史过期数据：走归档或分区清理，不在主表上无限累积。</li></ul><p>如果数据天然按时间生命周期变化，可以评估分区：</p><ul><li>当前和未来周期保留在活跃分区；</li><li>历史分区只读或归档；</li><li>超过保留期直接 <code>DROP PARTITION</code>；</li><li>避免在大主表上做持续大批量 <code>DELETE</code>。</li></ul><p>最后，索引设计要进入开发评审流程。新增一个索引至少说明：</p><ul><li>服务哪个接口或 SQL；</li><li>典型 <code>WHERE</code>、<code>JOIN</code>、<code>ORDER BY</code> 条件是什么；</li><li>是否被已有复合索引前缀覆盖；</li><li>是否位于高 churn 表；</li><li>上线后如何用 <code>pg_stat_user_indexes</code> 和 <code>EXPLAIN</code> 复核；</li><li>如果未来不用了，谁负责下线。</li></ul><p>索引不是免费的。它用空间换读取性能，也会增加写入成本。对读多写少的主数据表，索引多一些通常可以接受；对高 churn 派生表，多一个索引就是持续的写入放大器。</p><h2 id="8-复盘"><a href="#8-复盘" class="headerlink" title="8. 复盘"></a>8. 复盘</h2><p>这次问题表面上是 PostgreSQL 索引膨胀，深层是业务写入模型、ORM 默认行为和运维流程共同作用的结果。</p><p>短期看，处理路径是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">定位大对象</span><br><span class="line">区分 heap 和 index</span><br><span class="line">确认普通 VACUUM 的边界</span><br><span class="line">删除无用索引</span><br><span class="line">REINDEX CONCURRENTLY 重建膨胀索引</span><br><span class="line">验证容量和查询计划</span><br></pre></td></tr></table></figure><p>长期看，真正需要改变的是：</p><ul><li>不把高频重建的派生数据简单当成长生命周期主表；</li><li>不让 ORM 默认索引和业务索引无审查地叠加；</li><li>不把 <code>idx_scan = 0</code> 当成唯一判断依据；</li><li>不把数据库维护留到磁盘告警之后；</li><li>不用 <code>delete + bulk_create</code> 处理本可以 diff 的数据。</li></ul><p>当一张几 MB 的表背着几十 GB 的索引时，问题已经不是“清理一下数据库”这么简单。它提醒我们：数据模型、写入方式、索引设计和运维流程必须一起设计，否则数据库迟早会把这些历史成本以容量、IO 和维护窗口的形式还回来。</p>]]></content>
    
    
    <summary type="html">一次 PostgreSQL 索引膨胀治理复盘：表数据很小但索引巨大，普通 VACUUM 无法缩小 B-tree 文件，最终需要从索引重建、冗余索引治理和写入模型改造三层处理。</summary>
    
    
    
    <category term="Database" scheme="https://www.yzhu.name/categories/Database/"/>
    
    
    <category term="PostgreSQL" scheme="https://www.yzhu.name/tags/PostgreSQL/"/>
    
    <category term="index-bloat" scheme="https://www.yzhu.name/tags/index-bloat/"/>
    
    <category term="autovacuum" scheme="https://www.yzhu.name/tags/autovacuum/"/>
    
    <category term="REINDEX" scheme="https://www.yzhu.name/tags/REINDEX/"/>
    
    <category term="database-architecture" scheme="https://www.yzhu.name/tags/database-architecture/"/>
    
  </entry>
  
  <entry>
    <title>网关层 Per-Client 鉴权隔离：一个 IAM 平台保护多个业务应用</title>
    <link href="https://www.yzhu.name/2026/06/07/per-client-auth-isolation-gateway/"/>
    <id>https://www.yzhu.name/2026/06/07/per-client-auth-isolation-gateway/</id>
    <published>2026-06-07T02:00:00.000Z</published>
    <updated>2026-06-07T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 语言</strong>: <strong>中文版</strong> | <a href="/2026/06/07/per-client-auth-isolation-gateway-en/">English Version</a></p></blockquote><blockquote><p><strong>场景前提</strong>：系统已经有一个基于 OAuth2&#x2F;OIDC 的统一 IAM 平台，API 网关（APISIX）通过 forward-auth 插件集中鉴权。网关路由和浏览器会话的前置设计，可参考 <a href="/2026/02/27/microservice-gateway-oauth2-routing/">微服务网关路由设计</a> 和 <a href="/2026/04/25/hmac-cookie-redis-sid-browser-session/">HMAC Cookie + Redis SID 双层会话</a>。</p></blockquote><span id="more"></span><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>这篇讨论的是统一 IAM 保护多个业务应用时，如何避免一个应用签发的 Token 被拿去访问另一个应用。核心做法是在 APISIX 路由上声明预期 OAuth2 Client，通过 <code>X-Expected-Client-Id</code> 传给 forward-auth，由鉴权服务比对 JWT 中的 <code>client_id</code>。</p><p>这层机制只解决”Token 属于哪个客户端”的问题，不替代用户准入、组织授权、会话撤销和业务权限。示例中的应用名、Client ID、路由前缀均为中性占位，不对应任何实际部署。</p><h2 id="1-问题：共享-IAM-下的-Token-越权风险"><a href="#1-问题：共享-IAM-下的-Token-越权风险" class="headerlink" title="1. 问题：共享 IAM 下的 Token 越权风险"></a>1. 问题：共享 IAM 下的 Token 越权风险</h2><p>当多个内部业务应用共用一个 IAM 平台时，最直觉的做法是：用户登录后拿到一个 JWT，所有应用都认这个 Token。问题在于——</p><blockquote><p><strong>应用 A 签发的 Token，能不能拿去访问应用 B 的接口？</strong></p></blockquote><p>OAuth2 协议中，Token 是签发给特定 Client 的。但网关层如果不校验”这个 Token 属于哪个 Client”，任何持有有效 Token 的请求都能穿透到任意后端服务。这在企业内部看似无害（都是自家员工），但会引发实际问题：</p><ul><li><strong>权限模型被绕过</strong>：应用 B 有独立的准入策略（如只允许特定部门使用），但网关只检查”Token 是否有效”，策略形同虚设。</li><li><strong>跨应用数据泄露</strong>：前端 JS 可以从 localStorage 读取 Token，手动调用其他应用的 API。</li><li><strong>审计链断裂</strong>：无法在网关层区分”这个请求是哪个应用的客户端发起的”。</li></ul><p>核心矛盾：<strong>网关只做了”认证”（Authentication），没有做”客户端绑定”（Client Binding）。</strong></p><hr><h2 id="2-架构决策：网关层注入预期-Client-ID"><a href="#2-架构决策：网关层注入预期-Client-ID" class="headerlink" title="2. 架构决策：网关层注入预期 Client ID"></a>2. 架构决策：网关层注入预期 Client ID</h2><h3 id="2-1-方案对比"><a href="#2-1-方案对比" class="headerlink" title="2.1 方案对比"></a>2.1 方案对比</h3><table><thead><tr><th>方案</th><th>做法</th><th>问题</th></tr></thead><tbody><tr><td>每个应用独立鉴权</td><td>后端服务各自校验 Token 的 client_id</td><td>鉴权逻辑散布，策略变更需要逐个服务改代码</td></tr><tr><td>网关只校验 Token 有效性</td><td>forward-auth 只检查 JWT 签名和过期</td><td>不解决跨应用 Token 滥用</td></tr><tr><td><strong>网关注入预期 Client ID</strong></td><td>路由配置声明”本路由只接受哪个 Client 的 Token”，鉴权服务校验绑定关系</td><td>集中管控，新增应用不需要改鉴权服务代码</td></tr></tbody></table><p>选择第三种方案。路由配置是声明式的，新增应用只需增加一段网关路由配置，无需改鉴权服务的代码。</p><h3 id="2-2-机制概览"><a href="#2-2-机制概览" class="headerlink" title="2.2 机制概览"></a>2.2 机制概览</h3><pre class="mermaid">sequenceDiagram    participant Client as 浏览器/客户端    participant GW as APISIX 网关    participant Auth as IAM 鉴权服务    participant Backend as 业务后端    Client->>GW: 请求 /api/app-a/tasks (带 Bearer Token)    Note over GW: 1. 路由匹配 → 应用 A 路由规则    Note over GW: 2. Lua 注入 X-Expected-Client-Id: app-a-web    GW->>Auth: forward-auth (Authorization + X-Expected-Client-Id)    Note over Auth: 3. 解码 JWT → 提取 client_id    Note over Auth: 4. 校验 JWT.client_id == X-Expected-Client-Id    alt Client ID 匹配 + 准入通过        Auth-->>GW: 200 OK + X-User-Id, X-Sid 等身份 Header        GW->>Backend: 注入身份 Header，转发请求        Backend-->>Client: 返回业务数据    else Client ID 不匹配        Auth-->>GW: 401 Unauthorized        GW-->>Client: 401 Unauthorized    end</pre><p>关键设计点：</p><ol><li><strong>路由配置声明 Client ID</strong>：每条路由通过网关配置声明自己绑定的 OAuth2 Client。</li><li><strong>Lua 函数注入 Header</strong>：请求进入路由后，先执行 <code>serverless-pre-function</code> 写入 <code>X-Expected-Client-Id</code> Header。</li><li><strong>forward-auth 携带到鉴权服务</strong>：<code>request_headers</code> 配置确保这个 Header 被转发。</li><li><strong>鉴权服务比对绑定关系</strong>：从 JWT 中提取 <code>client_id</code> 声明，与 <code>X-Expected-Client-Id</code> 比对。</li></ol><hr><h2 id="3-实现：APISIX-路由配置"><a href="#3-实现：APISIX-路由配置" class="headerlink" title="3. 实现：APISIX 路由配置"></a>3. 实现：APISIX 路由配置</h2><h3 id="3-1-路由配置结构"><a href="#3-1-路由配置结构" class="headerlink" title="3.1 路由配置结构"></a>3.1 路由配置结构</h3><p>每个业务应用对应一段独立的网关路由配置，声明路由、上游和认证方式。以应用 A 为例：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;routes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-a-api-route&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/api/app-a/*&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;apps.example.com&quot;</span><span class="punctuation">,</span> <span class="string">&quot;console.example.com&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">91</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;upstream_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-a-upstream&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugin_config_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;protected-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;serverless-pre-function&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;phase&quot;</span><span class="punctuation">:</span> <span class="string">&quot;access&quot;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;functions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;return function() ngx.req.set_header(\&quot;X-Expected-Client-Id\&quot;, \&quot;app-a-web\&quot;) end&quot;</span></span><br><span class="line">          <span class="punctuation">]</span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>执行顺序：</p><ol><li><code>serverless-pre-function</code>（<code>access</code> 阶段）：设置 <code>X-Expected-Client-Id</code> Header</li><li><code>forward-auth</code> 插件（引用 <code>protected-api-auth</code> 配置）：将 Header 连同 <code>Authorization</code> 发给鉴权服务</li><li>鉴权通过后，<code>proxy-rewrite</code> 转发到后端</li></ol><h3 id="3-2-全局-forward-auth-配置"><a href="#3-2-全局-forward-auth-配置" class="headerlink" title="3.2 全局 forward-auth 配置"></a>3.2 全局 forward-auth 配置</h3><p>两个可复用的 plugin_config，定义在全局网关配置中：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;protected-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;forward-auth&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://127.0.0.1:9080/__internal/auth/gateway/verify?gateway_secret=$&#123;GATEWAY_AUTH_SECRET&#125;&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;request_headers&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;Authorization&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Trace-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Request-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Expected-Client-Id&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;upstream_headers&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;X-User-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Sid&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-User-Email&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-User-Display-Name&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Client-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Idp&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Idp-User-Id&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;timeout&quot;</span><span class="punctuation">:</span> <span class="number">5000</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;status_on_error&quot;</span><span class="punctuation">:</span> <span class="number">503</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>request_headers</code> 中的 <code>X-Expected-Client-Id</code> 是关键——只有声明了这个 Header，forward-auth 子请求才会携带它。如果路由没有通过 Lua 设置这个 Header，forward-auth 会发一个空的 <code>X-Expected-Client-Id</code>，鉴权服务会拒绝请求。</p><h3 id="3-3-内部鉴权路由保护"><a href="#3-3-内部鉴权路由保护" class="headerlink" title="3.3 内部鉴权路由保护"></a>3.3 内部鉴权路由保护</h3><p>forward-auth 调用的是 APISIX 自身的内部路由（<code>127.0.0.1:9080</code>），需要防止外部直接访问：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;auth-gateway-verify-internal&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/__internal/auth/gateway/verify&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;127.0.0.1&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">110</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;vars&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">[</span><span class="string">&quot;arg_gateway_secret&quot;</span><span class="punctuation">,</span> <span class="string">&quot;==&quot;</span><span class="punctuation">,</span> <span class="string">&quot;$&#123;GATEWAY_AUTH_SECRET&#125;&quot;</span><span class="punctuation">]</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;upstream_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;auth-service-upstream&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>三层保护：</p><ul><li><strong>hosts: 127.0.0.1</strong>：只接受本机回环地址的请求</li><li><strong>vars: gateway_secret</strong>：查询参数必须携带共享密钥</li><li><strong>priority: 110</strong>：高于所有业务路由，确保内部路由优先匹配</li></ul><hr><h2 id="4-两种认证模式"><a href="#4-两种认证模式" class="headerlink" title="4. 两种认证模式"></a>4. 两种认证模式</h2><p>实际业务中不是所有接口都需要严格的 Client 绑定。比如 <code>/me</code>（获取当前用户信息），用户从任何应用登录后都应该能访问。因此设计了两种模式：</p><h3 id="4-1-Protected-模式（默认）"><a href="#4-1-Protected-模式（默认）" class="headerlink" title="4.1 Protected 模式（默认）"></a>4.1 Protected 模式（默认）</h3><p>用于业务 API，强制 Client 绑定。</p><ul><li>Lua 设置 <code>X-Expected-Client-Id</code></li><li>forward-auth 调用 <code>/__internal/auth/gateway/verify</code></li><li>鉴权服务校验 <code>JWT.client_id == X-Expected-Client-Id</code>，不匹配则 401</li></ul><h3 id="4-2-Authenticated-模式"><a href="#4-2-Authenticated-模式" class="headerlink" title="4.2 Authenticated 模式"></a>4.2 Authenticated 模式</h3><p>用于通用用户信息接口，只要求登录状态。</p><ul><li>Lua 设置 <code>X-Auth-Mode: authenticated</code></li><li>forward-auth 调用 <code>/__internal/auth/gateway/session</code>（内部路由会额外设置 <code>X-Auth-Mode</code>）</li><li>鉴权服务跳过 Client ID 校验，只验证 Token 有效 + Session 活跃</li></ul><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;account-profile-authenticated-route&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/api/account/profile/*&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">95</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugin_config_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;authenticated-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;serverless-pre-function&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;phase&quot;</span><span class="punctuation">:</span> <span class="string">&quot;access&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;functions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="string">&quot;return function() ngx.req.set_header(\&quot;X-Auth-Mode\&quot;, \&quot;authenticated\&quot;) end&quot;</span></span><br><span class="line">      <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>两种模式最终都路由到鉴权服务的同一个内部鉴权端点，通过 <code>X-Auth-Mode</code> Header 区分行为：</p><table><thead><tr><th>模式</th><th>X-Expected-Client-Id</th><th>X-Auth-Mode</th><th>校验逻辑</th></tr></thead><tbody><tr><td>Protected</td><td>必须存在，与 JWT client_id 匹配</td><td>不设置</td><td>Client 绑定 + 完整准入策略</td></tr><tr><td>Authenticated</td><td>不需要</td><td><code>authenticated</code></td><td>仅验证登录状态</td></tr></tbody></table><h3 id="4-3-路由优先级设计"><a href="#4-3-路由优先级设计" class="headerlink" title="4.3 路由优先级设计"></a>4.3 路由优先级设计</h3><p>两种模式通过优先级区分，确保精确路由优先：</p><table><thead><tr><th>优先级</th><th>路由类型</th><th>示例</th></tr></thead><tbody><tr><td>110</td><td>内部鉴权路由</td><td><code>/__internal/auth/gateway/verify</code></td></tr><tr><td>100</td><td>公开回调（Webhook）</td><td><code>/callback/app-a/*</code>（无需鉴权）</td></tr><tr><td>95-96</td><td>Authenticated 模式 API</td><td><code>/api/account/profile/*</code></td></tr><tr><td>91</td><td>Protected 模式 API</td><td><code>/api/app-a/*</code>, <code>/api/app-b/*</code></td></tr><tr><td>80-83</td><td>静态 UI、SPA</td><td><code>/ui/app-a/*</code></td></tr><tr><td>69-70</td><td>SPA 根路由和兜底</td><td><code>/*</code></td></tr></tbody></table><p><code>/me</code> 端点的优先级（95）高于业务 API（91），确保”查看自己的信息”走 Authenticated 模式而不是被业务 API 的 Protected 规则捕获。</p><hr><h2 id="5-鉴权服务端：策略决策链"><a href="#5-鉴权服务端：策略决策链" class="headerlink" title="5. 鉴权服务端：策略决策链"></a>5. 鉴权服务端：策略决策链</h2><p>网关层完成了”这个 Token 是否属于这个 Client”的校验。鉴权服务端在此基础上执行完整的准入策略：</p><pre class="mermaid">flowchart TD    A[收到 forward-auth 请求] --> B{X-Auth-Mode?}    B -->|authenticated| C[跳过 Client 校验<br>仅检查 Session 活跃]    B -->|entitled/默认| D{X-Expected-Client-Id 存在?}    D -->|缺失| E[500 配置错误]    D -->|存在| F{JWT.client_id<br>== Expected?}    F -->|不匹配| G[401 Client 不匹配]    F -->|匹配| H[检查 Client 状态]    H -->|已禁用| I[403 Client 已禁用]    H -->|活跃| J{Access Level?}    J -->|PUBLIC| K[直接放行]    J -->|AUTHENTICATED| L[检查用户状态<br>+ Token Version]    J -->|PRIVATE| M[完整准入策略]    M --> N{用户状态?}    N -->|禁用/离职| O[403]    N -->|正常| P{Token Version<br>匹配?}    P -->|不匹配| Q[401 版本过期<br>触发重新鉴权]    P -->|匹配| R{Security Stamp<br>匹配?}    R -->|不匹配| S[401 安全戳变更]    R -->|匹配| T{系统角色<br>超级管理员?}    T -->|是| U[放行]    T -->|否| V{应用管理员?}    V -->|是| U    V -->|否| W{用户级 Entitlement}    W -->|明确 DENY| X[403]    W -->|明确 ALLOW| U    W -->|无记录| Y{组织级 Entitlement}    Y -->|任一 DENY| X    Y -->|任一 ALLOW| U    Y -->|无记录| Z[403 默认拒绝]</pre><h3 id="5-1-三级访问控制"><a href="#5-1-三级访问控制" class="headerlink" title="5.1 三级访问控制"></a>5.1 三级访问控制</h3><p>每个 OAuth2 Client 注册时设置 <code>access_level</code>，决定了准入的严格程度：</p><table><thead><tr><th>Access Level</th><th>值</th><th>含义</th><th>适用场景</th></tr></thead><tbody><tr><td><strong>PRIVATE</strong></td><td>1</td><td>必须通过完整的 Entitlement 策略链</td><td>核心业务应用、后台管理应用</td></tr><tr><td><strong>AUTHENTICATED</strong></td><td>2</td><td>只要求用户登录且状态正常</td><td>通用工具类应用、内部文档站</td></tr><tr><td><strong>PUBLIC</strong></td><td>3</td><td>Client 绑定校验通过即放行</td><td>低风险、仍需要识别调用 Client 的接口</td></tr></tbody></table><p>这个分级让鉴权服务可以根据应用的安全需求做短路判断，而不是所有应用都走完整的策略链。</p><h3 id="5-2-Entitlement-策略链（PRIVATE-级别）"><a href="#5-2-Entitlement-策略链（PRIVATE-级别）" class="headerlink" title="5.2 Entitlement 策略链（PRIVATE 级别）"></a>5.2 Entitlement 策略链（PRIVATE 级别）</h3><p>对于 <code>PRIVATE</code> 级别的应用，准入决策按优先级检查：</p><p><strong>1. 系统角色豁免</strong></p><p>超级管理员和平台级应用管理员直接放行。这是运维逃逸通道，用于紧急情况。</p><p><strong>2. 应用管理员</strong></p><p>应用管理员配置记录每个 Client 的管理员列表。应用管理员对自己的应用拥有完全访问权。</p><p><strong>3. 用户级 Entitlement</strong></p><p>用户级授权配置记录单个用户对特定应用的授权。每条记录有 <code>ALLOW</code> 或 <code>DENY</code> 两种效果。如果存在用户级记录，以该记录为准。</p><p><strong>4. 组织级 Entitlement</strong></p><p>通过组织架构关系解析用户所属的所有组织单元（含祖先），然后检查组织级授权配置。规则：</p><ul><li>任何一个组织单元的 <code>DENY</code> 优先级最高，覆盖所有 <code>ALLOW</code></li><li>没有任何 <code>DENY</code> 的情况下，至少一个 <code>ALLOW</code> 即可放行</li><li>用户级记录优先于组织级记录</li></ul><p><strong>5. 默认拒绝</strong></p><p>如果以上都没有匹配，拒绝访问。</p><h3 id="5-3-版本一致性校验"><a href="#5-3-版本一致性校验" class="headerlink" title="5.3 版本一致性校验"></a>5.3 版本一致性校验</h3><p>Token Version 和 Security Stamp 是两个独立的撤回信号：</p><table><thead><tr><th>校验项</th><th>触发变更的场景</th><th>效果</th></tr></thead><tbody><tr><td><strong>Token Version</strong></td><td>管理员修改用户角色&#x2F;权限</td><td>版本号递增，所有活跃 Token 即刻失效</td></tr><tr><td><strong>Security Stamp</strong></td><td>用户修改密码、账户被禁用</td><td>Stamp 变更，强制重新认证</td></tr></tbody></table><p>这两个机制在 <a href="/2026/04/25/hmac-cookie-redis-sid-browser-session/">HMAC Cookie + Redis SID 双层会话</a> 中有详细设计，此处不再展开。</p><hr><h2 id="6-多应用的-Client-ID-映射"><a href="#6-多应用的-Client-ID-映射" class="headerlink" title="6. 多应用的 Client ID 映射"></a>6. 多应用的 Client ID 映射</h2><p>在设计示例中，每个应用的 Client ID 格式可以灵活选择：</p><table><thead><tr><th>应用</th><th>Client ID</th><th>格式</th><th>路由前缀</th></tr></thead><tbody><tr><td>管理控制台</td><td><code>admin-console</code></td><td>简明名称</td><td><code>/api/admin/*</code></td></tr><tr><td>工作台应用</td><td><code>workbench-web</code></td><td>简明名称</td><td><code>/api/workbench/*</code></td></tr><tr><td>应用 A</td><td><code>app-a-web</code></td><td>应用 + 端类型</td><td><code>/api/app-a/*</code></td></tr><tr><td>应用 B</td><td><code>app-b-service</code></td><td>应用 + 端类型</td><td><code>/api/app-b/*</code></td></tr><tr><td>应用 C</td><td><code>app-c-web-r7k3</code></td><td>应用 + 端类型 + 随机后缀</td><td><code>/api/app-c/*</code></td></tr></tbody></table><p>几种格式各有考量：</p><ul><li><strong>简明名称</strong>（如 <code>admin-console</code>）：可读性好，运维排查时一眼能识别来源应用。适合内部系统和管理后台。</li><li><strong>应用 + 端类型</strong>（如 <code>app-a-web</code>）：能区分同一应用的 Web、移动端、服务端 Client，便于审计。</li><li><strong>随机后缀</strong>（如 <code>app-c-web-r7k3</code>）：主要用于降低命名冲突和误猜枚举风险，不是安全边界。Token 防伪仍依赖 JWT 签名、issuer&#x2F;audience&#x2F;client_id 校验、client secret 或 PKCE 等机制。</li></ul><p>Client ID 的值不影响隔离机制本身——网关和鉴权服务只做精确匹配，不关心格式。</p><hr><h2 id="7-新增应用的接入流程"><a href="#7-新增应用的接入流程" class="headerlink" title="7. 新增应用的接入流程"></a>7. 新增应用的接入流程</h2><p>基于这套机制，新增一个业务应用只需三步：</p><p><strong>步骤 1：注册 OAuth2 Client</strong></p><p>通过管理 API 创建 Client，设置 <code>access_level</code> 和对应的客户端认证方式。系统生成或记录 Client ID。</p><p><strong>步骤 2：添加网关路由配置</strong></p><p>在网关配置仓库中新增一段应用路由配置，声明路由和 Lua 注入的 Client ID：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;routes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-x-api-route&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/api/app-x/*&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;apps.example.com&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">91</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;upstream_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-x-upstream&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugin_config_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;protected-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;serverless-pre-function&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;phase&quot;</span><span class="punctuation">:</span> <span class="string">&quot;access&quot;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;functions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;return function() ngx.req.set_header(\&quot;X-Expected-Client-Id\&quot;, \&quot;app-x-web\&quot;) end&quot;</span></span><br><span class="line">          <span class="punctuation">]</span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><strong>步骤 3：配置 Entitlement</strong></p><p>通过管理接口为用户或组织分配新应用的访问权限。</p><p>全程无需修改鉴权服务代码。网关和鉴权服务是通用的基础设施，业务应用只需声明式配置。</p><hr><h2 id="8-设计小结"><a href="#8-设计小结" class="headerlink" title="8. 设计小结"></a>8. 设计小结</h2><p>这套 Per-Client 鉴权隔离机制的核心思路：</p><ol><li><strong>声明式绑定</strong>：路由配置声明”我期望的 Client ID”，而非在鉴权服务中硬编码映射关系。新增应用不需要改鉴权服务代码。</li><li><strong>两级校验</strong>：网关层做 Client 绑定（Token 属于谁），鉴权服务做准入策略（这个用户能不能用这个应用）。职责分离，各不侵入。</li><li><strong>可分级</strong>：通过 <code>access_level</code> 实现不同严格程度的准入，避免所有应用都走最重的策略链。</li><li><strong>模式可选</strong>：Protected 模式保护业务 API，Authenticated 模式放行通用接口，通过路由优先级共存于同一网关。</li></ol><p>关键权衡：网关层的 Lua 注入和 forward-auth 子请求增加了每次请求的延迟（约 2-5ms 的内部回环调用），换来了集中管控和跨应用隔离的能力。在内部企业网络中，这个代价是合理的。</p>]]></content>
    
    
    <summary type="html">在统一 IAM 平台保护多个业务应用的场景下，通过网关层 Per-Client 鉴权隔离防止跨应用 Token 滥用，覆盖 X-Expected-Client-Id 机制、访问级别与 entitlement 策略链的设计边界。</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="api-gateway" scheme="https://www.yzhu.name/tags/api-gateway/"/>
    
    <category term="multi-tenant" scheme="https://www.yzhu.name/tags/multi-tenant/"/>
    
    <category term="forward-auth" scheme="https://www.yzhu.name/tags/forward-auth/"/>
    
    <category term="apisix" scheme="https://www.yzhu.name/tags/apisix/"/>
    
  </entry>
  
  <entry>
    <title>Per-Client Authorization Isolation at the Gateway: One IAM Platform for Multiple Applications</title>
    <link href="https://www.yzhu.name/2026/06/07/per-client-auth-isolation-gateway-en/"/>
    <id>https://www.yzhu.name/2026/06/07/per-client-auth-isolation-gateway-en/</id>
    <published>2026-06-07T02:00:00.000Z</published>
    <updated>2026-06-07T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/06/07/per-client-auth-isolation-gateway/">中文版</a></p></blockquote><blockquote><p><strong>Context</strong>: This design assumes an OAuth2&#x2F;OIDC-based IAM platform and an API gateway, such as APISIX, using the forward-auth plugin for centralized authorization. For the surrounding routing and browser-session design, see <a href="/2026/02/27/microservice-gateway-oauth2-routing-en/">Microservice Gateway Routing</a> and <a href="/2026/04/25/hmac-cookie-redis-sid-browser-session-en/">HMAC Cookie + Redis SID Dual-Layer Sessions</a>.</p></blockquote><span id="more"></span><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>This article focuses on one specific problem in a shared IAM platform: preventing a token issued for one application from being used against another application. The core mechanism is to declare the expected OAuth2 Client at the APISIX route level, pass it to forward-auth through <code>X-Expected-Client-Id</code>, and let the authorization service compare it with the <code>client_id</code> claim in the JWT.</p><p>This mechanism answers only one question: <strong>which client does this token belong to?</strong> It does not replace user admission policy, organization entitlement, session revocation, or application-level permissions. Application names, Client IDs, and route prefixes in this article are neutral examples and do not represent any real deployment.</p><h2 id="1-The-Problem-Token-Overreach-in-a-Shared-IAM-Platform"><a href="#1-The-Problem-Token-Overreach-in-a-Shared-IAM-Platform" class="headerlink" title="1. The Problem: Token Overreach in a Shared IAM Platform"></a>1. The Problem: Token Overreach in a Shared IAM Platform</h2><p>When multiple internal applications share one IAM platform, the naive model is straightforward: a user logs in, receives a JWT, and every application accepts that token. The problem is:</p><blockquote><p><strong>Can a token issued for Application A be used to access Application B?</strong></p></blockquote><p>In OAuth2, tokens are issued for a specific Client. If the gateway only checks whether a token is valid, but does not verify which Client the token belongs to, any request carrying a valid token can pass through to any backend service. That may look harmless inside an enterprise network, but it creates real risks:</p><ul><li><strong>Access policy bypass</strong>: Application B may have its own admission policy, but the gateway only checks “is the token valid.”</li><li><strong>Cross-application data exposure</strong>: Browser-side code or manual API calls can reuse a token against another application’s API.</li><li><strong>Broken audit boundaries</strong>: The gateway cannot reliably distinguish which application client initiated a request.</li></ul><p>The core gap is simple: <strong>the gateway performs authentication, but not client binding.</strong></p><hr><h2 id="2-Architecture-Decision-Inject-the-Expected-Client-ID-at-the-Gateway"><a href="#2-Architecture-Decision-Inject-the-Expected-Client-ID-at-the-Gateway" class="headerlink" title="2. Architecture Decision: Inject the Expected Client ID at the Gateway"></a>2. Architecture Decision: Inject the Expected Client ID at the Gateway</h2><h3 id="2-1-Options-Considered"><a href="#2-1-Options-Considered" class="headerlink" title="2.1 Options Considered"></a>2.1 Options Considered</h3><table><thead><tr><th>Option</th><th>Approach</th><th>Problem</th></tr></thead><tbody><tr><td>Per-application authorization</td><td>Each backend validates the token’s <code>client_id</code></td><td>Authorization logic spreads across services, and policy changes require repeated code changes</td></tr><tr><td>Gateway validates only token liveness</td><td>forward-auth checks JWT signature and expiration</td><td>Does not prevent cross-application token reuse</td></tr><tr><td><strong>Gateway injects expected Client ID</strong></td><td>Route config declares which Client is allowed for this route; the authorization service validates the binding</td><td>Centralized control, and adding a new application does not require changes to the authorization service</td></tr></tbody></table><p>The third option is the chosen design. The route configuration is declarative: adding a new application requires a new gateway route config, not a code change in the authorization service.</p><h3 id="2-2-Mechanism-Overview"><a href="#2-2-Mechanism-Overview" class="headerlink" title="2.2 Mechanism Overview"></a>2.2 Mechanism Overview</h3><pre class="mermaid">sequenceDiagram    participant Client as Browser / Client    participant GW as APISIX Gateway    participant Auth as Authorization Service    participant Backend as Business Backend    Client->>GW: Request /api/app-a/tasks with Bearer token    Note over GW: 1. Route matches Application A    Note over GW: 2. Lua injects X-Expected-Client-Id: app-a-web    GW->>Auth: forward-auth (Authorization + X-Expected-Client-Id)    Note over Auth: 3. Decode JWT and read client_id    Note over Auth: 4. Compare JWT.client_id with X-Expected-Client-Id    alt Client ID matches and admission passes        Auth-->>GW: 200 OK + X-User-Id, X-Sid, etc.        GW->>Backend: Forward request with identity headers        Backend-->>Client: Return business data    else Client ID mismatch        Auth-->>GW: 401 Unauthorized        GW-->>Client: 401 Unauthorized    end</pre><p>Key design points:</p><ol><li><strong>Route config declares the Client ID</strong>: Each protected route declares the OAuth2 Client it expects.</li><li><strong>Lua injects a request header</strong>: <code>serverless-pre-function</code> writes <code>X-Expected-Client-Id</code> before forward-auth runs.</li><li><strong>forward-auth forwards the header</strong>: <code>request_headers</code> must include <code>X-Expected-Client-Id</code>.</li><li><strong>The authorization service validates the binding</strong>: It reads the JWT <code>client_id</code> claim and compares it with <code>X-Expected-Client-Id</code>.</li></ol><hr><h2 id="3-APISIX-Route-Configuration"><a href="#3-APISIX-Route-Configuration" class="headerlink" title="3. APISIX Route Configuration"></a>3. APISIX Route Configuration</h2><h3 id="3-1-Route-Config-Structure"><a href="#3-1-Route-Config-Structure" class="headerlink" title="3.1 Route Config Structure"></a>3.1 Route Config Structure</h3><p>Each application owns an independent gateway route config that declares the route, upstream, and authentication behavior. Application A is used here as a neutral example:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;routes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-a-api-route&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/api/app-a/*&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;apps.example.com&quot;</span><span class="punctuation">,</span> <span class="string">&quot;console.example.com&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">91</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;upstream_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-a-upstream&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugin_config_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;protected-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;serverless-pre-function&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;phase&quot;</span><span class="punctuation">:</span> <span class="string">&quot;access&quot;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;functions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;return function() ngx.req.set_header(\&quot;X-Expected-Client-Id\&quot;, \&quot;app-a-web\&quot;) end&quot;</span></span><br><span class="line">          <span class="punctuation">]</span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>Execution order:</p><ol><li><code>serverless-pre-function</code> in the <code>access</code> phase sets <code>X-Expected-Client-Id</code></li><li><code>forward-auth</code>, through the reusable <code>protected-api-auth</code> config, sends that header and <code>Authorization</code> to the authorization service</li><li>After authorization succeeds, the gateway forwards the request to the backend</li></ol><h3 id="3-2-Reusable-forward-auth-Config"><a href="#3-2-Reusable-forward-auth-Config" class="headerlink" title="3.2 Reusable forward-auth Config"></a>3.2 Reusable forward-auth Config</h3><p>The reusable plugin config looks like this:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;protected-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;forward-auth&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://127.0.0.1:9080/__internal/auth/gateway/verify?gateway_secret=$&#123;GATEWAY_AUTH_SECRET&#125;&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;request_headers&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;Authorization&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Trace-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Request-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Expected-Client-Id&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;upstream_headers&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;X-User-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Sid&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-User-Email&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-User-Display-Name&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Client-Id&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Idp&quot;</span><span class="punctuation">,</span> <span class="string">&quot;X-Idp-User-Id&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;timeout&quot;</span><span class="punctuation">:</span> <span class="number">5000</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;status_on_error&quot;</span><span class="punctuation">:</span> <span class="number">503</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>X-Expected-Client-Id</code> in <code>request_headers</code> is the critical part. Without it, the forward-auth subrequest will not carry the expected Client ID. If a route does not set this header, the authorization service should reject the request instead of falling back to a permissive mode.</p><h3 id="3-3-Protecting-the-Internal-Authorization-Route"><a href="#3-3-Protecting-the-Internal-Authorization-Route" class="headerlink" title="3.3 Protecting the Internal Authorization Route"></a>3.3 Protecting the Internal Authorization Route</h3><p>The forward-auth subrequest targets an APISIX internal route on <code>127.0.0.1:9080</code>. This route must not be reachable from the public edge:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;auth-gateway-verify-internal&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/__internal/auth/gateway/verify&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;127.0.0.1&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">110</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;vars&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">[</span><span class="string">&quot;arg_gateway_secret&quot;</span><span class="punctuation">,</span> <span class="string">&quot;==&quot;</span><span class="punctuation">,</span> <span class="string">&quot;$&#123;GATEWAY_AUTH_SECRET&#125;&quot;</span><span class="punctuation">]</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;upstream_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;auth-service-upstream&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>Three layers of protection:</p><ul><li><strong><code>hosts: 127.0.0.1</code></strong>: Accept only loopback requests</li><li><strong><code>gateway_secret</code> variable check</strong>: Require a shared secret in the query string</li><li><strong>High priority</strong>: Ensure the internal route wins over business routes</li></ul><hr><h2 id="4-Two-Authorization-Modes"><a href="#4-Two-Authorization-Modes" class="headerlink" title="4. Two Authorization Modes"></a>4. Two Authorization Modes</h2><p>Not every endpoint needs strict Client binding. A profile endpoint such as <code>/me</code> may be valid from any logged-in application. This leads to two modes.</p><h3 id="4-1-Protected-Mode"><a href="#4-1-Protected-Mode" class="headerlink" title="4.1 Protected Mode"></a>4.1 Protected Mode</h3><p>Protected mode is the default for business APIs and enforces Client binding.</p><ul><li>Lua sets <code>X-Expected-Client-Id</code></li><li>forward-auth calls <code>/__internal/auth/gateway/verify</code></li><li>The authorization service checks <code>JWT.client_id == X-Expected-Client-Id</code>; mismatch returns 401</li></ul><h3 id="4-2-Authenticated-Mode"><a href="#4-2-Authenticated-Mode" class="headerlink" title="4.2 Authenticated Mode"></a>4.2 Authenticated Mode</h3><p>Authenticated mode is for shared user-info endpoints. It requires a valid login state, but does not enforce a specific Client binding.</p><ul><li>Lua sets <code>X-Auth-Mode: authenticated</code></li><li>forward-auth calls <code>/__internal/auth/gateway/session</code></li><li>The authorization service skips Client ID matching and validates only token liveness, user state, and active session state</li></ul><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;account-profile-authenticated-route&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/api/account/profile/*&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">95</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugin_config_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;authenticated-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;serverless-pre-function&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;phase&quot;</span><span class="punctuation">:</span> <span class="string">&quot;access&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;functions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="string">&quot;return function() ngx.req.set_header(\&quot;X-Auth-Mode\&quot;, \&quot;authenticated\&quot;) end&quot;</span></span><br><span class="line">      <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>Both modes ultimately reach the same internal authorization capability. <code>X-Auth-Mode</code> selects the behavior:</p><table><thead><tr><th>Mode</th><th>X-Expected-Client-Id</th><th>X-Auth-Mode</th><th>Validation</th></tr></thead><tbody><tr><td>Protected</td><td>Required, must match JWT <code>client_id</code></td><td>Not set</td><td>Client binding + full admission policy</td></tr><tr><td>Authenticated</td><td>Not required</td><td><code>authenticated</code></td><td>Login state only</td></tr></tbody></table><h3 id="4-3-Route-Priority"><a href="#4-3-Route-Priority" class="headerlink" title="4.3 Route Priority"></a>4.3 Route Priority</h3><p>Route priority keeps exact routes from being captured by broader API routes:</p><table><thead><tr><th>Priority</th><th>Route Type</th><th>Example</th></tr></thead><tbody><tr><td>110</td><td>Internal authorization route</td><td><code>/__internal/auth/gateway/verify</code></td></tr><tr><td>100</td><td>Public callback &#x2F; webhook</td><td><code>/callback/app-a/*</code></td></tr><tr><td>95-96</td><td>Authenticated API</td><td><code>/api/account/profile/*</code></td></tr><tr><td>91</td><td>Protected API</td><td><code>/api/app-a/*</code>, <code>/api/app-b/*</code></td></tr><tr><td>80-83</td><td>Static UI &#x2F; SPA</td><td><code>/ui/app-a/*</code></td></tr><tr><td>69-70</td><td>SPA root and fallback</td><td><code>/*</code></td></tr></tbody></table><p>The profile route has higher priority than business API routes, so “read my own profile” goes through Authenticated mode instead of being captured by a Protected route.</p><hr><h2 id="5-Authorization-Service-Policy-Decision-Chain"><a href="#5-Authorization-Service-Policy-Decision-Chain" class="headerlink" title="5. Authorization Service: Policy Decision Chain"></a>5. Authorization Service: Policy Decision Chain</h2><p>The gateway handles the question “does this token belong to this Client.” The authorization service then evaluates the admission policy:</p><pre class="mermaid">flowchart TD    A[Receive forward-auth request] --> B{X-Auth-Mode?}    B -->|authenticated| C[Skip Client check<br>validate active session]    B -->|entitled/default| D{X-Expected-Client-Id present?}    D -->|missing| E[500 config error]    D -->|present| F{JWT.client_id<br>== Expected?}    F -->|mismatch| G[401 Client mismatch]    F -->|match| H[Check Client status]    H -->|disabled| I[403 Client disabled]    H -->|active| J{Access Level?}    J -->|PUBLIC| K[Allow]    J -->|AUTHENTICATED| L[Check user state<br>+ token version]    J -->|PRIVATE| M[Full admission policy]    M --> N{User state?}    N -->|disabled/left| O[403]    N -->|active| P{Token Version<br>matches?}    P -->|mismatch| Q[401 stale token<br>force re-auth]    P -->|match| R{Security Stamp<br>matches?}    R -->|mismatch| S[401 security stamp changed]    R -->|match| T{Platform admin?}    T -->|yes| U[Allow]    T -->|no| V{Application admin?}    V -->|yes| U    V -->|no| W{User entitlement}    W -->|explicit DENY| X[403]    W -->|explicit ALLOW| U    W -->|none| Y{Organization entitlement}    Y -->|any DENY| X    Y -->|any ALLOW| U    Y -->|none| Z[403 default deny]</pre><h3 id="5-1-Three-Access-Levels"><a href="#5-1-Three-Access-Levels" class="headerlink" title="5.1 Three Access Levels"></a>5.1 Three Access Levels</h3><p>Each OAuth2 Client has an <code>access_level</code> that determines how strict admission should be:</p><table><thead><tr><th>Access Level</th><th>Value</th><th>Meaning</th><th>Suitable For</th></tr></thead><tbody><tr><td><strong>PRIVATE</strong></td><td>1</td><td>Full entitlement policy chain required</td><td>Core business apps and admin applications</td></tr><tr><td><strong>AUTHENTICATED</strong></td><td>2</td><td>Logged-in user with valid state is enough</td><td>Shared internal tools and documentation sites</td></tr><tr><td><strong>PUBLIC</strong></td><td>3</td><td>Client binding passes, then allow</td><td>Low-risk endpoints that still need Client identity</td></tr></tbody></table><p>This lets the authorization service short-circuit lower-risk Clients instead of forcing every application through the heaviest policy chain.</p><h3 id="5-2-Entitlement-Chain-for-PRIVATE-Clients"><a href="#5-2-Entitlement-Chain-for-PRIVATE-Clients" class="headerlink" title="5.2 Entitlement Chain for PRIVATE Clients"></a>5.2 Entitlement Chain for PRIVATE Clients</h3><p>For <code>PRIVATE</code> applications, admission follows a deterministic order:</p><p><strong>1. Platform role bypass</strong></p><p>Platform administrators and global application administrators are allowed directly. This is an operational escape path for emergencies.</p><p><strong>2. Application administrator</strong></p><p>Application-admin configuration records which users administer each Client. Application admins have full access to their own application.</p><p><strong>3. User-level entitlement</strong></p><p>User-level entitlement records explicit <code>ALLOW</code> or <code>DENY</code> decisions for one user and one application. If a user-level record exists, it wins.</p><p><strong>4. Organization-level entitlement</strong></p><p>The system resolves all organization units a user belongs to, including ancestors, and evaluates organization-level entitlement:</p><ul><li>Any <code>DENY</code> wins over all <code>ALLOW</code> records</li><li>If there is no <code>DENY</code>, at least one <code>ALLOW</code> grants access</li><li>User-level records take precedence over organization-level records</li></ul><p><strong>5. Default deny</strong></p><p>If nothing matches, deny access.</p><h3 id="5-3-Version-Consistency-Checks"><a href="#5-3-Version-Consistency-Checks" class="headerlink" title="5.3 Version Consistency Checks"></a>5.3 Version Consistency Checks</h3><p>Token Version and Security Stamp are independent revocation signals:</p><table><thead><tr><th>Check</th><th>Trigger</th><th>Effect</th></tr></thead><tbody><tr><td><strong>Token Version</strong></td><td>Admin changes roles or access policy</td><td>Version increments, and active tokens become stale</td></tr><tr><td><strong>Security Stamp</strong></td><td>User changes password or account is disabled</td><td>Stamp changes, forcing re-authentication</td></tr></tbody></table><p>These two signals are discussed in more detail in <a href="/2026/04/25/hmac-cookie-redis-sid-browser-session-en/">HMAC Cookie + Redis SID Dual-Layer Sessions</a>.</p><hr><h2 id="6-Client-ID-Mapping-for-Multiple-Applications"><a href="#6-Client-ID-Mapping-for-Multiple-Applications" class="headerlink" title="6. Client ID Mapping for Multiple Applications"></a>6. Client ID Mapping for Multiple Applications</h2><p>In the examples, Client ID formats are intentionally generic:</p><table><thead><tr><th>Application</th><th>Client ID</th><th>Format</th><th>Route Prefix</th></tr></thead><tbody><tr><td>Admin Console</td><td><code>admin-console</code></td><td>Readable name</td><td><code>/api/admin/*</code></td></tr><tr><td>Workbench</td><td><code>workbench-web</code></td><td>Readable name</td><td><code>/api/workbench/*</code></td></tr><tr><td>Application A</td><td><code>app-a-web</code></td><td>App + endpoint type</td><td><code>/api/app-a/*</code></td></tr><tr><td>Application B</td><td><code>app-b-service</code></td><td>App + endpoint type</td><td><code>/api/app-b/*</code></td></tr><tr><td>Application C</td><td><code>app-c-web-r7k3</code></td><td>App + endpoint type + suffix</td><td><code>/api/app-c/*</code></td></tr></tbody></table><p>Several formats are useful:</p><ul><li><strong>Readable name</strong>, such as <code>admin-console</code>: easy to recognize during operations</li><li><strong>Application + endpoint type</strong>, such as <code>app-a-web</code>: distinguishes Web, mobile, and service Clients for the same application</li><li><strong>Random suffix</strong>, such as <code>app-c-web-r7k3</code>: reduces naming collision and enumeration risk, but is not a security boundary</li></ul><p>The Client ID format does not change the isolation mechanism. The gateway and authorization service perform exact matching. Token forgery prevention still depends on JWT signatures, issuer&#x2F;audience&#x2F;client_id validation, client secret, PKCE, and related OAuth2 controls.</p><hr><h2 id="7-Onboarding-a-New-Application"><a href="#7-Onboarding-a-New-Application" class="headerlink" title="7. Onboarding a New Application"></a>7. Onboarding a New Application</h2><p>With this model, adding a new application has three steps.</p><p><strong>Step 1: Register an OAuth2 Client</strong></p><p>Create a Client through the management API, set <code>access_level</code>, and configure the appropriate client authentication method. The platform generates or records the Client ID.</p><p><strong>Step 2: Add Gateway Route Config</strong></p><p>Add a route config that declares the route and injects the expected Client ID:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;routes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-x-api-route&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;uri&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/api/app-x/*&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;apps.example.com&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;priority&quot;</span><span class="punctuation">:</span> <span class="number">91</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;upstream_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;app-x-upstream&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugin_config_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;protected-api-auth&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;serverless-pre-function&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;phase&quot;</span><span class="punctuation">:</span> <span class="string">&quot;access&quot;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;functions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;return function() ngx.req.set_header(\&quot;X-Expected-Client-Id\&quot;, \&quot;app-x-web\&quot;) end&quot;</span></span><br><span class="line">          <span class="punctuation">]</span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><strong>Step 3: Configure Entitlements</strong></p><p>Use the management interface to assign access to users or organizations.</p><p>The gateway and authorization service remain generic infrastructure. Business applications only declare their routing and access policy.</p><hr><h2 id="8-Key-Takeaways"><a href="#8-Key-Takeaways" class="headerlink" title="8. Key Takeaways"></a>8. Key Takeaways</h2><p>The Per-Client authorization isolation mechanism is built on four decisions:</p><ol><li><strong>Declarative binding</strong>: Route config declares the expected Client ID instead of hardcoding route-to-Client mapping in the authorization service.</li><li><strong>Two-layer validation</strong>: The gateway layer enforces Client binding; the authorization service evaluates whether the user may enter the application.</li><li><strong>Tiered admission</strong>: <code>access_level</code> keeps low-risk applications away from the heaviest policy chain.</li><li><strong>Mode separation</strong>: Protected mode secures business APIs, while Authenticated mode supports shared user-info endpoints.</li></ol><p>The trade-off is clear: Lua header injection and the forward-auth subrequest add latency, typically a small internal loopback cost. In return, the system gains centralized control and cross-application token isolation.</p>]]></content>
    
    
    <summary type="html">A gateway-level Per-Client authorization isolation design for protecting multiple applications with one IAM platform, covering X-Expected-Client-Id, access levels, and entitlement policy boundaries.</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="api-gateway" scheme="https://www.yzhu.name/tags/api-gateway/"/>
    
    <category term="multi-tenant" scheme="https://www.yzhu.name/tags/multi-tenant/"/>
    
    <category term="forward-auth" scheme="https://www.yzhu.name/tags/forward-auth/"/>
    
    <category term="apisix" scheme="https://www.yzhu.name/tags/apisix/"/>
    
  </entry>
  
  <entry>
    <title>HMAC Cookie + Redis SID Dual-Layer Sessions: Browser Session Governance in an OAuth2/JWT System</title>
    <link href="https://www.yzhu.name/2026/04/25/hmac-cookie-redis-sid-browser-session-en/"/>
    <id>https://www.yzhu.name/2026/04/25/hmac-cookie-redis-sid-browser-session-en/</id>
    <published>2026-04-25T14:30:17.000Z</published>
    <updated>2026-04-28T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/04/25/hmac-cookie-redis-sid-browser-session/">中文版</a></p></blockquote><p>In an OAuth2&#x2F;OIDC system, JWTs are well-suited as request admission credentials. The gateway can verify signatures locally, check expiration, and parse user and permission information without querying a session store for every request. Horizontal scaling is straightforward.</p><p>But browser login state introduces a separate set of concerns: explicit logout, admin-initiated session termination, immediate enforcement of permission changes, and active session tracking. These are stateful governance problems, distinct from the stateless admission that JWTs handle.</p><p>This article records a session governance design: adding a Redis SID layer alongside an existing OAuth2&#x2F;JWT system. The browser carries the SID in an HMAC-signed cookie, the server maintains the SID’s liveness in Redis, and the same SID propagates into the JWT. Browser sessions and API tokens share a single set of revocation signals.</p><span id="more"></span><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>Core ideas:</p><ul><li>The browser carries a session identifier in an HMAC-signed cookie, which is stateless and tamper-resistant</li><li>Redis stores the Session ID (SID), which is stateful and revocable</li><li>The SID flows from the browser cookie into the JWT; revoking one SID invalidates that browser session and the tokens derived from it</li><li>Two revocation signals: a security stamp for account-level events such as password changes, and a permission version for role changes</li></ul><h2 id="Problem-Boundary"><a href="#Problem-Boundary" class="headerlink" title="Problem Boundary"></a>Problem Boundary</h2><p>The strength of JWTs lies in stateless validation. That strength does not need to be challenged.</p><p>The problem surfaces when the system begins to require stateful capabilities:</p><ul><li>After a user explicitly logs out, can the old JWT still access APIs</li><li>After an admin disables a user, does the old JWT become invalid immediately</li><li>After a user’s roles are adjusted, are the permissions in the old JWT still trustworthy</li><li>Can the system see how many active browser sessions a user has</li><li>Can the system kick out only one device or one browser session</li></ul><p>These capabilities fall outside the scope of “does the signature verify.” JWTs handle request admission; browser session governance handles the login state lifecycle. Mixing the two typically leads to one of two extremes: either waiting for tokens to expire naturally, or reverting to full server-side sessions.</p><p>The third approach chosen here: preserve the OAuth2&#x2F;JWT request admission model, and only add a revocable SID for browser sessions.</p><h2 id="Why-Not-Traditional-Sessions"><a href="#Why-Not-Traditional-Sessions" class="headerlink" title="Why Not Traditional Sessions"></a>Why Not Traditional Sessions</h2><p>Traditional server-side sessions can handle revocation, kick-out, and session tracking, but in this scenario, they are not the most suitable primary model.</p><p>First, the system already has a complete OAuth2 authorization code flow and JWT issuance mechanism. The browser session layer needs to supplement revocation capability, not replace the existing token system.</p><p>Second, browser page requests and static resource requests are frequent. If all requests depend on centralized session lookups, storage pressure and the blast radius of failures both increase.</p><p>Third, business APIs are already under gateway-based JWT admission control. The session governance layer is better suited to providing a clear revocation signal than re-assuming the full authentication context.</p><p>So the boundary of this design is:</p><ul><li>JWTs handle API request admission</li><li>Cookies carry browser login state</li><li>Redis SIDs provide session-level revocation signals</li><li>Database security stamps handle account-level forced invalidation</li><li>Permission version numbers handle authorization change synchronization</li></ul><p>This boundary matters more than the question of “Session vs. Token.”</p><h2 id="Trade-Offs-Up-Front"><a href="#Trade-Offs-Up-Front" class="headerlink" title="Trade-Offs Up Front"></a>Trade-Offs Up Front</h2><p>Adding SIDs gives the system three categories of capability.</p><p>First, session-level revocation. Deleting one SID invalidates the corresponding browser session and any JWTs issued through it, without maintaining a token blacklist.</p><p>Second, finer-grained invalidation semantics. Logout and kick-out go through the SID; password changes and account disabling go through the security stamp; role adjustments and authorization changes go through the permission version number. Different events no longer crowd into a single field.</p><p>Third, the OAuth2 flow remains intact. The SID flows from the Cookie into the OAuth2 Authorization and then into the JWT. The existing authorization chain still holds.</p><p>The cost is equally direct.</p><p>Redis is no longer just a cache. It becomes session infrastructure. When SID liveness is undeterminable, the system cannot continue to allow requests through. Otherwise, the promises of “logout takes effect immediately” and “admin kick-out takes effect immediately” are broken.</p><p>Every browser login state restoration and every gateway authorization check incurs an additional SID lookup. The implementation also needs to handle Cookie signing, SID propagation, security stamp, and permission version number pathways.</p><p>This is not free complexity. It suits systems that need session revocation, fast permission enforcement, and admin-initiated session termination. It does not suit pure public APIs with no browser session concept.</p><h2 id="Dual-Layer-Session-Structure"><a href="#Dual-Layer-Session-Structure" class="headerlink" title="Dual-Layer Session Structure"></a>Dual-Layer Session Structure</h2><p>The overall structure can be viewed as three checks.</p><pre class="mermaid">flowchart LR    subgraph Browser["Browser"]        Cookie["Signed Cookie\n(HMAC-SHA256)"]    end    subgraph Server["Server"]        Redis["Redis\nsession:sid:{sid}"]        DB["Database\nuser.security_stamp"]    end    Cookie -->|"1. HMAC verify + exp check"| Redis    Redis -->|"2. SID alive + sliding renewal"| DB    DB -->|"3. security_stamp match"| Cookie</pre><p>The first layer is the HMAC-signed cookie.</p><p>The cookie contains the user identifier, SID, security stamp, and expiration time. The server can perform stateless signature verification and expiration checks first. A passing signature only means the cookie was not tampered with, not that the session is still valid.</p><p>The second layer is the Redis SID.</p><p>At login, a random SID is generated, written to Redis, and assigned a TTL. Subsequent requests check whether the SID still exists; if so, the browser session is still valid. Logout, kick-out, and expiration can all be handled by deleting the SID or waiting for the TTL to expire.</p><p>The third layer is the database security stamp.</p><p>Security events such as password changes, admin-initiated password resets, and account disabling update the <code>security_stamp</code> in the user table. Cookies and JWTs also carry the security stamp from the time they were issued. If the stamps do not match during validation, the authentication state is not restored.</p><p>If any of the three layers fails, the request is treated as unauthenticated.</p><h2 id="Cookie-Carries-Only-Essential-Information"><a href="#Cookie-Carries-Only-Essential-Information" class="headerlink" title="Cookie Carries Only Essential Information"></a>Cookie Carries Only Essential Information</h2><p>The cookie value uses a two-part structure:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">base64url(JSON payload) . base64url(HMAC-SHA256(payload))</span><br></pre></td></tr></table></figure><p>A payload example:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;uid&quot;</span><span class="punctuation">:</span> <span class="number">100</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;email&quot;</span><span class="punctuation">:</span> <span class="string">&quot;user@example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;display_name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;张三&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;sid&quot;</span><span class="punctuation">:</span> <span class="string">&quot;a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;idp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;EMAIL&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;security_stamp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;abc123def456&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exp&quot;</span><span class="punctuation">:</span> <span class="number">1745577600</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><table><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>uid</code></td><td>Local user ID</td></tr><tr><td><code>sid</code></td><td>Current browser session ID, corresponding to the Redis key</td></tr><tr><td><code>security_stamp</code></td><td>Account-level security stamp, used to force-invalidate old sessions</td></tr><tr><td><code>exp</code></td><td>Cookie hard expiration time</td></tr><tr><td><code>idp</code></td><td>Identity provider identifier</td></tr><tr><td><code>email</code> &#x2F; <code>display_name</code></td><td>Basic user information commonly used for page rendering</td></tr></tbody></table><p>Signature comparison uses constant-time comparison, such as <code>MessageDigest.isEqual()</code>. Ordinary byte-by-byte comparison may expose timing attack risk.</p><p>HMAC keys should be injected through a secure configuration service, key management system, or environment variables. They must not be committed to code repositories. Key rotation strategies also need to be defined in advance. Otherwise, the cookie signing mechanism becomes dependent on a long-lived fixed key.</p><p>Cookie attributes should maintain basic constraints: <code>HttpOnly</code>, <code>Secure</code>, <code>SameSite=Lax</code>, <code>Path=/</code>. If the service is deployed behind a TLS-terminating proxy, the <code>Secure</code> determination should align with the actual external HTTPS entry point.</p><h2 id="Redis-SID-Does-One-Thing"><a href="#Redis-SID-Does-One-Thing" class="headerlink" title="Redis SID Does One Thing"></a>Redis SID Does One Thing</h2><p>Sessions in Redis are not designed as “fat sessions.” A thin structure is sufficient:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Key:   session:sid:&#123;sid&#125;</span><br><span class="line">Value: &#123;&quot;uid&quot;: 100&#125;</span><br><span class="line">TTL:   43200 seconds</span><br></pre></td></tr></table></figure><p>The Redis SID is responsible for only two things:</p><ul><li>Marking whether this session is still valid</li><li>Performing sliding renewal on active access</li></ul><p>Basic user information is already in the cookie. Permissions, security state, and account status remain authoritative in the database and policy cache. Storing large amounts of permission data in Redis sessions makes permission-change synchronization more complex and amplifies session storage maintenance costs.</p><p>Cookie <code>exp</code> and Redis TTL represent two types of boundaries:</p><ul><li>Cookie <code>exp</code> is the hard upper bound for the browser-held credential, immutable by the browser after signing</li><li>Redis TTL is the active window for the server-side session, renewable on access</li></ul><p>If the business needs to keep active users online, the server needs to refresh the cookie’s <code>exp</code> field and return a new signed cookie via <code>Set-Cookie</code>. Otherwise, Redis renewal only ensures the SID does not expire; the cookie will still become invalid at the hard limit.</p><p>This trade-off belongs to session security policy, not just user experience configuration.</p><h2 id="Validation-Flow"><a href="#Validation-Flow" class="headerlink" title="Validation Flow"></a>Validation Flow</h2><p>When restoring browser login state, the flow is roughly as follows:</p><pre class="mermaid">flowchart TD    A["Request arrives"] --> B{"Parse signed cookie"}    B -->|"Missing"| Z["Unauthenticated"]    B -->|"Present"| C{"HMAC verification"}    C -->|"Mismatch"| Z    C -->|"Passed"| D{"Check exp"}    D -->|"Expired"| Z    D -->|"Not expired"| E{"Redis SID alive?"}    E -->|"Missing"| Z    E -->|"Present + renewed"| F{"DB security stamp match?"}    F -->|"Mismatch"| Z    F -->|"Match"| G["Authenticated"]    Z --> H["Unauthenticated / cookie may be cleared"]</pre><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> SessionClaims <span class="title function_">resolveActiveClaims</span><span class="params">(Request request, Response response)</span> &#123;</span><br><span class="line">    <span class="type">SessionClaims</span> <span class="variable">claims</span> <span class="operator">=</span> decodeAndVerifyCookie(request);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (claims == <span class="literal">null</span> || claims.sid() == <span class="literal">null</span> || claims.securityStamp() == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sessionStore.isSidActive(claims.sid())) &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userRepo.findActiveById(claims.userId());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (user != <span class="literal">null</span> &amp;&amp; claims.securityStamp().equals(user.getSecurityStamp())) &#123;</span><br><span class="line">            <span class="keyword">return</span> claims;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    clearCookie(request, response);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The three checks correspond to three categories of system semantics:</p><table><thead><tr><th>Layer</th><th>Check</th><th>Problem Solved</th></tr></thead><tbody><tr><td>HMAC Cookie</td><td>Tampered or expired</td><td>Prevent forged UID, SID, exp</td></tr><tr><td>Redis SID</td><td>Session still valid</td><td>Support logout, kick-out, session expiration</td></tr><tr><td>DB security stamp</td><td>Account security state changed</td><td>Support forced invalidation after password change or account disablement</td></tr></tbody></table><p>For malformed input and signature errors, treat as unauthenticated directly. For requests that can be determined as invalid, the cookie can be cleared. Skipping the SID check after HMAC verification passes would regress “revocable session” back to “irrevocable cookie.”</p><h2 id="How-the-SID-Enters-the-JWT"><a href="#How-the-SID-Enters-the-JWT" class="headerlink" title="How the SID Enters the JWT"></a>How the SID Enters the JWT</h2><p>The SID in the browser cookie does not exist in isolation. It propagates to the OAuth2 Authorization and then into the JWT.</p><pre class="mermaid">sequenceDiagram    participant B as Browser    participant Auth as Auth Service    participant Redis as Redis    participant GW as API Gateway    B->>Auth: Login (password / federated login)    Auth->>Redis: Create SID    Auth-->>B: Set-Cookie: signed cookie (with sid)    B->>Auth: OAuth2 authorization request (with Cookie)    Auth->>Auth: Extract sid from Cookie    Auth->>Auth: Attach sid to OAuth2 Authorization    Auth->>Auth: Issue JWT (write sid + security stamp + permission version)    Auth-->>B: Return JWT    B->>GW: API request (Bearer JWT)    GW->>Auth: Auth callback    Auth->>Auth: Decode JWT, extract sid    Auth->>Redis: Check SID alive    Auth->>Auth: Policy decision    Auth-->>GW: 200 + user headers    GW-->>B: Business response</pre><p>At login, the auth service creates an SID and writes it into a signed cookie.</p><p>When the browser subsequently initiates an OAuth2 authorization request, the auth service reads the SID from the cookie and attaches it to the OAuth2 Authorization object.</p><p>During token issuance, the SID is written into the JWT claims:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">sid</span> <span class="operator">=</span> context.getAuthorization().getAttribute(<span class="string">&quot;sid&quot;</span>);</span><br><span class="line"></span><br><span class="line">context.getClaims().claim(<span class="string">&quot;sid&quot;</span>, sid);</span><br><span class="line">context.getClaims().claim(<span class="string">&quot;security_stamp&quot;</span>, user.getSecurityStamp());</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (isAccessToken) &#123;</span><br><span class="line">    context.getClaims().claim(<span class="string">&quot;token_version&quot;</span>, user.getPermissionVersion());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The SID in the cookie and the SID in the JWT are the same value. Once this SID is revoked in Redis, both browser page requests and API requests fail in subsequent validation.</p><p>A token blacklist operates at token granularity. The SID operates at session granularity. How many access tokens have been issued within a single browser session does not affect the revocation action; as long as they share the same SID, one session signal can invalidate them all uniformly.</p><h2 id="Two-Revocation-Signals"><a href="#Two-Revocation-Signals" class="headerlink" title="Two Revocation Signals"></a>Two Revocation Signals</h2><p>Session governance should not compress all invalidation scenarios into a single field. Here they are split into two signals:</p><ul><li><code>security_stamp</code>: account-level security events</li><li><code>permission_version</code>: permission change events</li></ul><p><code>security_stamp</code> is stored in the user table, typically updated when a user changes their password, an admin resets a password, or an account is disabled. The security stamp is written into both the cookie and the JWT. When the current value in the database changes, old cookies and old JWTs both become invalid.</p><p><code>permission_version</code> is also a field in the user table, incremented each time roles are adjusted or authorizations are added or removed. It is primarily written into access tokens and compared against the current policy version during gateway authorization. When the version in an old access token falls behind, the gateway rejects the request, and the client re-runs the authorization flow to obtain a new token.</p><p>Splitting the two makes user experience more controllable:</p><table><thead><tr><th>Dimension</th><th>Security Stamp</th><th>Permission Version</th></tr></thead><tbody><tr><td>Trigger scenario</td><td>Password change, account disabled</td><td>Role adjustment, authorization added or removed</td></tr><tr><td>Impact scope</td><td>Cookie + JWT</td><td>Primarily access token</td></tr><tr><td>User experience</td><td>Usually requires re-login</td><td>Usually just re-obtains token</td></tr><tr><td>Update frequency</td><td>Low</td><td>Medium</td></tr></tbody></table><p>If only one field is used, every permission change could force a full re-login across all devices. Permission changes are typically more frequent than security events. After the split, the invalidation scope more closely matches the actual business event.</p><h2 id="Gateway-Authorization-Chain"><a href="#Gateway-Authorization-Chain" class="headerlink" title="Gateway Authorization Chain"></a>Gateway Authorization Chain</h2><p>When API requests go through the gateway, JWT validation checks not only signature and expiration, but also SID and policy version.</p><pre class="mermaid">flowchart LR    Client["Client"] -->|"Bearer JWT"| GW["API Gateway"]    GW -->|"forward-auth\nforward Authorization header"| Auth["Auth Service"]    Auth -->|"1. Decode JWT"| Auth    Auth -->|"2. SID alive check"| Redis["Redis"]    Auth -->|"3. Policy decision"| Policy["Policy cache / DB"]    Auth -->|"200 + user headers"| GW    GW -->|"Inject request headers"| Svc["Business Service"]</pre><p>A typical forward-auth flow:</p><ol><li>Client sends a <code>Bearer JWT</code> request to the API gateway</li><li>Gateway forwards the Authorization header to the auth service</li><li>Auth service decodes the JWT, reads <code>uid</code>, <code>sid</code>, <code>security_stamp</code>, <code>token_version</code></li><li>Checks whether the Redis SID still exists</li><li>Reads the user policy, compares security stamp and permission version</li><li>After authorization passes, the gateway injects user header information and forwards to the business service</li></ol><p>Two modes can be supported here:</p><ul><li><code>authenticated</code>: only confirms who sent the request, checks user status, SID, security stamp, and permission version</li><li><code>entitled</code>: continues with RBAC, application authorization, and resource authorization on top of authenticated</li></ul><p>The difference between the two modes is in authorization depth, not session validation. SID and security stamp remain the common foundational layer.</p><h2 id="When-Redis-Is-Unavailable"><a href="#When-Redis-Is-Unavailable" class="headerlink" title="When Redis Is Unavailable"></a>When Redis Is Unavailable</h2><p>This design introduces a Redis dependency, so failure semantics must be defined.</p><p>If the policy cache is unavailable, the system can fall back to the database. Policy data has an authoritative source; Redis is only a cache.</p><p>SID liveness checks are different. The SID is the source of truth for whether a session is still valid. When Redis is unavailable, the system does not know whether the session has been revoked. Continuing to allow requests through would break the promises of “logout takes effect immediately” and “admin kick-out takes effect immediately.”</p><p>Therefore, the SID validation path is better suited to fail-closed behavior: when Redis is unavailable, reject session restoration and API authorization.</p><p>This also means Redis high availability is not optional. Master-replica replication with Sentinel, Redis Cluster, or other high-availability solutions that meet deployment requirements need to be designed as part of the session infrastructure, not added after going live.</p><h2 id="Logout-and-Kick-Out"><a href="#Logout-and-Kick-Out" class="headerlink" title="Logout and Kick-Out"></a>Logout and Kick-Out</h2><p>Logout clears both layers:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">logout</span><span class="params">(Request request, Response response)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sid</span> <span class="operator">=</span> cookieService.resolveSid(request);</span><br><span class="line"></span><br><span class="line">    sessionStore.invalidateSid(sid);</span><br><span class="line">    cookieService.clearCookie(request, response);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>After the Redis SID is deleted, the impact covers both pathways simultaneously:</p><ul><li>Browser requests: the cookie signature may still verify, but the SID check fails</li><li>API requests: the JWT may not have expired, but the SID check in the JWT fails</li></ul><p>Kicking out a specific browser session is essentially the same: delete the corresponding SID.</p><p>Invalidating all sessions for a user is better handled by updating the <code>security_stamp</code>. This causes the security stamp in all of that user’s old cookies and old JWTs to mismatch.</p><h2 id="Applicability-Boundary"><a href="#Applicability-Boundary" class="headerlink" title="Applicability Boundary"></a>Applicability Boundary</h2><table><thead><tr><th>Scenario</th><th>Judgment</th></tr></thead><tbody><tr><td>Internal systems requiring immediate logout</td><td>Suitable</td></tr><tr><td>Admin consoles requiring admin kick-out</td><td>Suitable</td></tr><tr><td>Permission changes need fast enforcement</td><td>Suitable</td></tr><tr><td>Browser sessions and API tokens need unified governance</td><td>Suitable</td></tr><tr><td>Pure public APIs with no browser session concept</td><td>Not very suitable</td></tr><tr><td>Cross-top-level-domain SSO</td><td>Requires a protocol-level solution, not just cookies</td></tr><tr><td>Extremely high concurrency that cannot bear centralized session dependency</td><td>Needs Redis architecture reassessment</td></tr></tbody></table><p>This design is not meant to prove that cookies are better than JWTs, or that sessions are more secure than tokens. What it does is separation of concerns:</p><ul><li>JWTs handle request admission credentials</li><li>Cookies carry browser login state</li><li>Redis SIDs provide session revocation signals</li><li>Security stamps handle account-level forced invalidation</li><li>Permission version numbers handle authorization change synchronization</li></ul><p>After these signals are separated, what each field affects, where it is validated, and how it behaves during failures all become clearer. The architectural benefit comes not just from “adding a Redis layer,” but from invalidation semantics being split to the right granularity.</p><h2 id="Further-Reading"><a href="#Further-Reading" class="headerlink" title="Further Reading"></a>Further Reading</h2><ul><li><a href="/2026/03/17/oauth2-oidc-session-timeout-boundaries-en/">OAuth2&#x2F;OIDC Session Timeout Boundaries: Frontend Tokens, Refresh Tokens, and SSO Sessions</a></li><li><a href="/2026/04/06/why-local-auth-alongside-federation/">Beyond Federated Login: Resilience Design for IAM Authentication Paths</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc9121">RFC 9121: Use of the HMAC Algorithm in JSON Object Signing and Encryption (JOSE)</a></li></ul>]]></content>
    
    
    <summary type="html">Managing browser sessions in an OAuth2/JWT system with a dual-layer structure: HMAC-signed cookies plus Redis-backed SIDs, aligning browser login state, token lifecycle, and revocation signals under one governance model.</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="session" scheme="https://www.yzhu.name/tags/session/"/>
    
    <category term="redis" scheme="https://www.yzhu.name/tags/redis/"/>
    
    <category term="cookie" scheme="https://www.yzhu.name/tags/cookie/"/>
    
    <category term="jwt" scheme="https://www.yzhu.name/tags/jwt/"/>
    
  </entry>
  
  <entry>
    <title>HMAC Cookie + Redis SID 双层会话：OAuth2/JWT 体系下的浏览器会话治理</title>
    <link href="https://www.yzhu.name/2026/04/25/hmac-cookie-redis-sid-browser-session/"/>
    <id>https://www.yzhu.name/2026/04/25/hmac-cookie-redis-sid-browser-session/</id>
    <published>2026-04-25T14:30:17.000Z</published>
    <updated>2026-04-28T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 语言</strong>: <strong>中文版</strong> | <a href="/2026/04/25/hmac-cookie-redis-sid-browser-session-en/">English Version</a></p></blockquote><p>在 OAuth2&#x2F;OIDC 体系里，JWT 很适合做请求准入凭证。网关拿到 token 后可以本地验签、检查过期时间、解析用户和权限信息，服务端不用为每个请求查 Session，水平扩展也简单。</p><p>但浏览器登录态还有另一组问题：主动登出、管理员踢人、权限变更即时生效、活跃会话追踪。这些都是有状态治理问题，和 JWT 的无状态准入不是同一类职责。</p><p>这篇记录的是一次会话治理设计：在现有 OAuth2&#x2F;JWT 体系旁边补一层 Redis SID。浏览器侧用 HMAC 签名 Cookie 承载 SID，服务端用 Redis 维护 SID 存活状态，再把同一个 SID 传播进 JWT。浏览器会话和 API 令牌共享同一套撤销信号。</p><span id="more"></span><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>核心思路：</p><ul><li>浏览器用 HMAC 签名 Cookie 承载会话标识，无状态、防篡改</li><li>Redis 中存储 Session ID（SID），有状态、可撤销</li><li>SID 从浏览器 Cookie 流入 JWT，撤销某个 SID 即可让该浏览器会话及其派生令牌失效</li><li>双撤销信号：安全戳处理密码修改等账户级事件，权限版本号处理角色变更</li></ul><h2 id="问题边界"><a href="#问题边界" class="headerlink" title="问题边界"></a>问题边界</h2><p>JWT 的优势在于无状态校验。这个优势不需要否定。</p><p>问题出现在系统开始要求有状态能力的时候：</p><ul><li>用户主动退出后，旧 JWT 是否还能继续访问 API</li><li>管理员禁用用户后，旧 JWT 是否立即失效</li><li>用户角色调整后，旧 JWT 里的权限是否还可信</li><li>是否能看到某个用户有多少活跃浏览器会话</li><li>是否能只踢掉某一台设备或某一个浏览器会话</li></ul><p>这些能力不属于”验签是否通过”的范畴。JWT 解决的是请求准入，浏览器会话治理解决的是登录态生命周期。把这两件事混在一起，系统最后通常会走向两种极端：要么只能等 token 自然过期，要么重新退回全量服务端 Session。</p><p>这里选择的是第三种方式：保留 OAuth2&#x2F;JWT 的请求准入模型，只给浏览器会话补一个可撤销的 SID。</p><h2 id="为什么不是传统-Session"><a href="#为什么不是传统-Session" class="headerlink" title="为什么不是传统 Session"></a>为什么不是传统 Session</h2><p>传统服务端 Session 可以处理撤销、踢人和会话追踪，但在这个场景里，它不是最合适的主模型。</p><p>第一，系统已经有完整的 OAuth2 授权码流程和 JWT 签发机制。浏览器会话层需要补足撤销能力，而不是替换现有 token 体系。</p><p>第二，浏览器页面请求和静态资源请求频率高。如果所有请求都依赖集中式 Session 读取，存储压力和故障影响面都会变大。</p><p>第三，业务 API 已经由网关基于 JWT 做准入控制。会话治理层更适合提供一个清晰的撤销信号，而不是重新承接全部认证上下文。</p><p>所以这套设计的边界是：</p><ul><li>JWT 做 API 请求准入</li><li>Cookie 承载浏览器登录态</li><li>Redis SID 提供会话级撤销信号</li><li>数据库安全戳处理账户级强制失效</li><li>权限版本号处理授权变更同步</li></ul><p>这个边界比”Session 和 Token 选哪个”更重要。</p><h2 id="方案取舍先放在前面"><a href="#方案取舍先放在前面" class="headerlink" title="方案取舍先放在前面"></a>方案取舍先放在前面</h2><p>补 SID 之后，系统换来的是三类能力。</p><p>第一，会话级撤销。删除一个 SID，就能让对应浏览器会话以及通过它签发的 JWT 同时失效，不需要维护 token 黑名单。</p><p>第二，失效语义更细。登出和踢人走 SID；密码修改、账户禁用走安全戳；角色调整、授权增删走权限版本号。不同事件不再挤到一个字段里。</p><p>第三，OAuth2 流程不用推翻。SID 从 Cookie 流入 OAuth2 Authorization，再写入 JWT，原有授权链路仍然成立。</p><p>代价也很直接。</p><p>Redis 不再只是普通缓存，而是会话基础设施。SID 存活不可判定时，系统不能继续放行，否则”登出立即失效”和”管理员踢人立即生效”这两个承诺会破掉。</p><p>每次恢复浏览器登录态、每次网关鉴权，也会多一次 SID 检查。实现上还需要处理 Cookie 签名、SID 传播、安全戳和权限版本号几条链路。</p><p>这不是免费的复杂度。它适合需要会话撤销、权限快速生效、后台管理踢人的系统；不适合没有浏览器会话概念的纯公开 API。</p><h2 id="双层会话结构"><a href="#双层会话结构" class="headerlink" title="双层会话结构"></a>双层会话结构</h2><p>整体结构可以看成三次判断。</p><pre class="mermaid">flowchart LR    subgraph Browser["浏览器"]        Cookie["签名 Cookie\n(HMAC-SHA256)"]    end    subgraph Server["服务端"]        Redis["Redis\nsession:sid:{sid}"]        DB["数据库\nuser.security_stamp"]    end    Cookie -->|"① HMAC 验签 + exp 检查"| Redis    Redis -->|"② SID 存活 + 滑动续期"| DB    DB -->|"③ security_stamp 匹配"| Cookie</pre><p>第一层是 HMAC 签名 Cookie。</p><p>Cookie 中包含用户标识、SID、安全戳和过期时间。服务端可以先做无状态验签和过期检查。签名通过只说明 Cookie 没被篡改，不代表这个会话仍然有效。</p><p>第二层是 Redis SID。</p><p>登录成功时生成一个随机 SID，写入 Redis，并设置 TTL。后续请求只要 SID 还存在，就说明这个浏览器会话仍然有效。登出、踢人、过期，都可以通过删除 SID 或等待 TTL 过期完成。</p><p>第三层是数据库安全戳。</p><p>用户修改密码、管理员重置密码、账户禁用等安全事件，会更新用户表里的 <code>security_stamp</code>。Cookie 和 JWT 中也携带签发时的安全戳。校验时发现不一致，就拒绝恢复认证状态。</p><p>三层里任何一层失败，都按未认证处理。</p><h2 id="Cookie-只承载必要信息"><a href="#Cookie-只承载必要信息" class="headerlink" title="Cookie 只承载必要信息"></a>Cookie 只承载必要信息</h2><p>Cookie 的值做成两段结构：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">base64url(JSON payload) . base64url(HMAC-SHA256(payload))</span><br></pre></td></tr></table></figure><p>一个 payload 示例：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;uid&quot;</span><span class="punctuation">:</span> <span class="number">100</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;email&quot;</span><span class="punctuation">:</span> <span class="string">&quot;user@example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;display_name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;张三&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;sid&quot;</span><span class="punctuation">:</span> <span class="string">&quot;a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;idp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;EMAIL&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;security_stamp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;abc123def456&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exp&quot;</span><span class="punctuation">:</span> <span class="number">1745577600</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>uid</code></td><td>本地用户 ID</td></tr><tr><td><code>sid</code></td><td>当前浏览器会话 ID，对应 Redis key</td></tr><tr><td><code>security_stamp</code></td><td>账户级安全戳，用于强制失效旧会话</td></tr><tr><td><code>exp</code></td><td>Cookie 硬过期时间</td></tr><tr><td><code>idp</code></td><td>身份提供商标识</td></tr><tr><td><code>email</code> &#x2F; <code>display_name</code></td><td>页面渲染常用的用户基本信息</td></tr></tbody></table><p>签名比对使用常量时间比较，例如 <code>MessageDigest.isEqual()</code>。普通逐字节比较可能暴露 timing attack 风险。</p><p>HMAC 密钥应由安全配置服务、密钥管理系统或环境变量注入，不能写进代码仓库。密钥轮换策略也需要提前定义，否则 Cookie 签名机制会变成长期固定密钥依赖。</p><p>Cookie 属性保持基本约束即可：<code>HttpOnly</code>、<code>Secure</code>、<code>SameSite=Lax</code>、<code>Path=/</code>。如果服务部署在 TLS 终止代理之后，<code>Secure</code> 的判断要和真实外部 HTTPS 入口对齐。</p><h2 id="Redis-SID-只做一件事"><a href="#Redis-SID-只做一件事" class="headerlink" title="Redis SID 只做一件事"></a>Redis SID 只做一件事</h2><p>Redis 中的 Session 不做成”胖 Session”。一个很薄的结构就够了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Key:   session:sid:&#123;sid&#125;</span><br><span class="line">Value: &#123;&quot;uid&quot;: 100&#125;</span><br><span class="line">TTL:   43200 秒</span><br></pre></td></tr></table></figure><p>Redis SID 只负责两件事：</p><ul><li>标记这个 Session 是否仍然有效</li><li>在活跃访问时滑动续期</li></ul><p>用户基本信息已经在 Cookie 中。权限、安全状态、账户状态仍然以数据库和策略缓存为准。把大量权限数据塞进 Redis Session，会让权限变更后的同步问题更复杂，也会放大会话存储的维护成本。</p><p>Cookie <code>exp</code> 和 Redis TTL 代表两类边界：</p><ul><li>Cookie <code>exp</code> 是浏览器持有凭证的硬上限，签名后不能被浏览器自己修改</li><li>Redis TTL 是服务端会话的活跃窗口，访问时可以滑动续期</li></ul><p>如果业务需要保持活跃用户持续在线，服务端需要刷新 Cookie 的 <code>exp</code> 字段，并通过 <code>Set-Cookie</code> 返回新的签名 Cookie。否则 Redis 续期只能保证 SID 不过期，Cookie 到硬上限后仍然会失效。</p><p>这个取舍属于会话安全策略，不只是用户体验配置。</p><h2 id="校验流程"><a href="#校验流程" class="headerlink" title="校验流程"></a>校验流程</h2><p>恢复浏览器登录态时，流程大致如下：</p><pre class="mermaid">flowchart TD    A["请求到达"] --> B{"解析签名 Cookie"}    B -->|"不存在"| Z["未认证"]    B -->|"存在"| C{"HMAC 验签"}    C -->|"不匹配"| Z    C -->|"通过"| D{"检查 exp"}    D -->|"已过期"| Z    D -->|"未过期"| E{"Redis SID 存活?"}    E -->|"不存在"| Z    E -->|"存在 + 续期"| F{"DB 安全戳匹配?"}    F -->|"不匹配"| Z    F -->|"匹配"| G["认证通过"]    Z --> H["未认证 / 可清除 Cookie"]</pre><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> SessionClaims <span class="title function_">resolveActiveClaims</span><span class="params">(Request request, Response response)</span> &#123;</span><br><span class="line">    <span class="type">SessionClaims</span> <span class="variable">claims</span> <span class="operator">=</span> decodeAndVerifyCookie(request);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (claims == <span class="literal">null</span> || claims.sid() == <span class="literal">null</span> || claims.securityStamp() == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sessionStore.isSidActive(claims.sid())) &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userRepo.findActiveById(claims.userId());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (user != <span class="literal">null</span> &amp;&amp; claims.securityStamp().equals(user.getSecurityStamp())) &#123;</span><br><span class="line">            <span class="keyword">return</span> claims;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    clearCookie(request, response);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>三个判断对应三类系统语义：</p><table><thead><tr><th>层级</th><th>检查内容</th><th>解决的问题</th></tr></thead><tbody><tr><td>HMAC Cookie</td><td>是否被篡改、是否过期</td><td>防止伪造 UID、SID、exp</td></tr><tr><td>Redis SID</td><td>会话是否仍然有效</td><td>支持登出、踢人、会话过期</td></tr><tr><td>DB 安全戳</td><td>账户安全状态是否变化</td><td>支持密码修改、账户禁用后强制失效</td></tr></tbody></table><p>对格式错误、签名错误这类输入，直接按未认证处理。对已经能判定失效的请求，可以清 Cookie。HMAC 验签通过后跳过 SID 检查，会把”可撤销会话”退回成”不可撤销 Cookie”。</p><h2 id="SID-如何进入-JWT"><a href="#SID-如何进入-JWT" class="headerlink" title="SID 如何进入 JWT"></a>SID 如何进入 JWT</h2><p>浏览器 Cookie 里的 SID 不是孤立存在的，它会传播到 OAuth2 Authorization，再进入 JWT。</p><pre class="mermaid">sequenceDiagram    participant B as 浏览器    participant Auth as 认证服务    participant Redis as Redis    participant GW as API 网关    B->>Auth: 登录（密码 / 联合登录）    Auth->>Redis: 创建 SID    Auth-->>B: Set-Cookie: 签名 Cookie（含 sid）    B->>Auth: OAuth2 授权请求（带 Cookie）    Auth->>Auth: 从 Cookie 提取 sid    Auth->>Auth: 将 sid 附加到 OAuth2 Authorization    Auth->>Auth: 签发 JWT（写入 sid + 安全戳 + 权限版本号）    Auth-->>B: 返回 JWT    B->>GW: API 请求（Bearer JWT）    GW->>Auth: 鉴权回调    Auth->>Auth: 解码 JWT，提取 sid    Auth->>Redis: 检查 SID 存活    Auth->>Auth: 策略决策    Auth-->>GW: 200 + 用户头信息    GW-->>B: 业务响应</pre><p>登录成功时，认证服务创建 SID，并写入签名 Cookie。</p><p>浏览器随后发起 OAuth2 授权请求时，认证服务从 Cookie 中读取 SID，并把它附加到 OAuth2 Authorization 对象。</p><p>Token 签发时，再把 SID 写入 JWT claims：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">sid</span> <span class="operator">=</span> context.getAuthorization().getAttribute(<span class="string">&quot;sid&quot;</span>);</span><br><span class="line"></span><br><span class="line">context.getClaims().claim(<span class="string">&quot;sid&quot;</span>, sid);</span><br><span class="line">context.getClaims().claim(<span class="string">&quot;security_stamp&quot;</span>, user.getSecurityStamp());</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (isAccessToken) &#123;</span><br><span class="line">    context.getClaims().claim(<span class="string">&quot;token_version&quot;</span>, user.getPermissionVersion());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Cookie 中的 SID 和 JWT 中的 SID 是同一个值。撤销 Redis 里的这个 SID 后，浏览器页面请求和 API 请求都会在后续校验中失效。</p><p>token 黑名单的粒度是 token，SID 的粒度是会话。一个浏览器会话里签发过多少 access token，并不影响撤销动作；只要它们共享同一个 SID，就可以用一个会话信号统一失效。</p><h2 id="两类撤销信号"><a href="#两类撤销信号" class="headerlink" title="两类撤销信号"></a>两类撤销信号</h2><p>会话治理不适合把所有失效场景都压到一个字段上。这里拆成两个信号：</p><ul><li><code>security_stamp</code>：账户级安全事件</li><li><code>permission_version</code>：权限变更事件</li></ul><p><code>security_stamp</code> 存在用户表中，通常在用户修改密码、管理员重置密码、账户禁用等事件发生时更新。安全戳同时写入 Cookie 和 JWT。数据库里的当前值一变，旧 Cookie 和旧 JWT 都会失效。</p><p><code>permission_version</code> 也是用户表中的字段，每次角色调整、授权增删时递增。它主要写入 access token，在网关鉴权时和当前策略版本比较。旧 access token 中的版本号落后，网关拒绝请求，客户端重新走授权流程拿新 token。</p><p>两者拆开后，用户体验会更可控：</p><table><thead><tr><th>维度</th><th>安全戳</th><th>权限版本号</th></tr></thead><tbody><tr><td>触发场景</td><td>密码修改、账户禁用</td><td>角色调整、授权增删</td></tr><tr><td>影响范围</td><td>Cookie + JWT</td><td>主要是 access token</td></tr><tr><td>用户体感</td><td>通常需要重新登录</td><td>通常重新获取 token</td></tr><tr><td>更新频率</td><td>低</td><td>中</td></tr></tbody></table><p>如果只用一个字段，每次改权限都可能导致全端重新登录。权限变更的频率通常高于安全事件，拆开以后，失效范围更接近真实业务事件。</p><h2 id="网关鉴权链路"><a href="#网关鉴权链路" class="headerlink" title="网关鉴权链路"></a>网关鉴权链路</h2><p>API 请求走网关时，JWT 校验不只看签名和过期时间，还要检查 SID 和策略版本。</p><pre class="mermaid">flowchart LR    Client["客户端"] -->|"Bearer JWT"| GW["API 网关"]    GW -->|"forward-auth\n转发 Authorization 头"| Auth["认证服务"]    Auth -->|"① 解码 JWT"| Auth    Auth -->|"② SID 存活检查"| Redis["Redis"]    Auth -->|"③ 策略决策"| Policy["策略缓存 / DB"]    Auth -->|"200 + 用户头"| GW    GW -->|"注入请求头"| Svc["业务服务"]</pre><p>一个典型的 forward-auth 流程：</p><ol><li>客户端携带 <code>Bearer JWT</code> 请求 API 网关</li><li>网关把 Authorization 头转发给认证服务</li><li>认证服务解码 JWT，读取 <code>uid</code>、<code>sid</code>、<code>security_stamp</code>、<code>token_version</code></li><li>检查 Redis SID 是否仍然存在</li><li>读取用户策略，比较安全戳和权限版本号</li><li>鉴权通过后，网关注入用户头信息，再转发给业务服务</li></ol><p>这里可以支持两种模式：</p><ul><li><code>authenticated</code>：只确认请求是谁发的，检查用户状态、SID、安全戳、权限版本号</li><li><code>entitled</code>：在 authenticated 基础上继续做 RBAC、应用授权、资源授权</li></ul><p>两种模式的差别在授权深度，不在会话校验。SID 和安全戳仍然是共同的基础层。</p><h2 id="Redis-不可用时怎么处理"><a href="#Redis-不可用时怎么处理" class="headerlink" title="Redis 不可用时怎么处理"></a>Redis 不可用时怎么处理</h2><p>这个设计引入了 Redis 依赖，因此必须定义故障语义。</p><p>策略缓存不可用，可以回 DB。因为策略数据有权威来源，Redis 只是缓存。</p><p>SID 存活检查不一样。SID 是会话是否仍然有效的状态源。Redis 不可用时，系统并不知道这个会话有没有被撤销。继续放行，会破坏”登出立即失效””管理员踢人立即生效”的承诺。</p><p>因此 SID 校验路径更适合 fail-closed：Redis 不可用时拒绝会话恢复和 API 鉴权。</p><p>这也意味着 Redis 高可用不是可选项。主从复制结合 Sentinel、Redis Cluster，或者其他符合部署条件的高可用方案，需要作为会话基础设施的一部分来设计，而不是上线后再补。</p><h2 id="登出和踢人"><a href="#登出和踢人" class="headerlink" title="登出和踢人"></a>登出和踢人</h2><p>登出时同时清两层：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">logout</span><span class="params">(Request request, Response response)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sid</span> <span class="operator">=</span> cookieService.resolveSid(request);</span><br><span class="line"></span><br><span class="line">    sessionStore.invalidateSid(sid);</span><br><span class="line">    cookieService.clearCookie(request, response);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>删除 Redis SID 后，影响会同时覆盖两条链路：</p><ul><li>浏览器请求：Cookie 可能仍能验签，但 SID 检查失败</li><li>API 请求：JWT 可能仍未过期，但 JWT 中的 SID 检查失败</li></ul><p>踢掉某个指定浏览器会话，本质上也是删除对应 SID。</p><p>让一个用户的所有会话失效，则更适合更新 <code>security_stamp</code>。这会让该用户旧 Cookie 和旧 JWT 中的安全戳全部失配。</p><h2 id="适用边界"><a href="#适用边界" class="headerlink" title="适用边界"></a>适用边界</h2><table><thead><tr><th>场景</th><th>判断</th></tr></thead><tbody><tr><td>内部系统，需要登出立即失效</td><td>适合</td></tr><tr><td>管理后台，需要管理员踢人</td><td>适合</td></tr><tr><td>权限调整需要快速生效</td><td>适合</td></tr><tr><td>浏览器会话和 API 令牌需要统一治理</td><td>适合</td></tr><tr><td>纯公开 API，没有浏览器会话概念</td><td>不太适合</td></tr><tr><td>跨顶级域名 SSO</td><td>需要协议级方案，不能只靠 Cookie</td></tr><tr><td>极高并发且不能承担集中式会话依赖</td><td>需要重新评估 Redis 架构</td></tr></tbody></table><p>这个设计不是为了证明 Cookie 比 JWT 更好，也不是为了证明 Session 比 Token 更安全。它做的是职责分离：</p><ul><li>JWT 做请求准入凭证</li><li>Cookie 做浏览器登录态承载</li><li>Redis SID 做会话撤销信号</li><li>安全戳做账户级强制失效</li><li>权限版本号做授权变更同步</li></ul><p>这些信号拆开以后，每个字段影响什么、在哪里校验、故障时怎么处理，都会更清楚。架构上的收益不只来自”多加了一层 Redis”，而是来自失效语义被拆到了合适的粒度。</p><h2 id="延伸阅读"><a href="#延伸阅读" class="headerlink" title="延伸阅读"></a>延伸阅读</h2><ul><li><a href="/2026/03/17/oauth2-oidc-session-timeout-boundaries/">OAuth2&#x2F;OIDC 会话超时的边界：前端 Token、Refresh Token 与 SSO Session</a></li><li><a href="/2026/04/06/why-local-auth-alongside-federation/">联合登录之外：IAM 认证路径的韧性设计</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc9121">RFC 9121: Use of the HMAC Algorithm in JSON Object Signing and Encryption (JOSE)</a></li></ul>]]></content>
    
    
    <summary type="html">在 OAuth2/JWT 体系下，用 HMAC 签名 Cookie + Redis SID 双层结构管理浏览器会话，把浏览器登录态、令牌生命周期和撤销信号放到同一套治理模型里。</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="session" scheme="https://www.yzhu.name/tags/session/"/>
    
    <category term="redis" scheme="https://www.yzhu.name/tags/redis/"/>
    
    <category term="cookie" scheme="https://www.yzhu.name/tags/cookie/"/>
    
    <category term="jwt" scheme="https://www.yzhu.name/tags/jwt/"/>
    
  </entry>
  
  <entry>
    <title>为什么企业 IAM 在联合登录之外还需要本地密码认证</title>
    <link href="https://www.yzhu.name/2026/04/06/why-local-auth-alongside-federation/"/>
    <id>https://www.yzhu.name/2026/04/06/why-local-auth-alongside-federation/</id>
    <published>2026-04-06T02:00:00.000Z</published>
    <updated>2026-04-06T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>在上一篇<a href="/2026/04/04/enterprise-unified-identity-architecture/">《基于 OAuth2&#x2F;OIDC 的企业级统一身份治理架构实践》</a>中，我们展示了身份映射表中同时存在联合身份（<code>OIDC_SUB</code>）和本地身份（<code>EMAIL_PWD</code>）的设计。有读者提出一个问题：既然认证已经委托给上游 IdP，为什么还要自建用户名密码登录？这是否会引入额外的安全成本？</p><p>这个问题涉及认证架构中一个常被忽视的维度——<strong>认证路径的韧性设计</strong>。本文将从容灾场景、安全成本分布和架构收敛三个角度展开分析。</p><hr><h2 id="一、身份映射表中的主备关系"><a href="#一、身份映射表中的主备关系" class="headerlink" title="一、身份映射表中的主备关系"></a>一、身份映射表中的主备关系</h2><p>回顾上篇文章中的身份映射结构：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">┌──────────┬──────────────┬──────────────────┬──────────────────┐</span><br><span class="line">│ user_id  │  identifier  │  identity_type   │      角色         │</span><br><span class="line">├──────────┼──────────────┼──────────────────┼──────────────────┤</span><br><span class="line">│   100    │ aaa111bbb... │ OIDC_SUB         │  主力联合身份     │</span><br><span class="line">│   100    │ zhang@co.com │ EMAIL_PWD        │  容灾/备用身份    │</span><br><span class="line">└──────────┴──────────────┴──────────────────┴──────────────────┘</span><br></pre></td></tr></table></figure><p>同一个用户绑定两种身份标识，它们在架构中的定位并非并列，而是主备关系：</p><ul><li><strong>OIDC_SUB</strong>：主力联合身份，承载日常 99% 的认证流量</li><li><strong>EMAIL_PWD</strong>：容灾备用身份，在主力路径不可用时提供替代入口</li></ul><p>这种设计的核心思路是将认证路径与最终身份标识解耦——无论通过哪条路径进入，下游系统看到的始终是统一的 <code>uid</code>。</p><hr><h2 id="二、上游-IdP-的可用性边界"><a href="#二、上游-IdP-的可用性边界" class="headerlink" title="二、上游 IdP 的可用性边界"></a>二、上游 IdP 的可用性边界</h2><h3 id="2-1-外部服务的故障现实"><a href="#2-1-外部服务的故障现实" class="headerlink" title="2.1 外部服务的故障现实"></a>2.1 外部服务的故障现实</h3><p>任何外部 SaaS 服务都存在不可用的可能性。企业级身份提供商（Azure AD、Google Workspace、Okta 等）历史上发生过多次全球性服务中断：</p><ul><li><strong>2023 年 7 月</strong>：Azure AD&#x2F;Entra ID 大规模中断，影响 Microsoft 365、Teams、Power Platform 等全部依赖其认证的服务</li><li><strong>2024 年 1 月</strong>：网络路由变更导致的全球性认证中断</li></ul><p>即使 SLA 达到 99.99%，年度计划外不可用时间仍有约 52 分钟。故障往往集中出现在工作日的业务高峰期。</p><h3 id="2-2-完全依赖联合登录的风险链路"><a href="#2-2-完全依赖联合登录的风险链路" class="headerlink" title="2.2 完全依赖联合登录的风险链路"></a>2.2 完全依赖联合登录的风险链路</h3><p>当 IAM 系统 100% 依赖上游 IdP 时，故障传播路径如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">上游 IdP 服务中断</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">所有依赖联合登录的内部系统无法认证</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">运维团队需要登录管理后台进行应急处理</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">管理后台同样依赖上游 IdP 认证</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">运维人员无法进入系统执行恢复操作</span><br></pre></td></tr></table></figure><p>这个场景与数据中心需要带外管理（Out-of-Band Management）的逻辑一致——当主网络通道失效时，需要一条完全不依赖主干网络的独立通道来恢复系统。本地密码认证在 IAM 架构中承担的就是这个角色。</p><hr><h2 id="三、其他需要本地认证的场景"><a href="#三、其他需要本地认证的场景" class="headerlink" title="三、其他需要本地认证的场景"></a>三、其他需要本地认证的场景</h2><h3 id="3-1-IdP-供应商迁移"><a href="#3-1-IdP-供应商迁移" class="headerlink" title="3.1 IdP 供应商迁移"></a>3.1 IdP 供应商迁移</h3><p>企业在生命周期中可能经历身份提供商的切换：从 Okta 迁移到 Entra ID、从 Entra ID 迁移到 Google Workspace、从一个 OIDC Provider 切换到另一个等。迁移过渡期的状态通常为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">旧 IdP —— 正在停用，部分用户已迁出</span><br><span class="line">新 IdP —— 正在启用，部分用户尚未迁入</span><br><span class="line"></span><br><span class="line">过渡期内，两边的联合身份都可能存在不确定性</span><br></pre></td></tr></table></figure><p>本地凭证在此阶段提供稳定的认证手段。</p><h3 id="3-2-系统初始部署"><a href="#3-2-系统初始部署" class="headerlink" title="3.2 系统初始部署"></a>3.2 系统初始部署</h3><p>IAM 系统自身就是认证基础设施。在系统初始部署阶段，上游 IdP 的集成尚未完成配置，首个超级管理员需要一个不依赖任何外部服务的入口来完成系统初始化。这是系统的自举（bootstrap）问题。</p><h3 id="3-3-邮箱域名变更与账号锁定"><a href="#3-3-邮箱域名变更与账号锁定" class="headerlink" title="3.3 邮箱域名变更与账号锁定"></a>3.3 邮箱域名变更与账号锁定</h3><p>公司并购、品牌重塑导致的邮箱域名变更，或上游 IdP 智能锁定机制触发的账号冻结，都可能使联合身份暂时不可用。对于需要执行紧急操作的系统管理员，独立的本地凭证提供了一条不受上游状态影响的通道。</p><hr><h2 id="四、安全成本的实际分布"><a href="#四、安全成本的实际分布" class="headerlink" title="四、安全成本的实际分布"></a>四、安全成本的实际分布</h2><h3 id="4-1-用户规模决定安全投入量级"><a href="#4-1-用户规模决定安全投入量级" class="headerlink" title="4.1 用户规模决定安全投入量级"></a>4.1 用户规模决定安全投入量级</h3><p>读者担心的逻辑链条是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">自建密码登录 → 需要自行管理密码安全 → MFA、暴力破解防护、泄露检测……</span><br><span class="line">→ 安全成本显著增加</span><br></pre></td></tr></table></figure><p>这个推导的前提假设是本地密码登录与联合登录面向相同的用户规模。在实际架构中，两个认证路径面向的用户群体存在显著差异：</p><table><thead><tr><th>维度</th><th>联合登录（上游 IdP）</th><th>本地密码登录</th></tr></thead><tbody><tr><td>用户规模</td><td>全员（数百至上千人）</td><td>超级管理员（个位数）</td></tr><tr><td>攻击面</td><td>所有可访问公司网络的人员</td><td>已知身份的内部管理员</td></tr><tr><td>所需安全深度</td><td>MFA + 条件访问 + 风险评估 + 泄露检测</td><td>密码复杂度 + BCrypt + 失败锁定 + HTTPS</td></tr><tr><td>安全投入承担方</td><td>上游 IdP</td><td>本地系统</td></tr></tbody></table><h3 id="4-2-安全面的收窄效应"><a href="#4-2-安全面的收窄效应" class="headerlink" title="4.2 安全面的收窄效应"></a>4.2 安全面的收窄效应</h3><p>引入联合登录后，安全成本的实际变化是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">引入前：</span><br><span class="line">  全员密码安全 = 密码策略 + MFA + 暴力破解防护 + 泄露检测 + SSO</span><br><span class="line"></span><br><span class="line">引入后：</span><br><span class="line">  全员 → 安全成本转移至上游 IdP</span><br><span class="line">  个位数管理员 → 本地承担有限范围的密码安全</span><br></pre></td></tr></table></figure><p>安全面从全员收窄至少数管理员，整体安全投入是下降的。</p><h3 id="4-3-本地密码登录的最小安全清单"><a href="#4-3-本地密码登录的最小安全清单" class="headerlink" title="4.3 本地密码登录的最小安全清单"></a>4.3 本地密码登录的最小安全清单</h3><p>面向个位数管理员的场景，最小必要安全措施如下：</p><table><thead><tr><th>措施</th><th>实现成本</th><th>说明</th></tr></thead><tbody><tr><td>密码复杂度策略</td><td>低</td><td>长度、字符类型要求</td></tr><tr><td>BCrypt 加密存储</td><td>低</td><td>Spring Security 默认支持，strength&#x3D;10</td></tr><tr><td>失败次数锁定</td><td>低</td><td>N 次失败后锁定 M 分钟</td></tr><tr><td>强制 HTTPS</td><td>零</td><td>网关层统一处理</td></tr><tr><td>登录审计日志</td><td>低</td><td>记录时间、IP、结果</td></tr><tr><td>权限即时撤销</td><td>低</td><td>permission_version 机制</td></tr></tbody></table><p>对于 5 个已知身份的管理员，上述 6 项措施已足够。面向数百人的场景则需要更完整的策略体系——这正是将主力认证交给上游 IdP 的原因。</p><hr><h2 id="五、认证路径的最终收敛"><a href="#五、认证路径的最终收敛" class="headerlink" title="五、认证路径的最终收敛"></a>五、认证路径的最终收敛</h2><p>架构设计的核心意图在于认证路径的收敛：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">OIDC 联合登录 → auth_identity 匹配 OIDC_SUB  → user_id = 100 → JWT uid = 100</span><br><span class="line">本地密码登录  → auth_identity 匹配 EMAIL_PWD → user_id = 100 → JWT uid = 100</span><br></pre></td></tr></table></figure><p>两条路径、两种身份标识、同一个用户。下游业务系统看到的始终是统一的 <code>uid</code>，无需感知用户的认证入口。</p><pre class="mermaid">graph LR    A[OIDC 联合登录] --> C[auth_identity 匹配]    B[本地密码登录] --> C    C --> D[user_id = 100]    D --> E[签发 JWT uid=100]    E --> F[下游系统 A]    E --> G[下游系统 B]    E --> H[下游系统 C]</pre><p>这种设计使认证入口的选择成为架构层面的韧性决策，而非下游系统需要处理的业务逻辑。</p><hr><h2 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h2><table><thead><tr><th>关注点</th><th>架构考量</th></tr></thead><tbody><tr><td>重复建设</td><td>联合登录与本地认证是主备关系，非功能重复</td></tr><tr><td>安全成本</td><td>安全面从全员收窄至个位数管理员，整体投入下降</td></tr><tr><td>上游可用性</td><td>任何外部服务都存在不可用场景，需要独立逃生通道</td></tr><tr><td>架构收敛</td><td>多认证路径最终统一至同一 uid，下游系统无感知</td></tr></tbody></table><p>在统一身份治理架构中，本地密码认证作为容灾兜底机制存在，安全成本在可控范围内。将主力认证流量委托给上游 IdP，同时保留极小范围的本地认证能力，是认证架构韧性设计的工程取舍。</p>]]></content>
    
    
    <summary type="html">探讨企业 IAM 架构中同时保留联合登录与本地密码认证的设计考量，分析容灾场景、安全成本分布与认证路径收敛的工程实践。</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="oidc" scheme="https://www.yzhu.name/tags/oidc/"/>
    
    <category term="identity-management" scheme="https://www.yzhu.name/tags/identity-management/"/>
    
    <category term="enterprise-architecture" scheme="https://www.yzhu.name/tags/enterprise-architecture/"/>
    
    <category term="sso" scheme="https://www.yzhu.name/tags/sso/"/>
    
  </entry>
  
  <entry>
    <title>Enterprise Unified Identity Governance Architecture with OAuth2/OIDC</title>
    <link href="https://www.yzhu.name/2026/04/04/enterprise-unified-identity-architecture-en/"/>
    <id>https://www.yzhu.name/2026/04/04/enterprise-unified-identity-architecture-en/</id>
    <published>2026-04-04T02:00:00.000Z</published>
    <updated>2026-04-04T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/04/04/enterprise-unified-identity-architecture/">中文版</a></p></blockquote><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>This article presents a production-grade identity governance architecture that bridges upstream Identity Providers with internal business systems through a protocol-agnostic identity relay layer. Key highlights:</p><ul><li><strong>Clean separation of concerns</strong>: Upstream IdP handles identity assertion, internal IAM handles access control and profile distribution</li><li><strong>Identity relay pattern</strong>: OIDC federation with standardized JWT issuance and gateway-based header injection</li><li><strong>SID-based session management</strong>: Embedding Session IDs in stateless JWTs for instant revocation and global logout</li><li><strong>Permission versioning</strong>: Minute-level propagation of access policy changes through version consistency checks</li></ul><hr><h2 id="1-Background-Challenges"><a href="#1-Background-Challenges" class="headerlink" title="1. Background &amp; Challenges"></a>1. Background &amp; Challenges</h2><p>In enterprise environments with multiple business systems running in parallel (OA, CRM, Project Management, Finance, etc.), centralized identity governance is critical to solving data silos. Common challenges include:</p><ul><li><strong>Scattered access control logic</strong>: Each business system implements access control independently, leading to multi-point refactoring when policies change</li><li><strong>Inconsistent identity identifiers</strong>: Different services use conflicting identification rules (Email, Subject ID) for the same user</li><li><strong>Duplicated attribute management</strong>: Organization architecture exists in multiple copies without a single source of truth</li></ul><p>Mature identity sources (Upstream IdP) primarily handle identity authenticity assertions. This architecture builds an <strong>identity relay layer</strong> based on standard OAuth2&#x2F;OIDC protocols to achieve unified access decisions and identity profile aggregation across systems.</p><hr><h2 id="2-Logical-Architecture-Design"><a href="#2-Logical-Architecture-Design" class="headerlink" title="2. Logical Architecture Design"></a>2. Logical Architecture Design</h2><p><strong>Core principle</strong>: <em>Upstream Identity Provider is responsible for identity authenticity, while internal identity system handles access decisions and attribute distribution.</em></p><h3 id="2-1-Architecture-Overview"><a href="#2-1-Architecture-Overview" class="headerlink" title="2.1 Architecture Overview"></a>2.1 Architecture Overview</h3><pre class="mermaid">graph TB    subgraph U["User Layer"]        EU["Enterprise Employee"]        AD["System Admin"]    end    subgraph A["External Identity Source (Upstream IdP)"]        IDP["OIDC Provider"]    end    subgraph I["Identity Governance Layer (Internal IAM)"]        IAS["Identity Access Service (IAS) - Access Decision Engine + Session Management"]        IMA["Identity Management API (IMA) - Read/Write Management Backend"]        IDS["Identity Data Service (IDS) - Identity Profile Query (Read-Only)"]        ISJ["Identity Sync Job (ISJ) - Mirror Data Sync"]    end    subgraph G["API Gateway Layer"]        GW["API Gateway - Identity Pass-through + Unified Authentication"]    end    subgraph B["Business System Layer"]        APP1["Business System A"]        APP2["Business System B"]        APP3["Business System C"]    end    EU -->|OIDC SSO| IDP    EU -.->|OIDC SSO| IAS    IDP -->|OIDC Federation| IAS    AD -->|Email/Password| IAS    IAS -->|Issue Internal Standard JWT| GW    GW -->|Header Injection X-User-Id| APP1    GW -->|Header Injection| APP2    GW -->|Header Injection| APP3    APP1 -->|Query User Profile| IDS    APP2 -->|Query User Profile| IDS    APP3 -->|Query User Profile| IDS    IMA -.->|Config Distribution| IAS    ISJ -->|Incremental Sync| IDP</pre><h3 id="2-2-Interaction-Flow-Analysis-Identity-Flows"><a href="#2-2-Interaction-Flow-Analysis-Identity-Flows" class="headerlink" title="2.2 Interaction Flow Analysis (Identity Flows)"></a>2.2 Interaction Flow Analysis (Identity Flows)</h3><h4 id="A-Federated-Login-Flow-OIDC-Federation"><a href="#A-Federated-Login-Flow-OIDC-Federation" class="headerlink" title="A. Federated Login Flow (OIDC Federation)"></a>A. Federated Login Flow (OIDC Federation)</h4><p>This flow implements identity relay based on standard OAuth2 protocol, supporting seamless integration with any standard OIDC Provider:</p><pre class="mermaid">sequenceDiagram    participant Browser as User Browser    participant GW as API Gateway    participant IAS as Identity Access Service (IAS)    participant IdP as Upstream IdP    Browser->>GW: Access Restricted System URL    GW->>IAS: Forward-Auth (JWT Validation)    IAS-->>GW: Unauthenticated (302 Redirect)    GW-->>Browser: 302 → Redirect to Login Page    Browser->>IAS: Select "Enterprise Account Login"    IAS-->>Browser: 302 → Redirect to Upstream IdP    Browser->>IdP: Submit Identity Assertion (MFA/SSO)    IdP-->>Browser: Callback IAS (Authorization Code)    Browser->>IAS: /oauth2/callback?code=xxxx    IAS->>IdP: Exchange Code for Token & UserInfo    IAS->>IAS: Internal Identity Aggregation (Ext ID → UID)    IAS->>IAS: Create Session (SID)    IAS-->>Browser: Issue Internal JWT (with UID, SID) + Set Auth Cookie    Browser->>GW: Re-access with JWT    GW->>IAS: Forward-Auth Decision Passed    GW->>Browser: Forward to Downstream Business System</pre><h4 id="B-Transparent-Authentication-Access-Flow-Decision-Engine"><a href="#B-Transparent-Authentication-Access-Flow-Decision-Engine" class="headerlink" title="B. Transparent Authentication &amp; Access Flow (Decision Engine)"></a>B. Transparent Authentication &amp; Access Flow (Decision Engine)</h4><p>The gateway layer performs real-time fine-grained control through the access decision engine:</p><pre class="mermaid">sequenceDiagram    participant Browser as User Browser    participant GW as API Gateway    participant IAS as Identity Access Service (IAS)    participant Backend as Business System    Browser->>GW: Access Business API with JWT    GW->>IAS: Forward-Auth Call    IAS->>IAS: 1. JWT Signature Validation    IAS->>IAS: 2. Permission Version Validation (Permission Consistency)    IAS->>IAS: 3. Active Session Validation (Check SID in Cache)    IAS->>IAS: 4. Application-level Access Decision (Access Level Check)    alt Access Granted        IAS-->>GW: 200 OK + Header (X-User-Id, X-Sid)        GW->>Backend: Map Identity Context to Request        Backend-->>Browser: Return Data    else Access Denied        IAS-->>GW: 403 Forbidden        GW-->>Browser: 403 Forbidden    end</pre><hr><h2 id="3-Key-Design-Decisions-Rationale"><a href="#3-Key-Design-Decisions-Rationale" class="headerlink" title="3. Key Design Decisions (Rationale)"></a>3. Key Design Decisions (Rationale)</h2><h3 id="3-1-Decoupling-Boundary-Access-Control-vs-Business-Permissions"><a href="#3-1-Decoupling-Boundary-Access-Control-vs-Business-Permissions" class="headerlink" title="3.1 Decoupling Boundary: Access Control vs. Business Permissions"></a>3.1 Decoupling Boundary: Access Control vs. Business Permissions</h3><ul><li><strong>Access Control</strong>: Determined by the identity system — whether this user has permission to enter this application</li><li><strong>Feature Permission</strong>: Determined internally by the application using attributes provided by IAM</li></ul><p><strong>Decision rationale</strong>: In this architecture design, the identity system serves as an <strong>Attribute Provider</strong>, while business systems remain <strong>Rule Definers</strong>, preventing the central identity system from over-penetrating business details.</p><h3 id="3-2-Technology-Selection-Protocol-Neutrality"><a href="#3-2-Technology-Selection-Protocol-Neutrality" class="headerlink" title="3.2 Technology Selection: Protocol Neutrality"></a>3.2 Technology Selection: Protocol Neutrality</h3><p>The architecture chooses <strong>Spring Authorization Server</strong> to build the IAS core:</p><ul><li><strong>Advantage</strong>: Its pure OAuth2 implementation supports abstracting any third-party Identity Provider as an “identity source”</li><li><strong>Comparison</strong>: Compared to Keycloak, it provides more flexible API-level extensions for inserting custom “access decision chains”</li></ul><h3 id="3-3-Balance-Between-Stateless-and-Stateful-SID-Design"><a href="#3-3-Balance-Between-Stateless-and-Stateful-SID-Design" class="headerlink" title="3.3 Balance Between Stateless and Stateful (SID Design)"></a>3.3 Balance Between Stateless and Stateful (SID Design)</h3><p>To support “instant permission revocation” and “global logout”:</p><ul><li><strong>Design point</strong>: Embed Session ID (SID) in JWT, maintained by backend Redis cluster</li><li><strong>Decision rationale</strong>: OAuth2 protocol itself doesn’t include session invalidation semantics. The SID mechanism supplements real-time session logout capability to meet enterprise-level management precision requirements</li></ul><h3 id="3-4-Instant-Permission-Effect-Version-Consistency-Validation"><a href="#3-4-Instant-Permission-Effect-Version-Consistency-Validation" class="headerlink" title="3.4 Instant Permission Effect: Version Consistency Validation"></a>3.4 Instant Permission Effect: Version Consistency Validation</h3><p>To achieve “minute-level response” to changes:</p><ol><li>User-permission mapping holds <code>permission_version</code></li><li>Gateway access performs version matching validation</li><li>Once administrators modify access policies, the version number increments, causing all active tokens to immediately fail and triggering re-authentication logic</li></ol><hr><h2 id="4-Identity-Aggregation-UID-Driven-Unified-Profile"><a href="#4-Identity-Aggregation-UID-Driven-Unified-Profile" class="headerlink" title="4. Identity Aggregation: UID-Driven Unified Profile"></a>4. Identity Aggregation: UID-Driven Unified Profile</h2><p>The architecture converts heterogeneous account identifiers to globally unique <code>UID</code> through “identity federation mapping”:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────┐</span><br><span class="line">│         Identity Federation Mapping    │</span><br><span class="line">├──────────┬─────────────┬─────────────┤</span><br><span class="line">│  UID     │ External ID │ Identity Type│</span><br><span class="line">├──────────┼─────────────┼─────────────┤</span><br><span class="line">│    100   │ aaa111bbb.. │ OIDC_SUB/OID │  ← Primary Federated Identity</span><br><span class="line">│    100   │ user@domain │ EMAIL_PWD    │  ← Fallback/Backup Identity</span><br><span class="line">└──────────┴─────────────┴─────────────┘</span><br></pre></td></tr></table></figure><p>The core design achieves <strong>IDP-Agnostic</strong> (identity source independence), ensuring that upstream account changes (e.g., Email domain changes due to corporate renaming) don’t affect data associations in business systems.</p><hr><h2 id="5-Collaboration-Boundaries"><a href="#5-Collaboration-Boundaries" class="headerlink" title="5. Collaboration Boundaries"></a>5. Collaboration Boundaries</h2><p>Standardized protocols clarify collaboration between the identity governance system and upstream IdP:</p><table><thead><tr><th>Responsibility Party</th><th>Core Capabilities</th></tr></thead><tbody><tr><td><strong>Upstream IdP</strong></td><td>Identity Assertion, MFA&#x2F;SSO, Lifecycle Sync (HR System Integration)</td></tr><tr><td><strong>Internal Governance Layer</strong></td><td>Access Decisions, Session Consistency Management, Enterprise Profile Enrichment, Global Operation Audit</td></tr></tbody></table><hr><h2 id="6-Key-Takeaways"><a href="#6-Key-Takeaways" class="headerlink" title="6. Key Takeaways"></a>6. Key Takeaways</h2><p>This architecture aims to establish a protocol-neutral, clearly-responsible identity governance bridge:</p><ol><li><strong>Protocol-neutral</strong>: Based on standard protocols, not bound to specific vendors, supporting any OIDC Provider</li><li><strong>Consistent governance</strong>: Global trusted identity distribution through gateway injection</li><li><strong>Controlled management</strong>: Combining stateless protocol flexibility with stateful management security through SID and versioning mechanisms</li></ol>]]></content>
    
    
    <summary type="html">Building an enterprise-grade identity relay architecture based on standard OAuth2/OIDC protocols for unified access decisions and identity profile aggregation across systems, covering federated login flow, transparent authentication, SID session management, and instant permission propagation.</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="oidc" scheme="https://www.yzhu.name/tags/oidc/"/>
    
    <category term="identity-management" scheme="https://www.yzhu.name/tags/identity-management/"/>
    
    <category term="enterprise-architecture" scheme="https://www.yzhu.name/tags/enterprise-architecture/"/>
    
    <category term="sso" scheme="https://www.yzhu.name/tags/sso/"/>
    
  </entry>
  
  <entry>
    <title>基于 OAuth2/OIDC 协议的企业级统一身份治理架构实践</title>
    <link href="https://www.yzhu.name/2026/04/04/enterprise-unified-identity-architecture/"/>
    <id>https://www.yzhu.name/2026/04/04/enterprise-unified-identity-architecture/</id>
    <published>2026-04-04T02:00:00.000Z</published>
    <updated>2026-04-04T02:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <a href="/2026/04/04/enterprise-unified-identity-architecture-en/">English Version</a> | <strong>中文版</strong></p></blockquote><h2 id="1-架构场景与背景分析"><a href="#1-架构场景与背景分析" class="headerlink" title="1. 架构场景与背景分析"></a>1. 架构场景与背景分析</h2><p>在多业务系统并行的企业环境（如 OA、CRM、项目管理、财务等），集中化的身份治理是解决数据孤岛的关键。常见挑战包括：</p><ul><li><strong>准入逻辑分散</strong>：各业务系统独立实现准入，策略变更导致多点重构。</li><li><strong>身份标识不一</strong>：不同服务对同一用户的标识规则（Email、Subject ID）冲突。</li><li><strong>属性管理重复</strong>：组织架构在多处副本，缺乏单一事实来源。</li></ul><p>成熟的身份源（Upstream IdP）主要负责解决身份真实性断言。本架构基于标准 OAuth2&#x2F;OIDC 协议构建了身份中继层（Identity Relay Layer），实现了跨系统的统一准入决策与身份画像汇聚。</p><hr><h2 id="2-逻辑架构设计"><a href="#2-逻辑架构设计" class="headerlink" title="2. 逻辑架构设计"></a>2. 逻辑架构设计</h2><p>架构核心原则：<strong>上游身份提供商 (Upstream IdP) 负责身份真实性，内部身份系统负责准入判定与属性分发。</strong></p><h3 id="2-1-架构全景图"><a href="#2-1-架构全景图" class="headerlink" title="2.1 架构全景图"></a>2.1 架构全景图</h3><pre class="mermaid">graph TB    subgraph U["用户层"]        EU["企业员工"]        AD["系统管理员"]    end    subgraph A["外部身份源 (Upstream IdP)"]        IDP["OIDC Provider"]    end    subgraph I["身份治理层 (Internal IAM)"]        IAS["Identity Access Service (IAS) - 准入决策引擎 + 会话管理"]        IMA["Identity Management API (IMA) - 读写管理后台"]        IDS["Identity Data Service (IDS) - 身份画像查询 (只读)"]        ISJ["Identity Sync Job (ISJ) - 镜像数据同步"]    end    subgraph G["API 网关层"]        GW["API Gateway - 身份透传 + 统一鉴权"]    end    subgraph B["业务系统层"]        APP1["业务系统 A"]        APP2["业务系统 B"]        APP3["业务系统 C"]    end    EU -->|OIDC SSO| IDP    EU -.->|OIDC SSO| IAS    IDP -->|OIDC Federation| IAS    AD -->|Email/Password| IAS    IAS -->|签发内部标准 JWT| GW    GW -->|Header 注入 X-User-Id| APP1    GW -->|Header 注入| APP2    GW -->|Header 注入| APP3    APP1 -->|查询用户画像| IDS    APP2 -->|查询用户画像| IDS    APP3 -->|查询用户画像| IDS    IMA -.->|配置分发| IAS    ISJ -->|增量同步| IDP</pre><h3 id="2-2-交互流分析-Identity-Flows"><a href="#2-2-交互流分析-Identity-Flows" class="headerlink" title="2.2 交互流分析 (Identity Flows)"></a>2.2 交互流分析 (Identity Flows)</h3><h4 id="A-联合登录流程-OIDC-Federation"><a href="#A-联合登录流程-OIDC-Federation" class="headerlink" title="A. 联合登录流程 (OIDC Federation)"></a>A. 联合登录流程 (OIDC Federation)</h4><p>此流程实现了基于标准 OAuth2 协议的身份中继，支持任意标准 OIDC Provider 的无缝接入：</p><pre class="mermaid">sequenceDiagram    participant Browser as 用户浏览器    participant GW as API Gateway    participant IAS as Identity Access Service (IAS)    participant IdP as Upstream IdP    Browser->>GW: 访问受限系统 URL    GW->>IAS: Forward-Auth (JWT 校验)    IAS-->>GW: 未认证 (302 Redirect)    GW-->>Browser: 302 → 进入登录页    Browser->>IAS: 选择 "企业账号登录"    IAS-->>Browser: 302 → 跳转至上游 IdP    Browser->>IdP: 提交身份断言 (MFA/SSO)    IdP-->>Browser: 回调 IAS (Authorization Code)    Browser->>IAS: /oauth2/callback?code=xxxx    IAS->>IdP: 用 Code 换取 Token & UserInfo    IAS->>IAS: 内部身份汇聚 (Ext ID → UID)    IAS->>IAS: 创建 Session (SID)    IAS-->>Browser: 签发内部 JWT (含 UID, SID) + 写 Auth Cookie    Browser->>GW: 带 JWT 重新访问    GW->>IAS: Forward-Auth 判定通过    GW->>Browser: 转发至下游业务系统</pre><h4 id="B-透明鉴权与准入流-Decision-Engine"><a href="#B-透明鉴权与准入流-Decision-Engine" class="headerlink" title="B. 透明鉴权与准入流 (Decision Engine)"></a>B. 透明鉴权与准入流 (Decision Engine)</h4><p>网关层通过准入引擎执行实时的精细化管控：</p><pre class="mermaid">sequenceDiagram    participant Browser as 用户浏览器    participant GW as API Gateway    participant IAS as Identity Access Service (IAS)    participant Backend as 业务系统    Browser->>GW: 带 JWT 访问业务 API    GW->>IAS: Forward-Auth 调用    IAS->>IAS: 1. JWT 签名校验    IAS->>IAS: 2. 权限版本校验 (Permission Consistency)    IAS->>IAS: 3. 活跃会话校验 (Check SID in Cache)    IAS->>IAS: 4. 应用级准入判定 (Access Level Check)    alt 准入成功        IAS-->>GW: 200 OK + Header (X-User-Id, X-Sid)        GW->>Backend: 映射身份上下文请求业务        Backend-->>Browser: 返回数据    else 准入拒绝        IAS-->>GW: 403 Forbidden        GW-->>Browser: 403 Forbidden    end</pre><hr><h2 id="3-关键设计决策-Rationale"><a href="#3-关键设计决策-Rationale" class="headerlink" title="3. 关键设计决策 (Rationale)"></a>3. 关键设计决策 (Rationale)</h2><h3 id="3-1-准入控制与业务权限的「解耦边界」"><a href="#3-1-准入控制与业务权限的「解耦边界」" class="headerlink" title="3.1 准入控制与业务权限的「解耦边界」"></a>3.1 准入控制与业务权限的「解耦边界」</h3><ul><li><strong>准入控制（Access Control）</strong>：由身份系统决定该用户是否有权进入此应用。</li><li><strong>功能权限（Feature Permission）</strong>：应用内部通过 IAM 提供的属性自行判定。</li></ul><p><strong>决策依据</strong>：在本架构设计中，身份系统作为 <strong>Attribute Provider（属性提供者）</strong>，而让业务系统保持 <strong>Rule Definer（规则定义者）</strong> 的角色，以防止中央身份系统过度侵入业务细节。</p><h3 id="3-2-选型思路：协议中立性"><a href="#3-2-选型思路：协议中立性" class="headerlink" title="3.2 选型思路：协议中立性"></a>3.2 选型思路：协议中立性</h3><p>架构选择 <strong>Spring Authorization Server</strong> 构建 IAS 核心：</p><ul><li><strong>优势</strong>：其纯净的 OAuth2 实现支持将任何第三方 Identity Provider 抽象为「身份源」。</li><li><strong>横向对比</strong>：相较于 Keycloak，它提供了更灵活的 API 级扩展，便于插入自定义的「准入决策链」。</li></ul><h3 id="3-3-无状态与有状态的平衡-SID-设计"><a href="#3-3-无状态与有状态的平衡-SID-设计" class="headerlink" title="3.3 无状态与有状态的平衡 (SID 设计)"></a>3.3 无状态与有状态的平衡 (SID 设计)</h3><p>为支持「即时撤回权限」和「全局注销」：</p><ul><li><strong>设计点</strong>：在 JWT 中嵌入 Session ID (SID)，由后端 Redis 集群维护。</li><li><strong>决策依据</strong>：OAuth2 协议本身不包含会话失效语义，此处通过 SID 机制补充了实时会话注销能力，以此满足企业级的管理精度需求。</li></ul><h3 id="3-4-权限即时生效：版本一致性校验"><a href="#3-4-权限即时生效：版本一致性校验" class="headerlink" title="3.4 权限即时生效：版本一致性校验"></a>3.4 权限即时生效：版本一致性校验</h3><p>为了实现变更的「分钟级响应」：</p><ol><li>用户与权限的映射持有 <code>permission_version</code>。</li><li>网关准入时进行版本匹配校验。</li><li>一旦管理员修改准入策略，版本号递增，促使所有活跃 Token 即刻失效，从而触发重新鉴权逻辑。</li></ol><hr><h2 id="4-身份汇聚：UID-驱动的统一画像"><a href="#4-身份汇聚：UID-驱动的统一画像" class="headerlink" title="4. 身份汇聚：UID 驱动的统一画像"></a>4. 身份汇聚：UID 驱动的统一画像</h2><p>架构通过「身份联合转换」将异构账户标识转换为全局唯一的 <code>UID</code>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────┐</span><br><span class="line">│            身份联合映射关系               │</span><br><span class="line">├──────────┬─────────────┬─────────────┤</span><br><span class="line">│  UID     │ External ID │ Identity Type│</span><br><span class="line">├──────────┼─────────────┼─────────────┤</span><br><span class="line">│    100   │ aaa111bbb.. │ OIDC_SUB/OID │  ← 主力联合身份</span><br><span class="line">│    100   │ user@domain │ EMAIL_PWD    │  ← 容灾/备用身份</span><br><span class="line">└──────────┴─────────────┴─────────────┘</span><br></pre></td></tr></table></figure><p>设计的核心在于实现 IDP-Agnostic（身份源无关），确保上游账号的变更（如企业更名导致的 Email 域名变更）不会影响业务系统中的数据关联。</p><hr><h2 id="5-协作边界规范"><a href="#5-协作边界规范" class="headerlink" title="5. 协作边界规范"></a>5. 协作边界规范</h2><p>通过标准化协议明确身份治理系统与上游 IdP 的协作：</p><table><thead><tr><th>职责方</th><th>负责的核心能力</th></tr></thead><tbody><tr><td><strong>上游 IdP</strong></td><td>身份断言 (Assertion)、MFA&#x2F;SSO、生命周期同步 (HR 系统集成)</td></tr><tr><td><strong>内部治理层</strong></td><td>准入决策、会话一致性管理、企业画像补全、全局操作审计</td></tr></tbody></table><hr><h2 id="6-实践小结"><a href="#6-实践小结" class="headerlink" title="6. 实践小结"></a>6. 实践小结</h2><p>本架构旨在建立一套协议中立、职责清晰的身份治理桥梁：</p><ol><li><strong>协议中立</strong>：基于标准协议，不绑定特定供应商，可对接任意 OIDC Provider。</li><li><strong>治理一致</strong>：通过网关注入实现身份的全局可信分发。</li><li><strong>管理可控</strong>：通过 SID 和版本机制，将无状态协议的灵活性与有状态管理的安全性结合。</li></ol>]]></content>
    
    
    <summary type="html">基于标准 OAuth2/OIDC 协议构建企业级身份中继架构，实现跨系统的统一准入决策与身份画像汇聚，覆盖联合登录流程、透明鉴权流、SID 会话管理与权限即时生效机制。</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="oidc" scheme="https://www.yzhu.name/tags/oidc/"/>
    
    <category term="identity-management" scheme="https://www.yzhu.name/tags/identity-management/"/>
    
    <category term="enterprise-architecture" scheme="https://www.yzhu.name/tags/enterprise-architecture/"/>
    
    <category term="sso" scheme="https://www.yzhu.name/tags/sso/"/>
    
  </entry>
  
  <entry>
    <title>OAuth2/OIDC 会话超时的边界：前端 Token、Refresh Token 与 SSO Session</title>
    <link href="https://www.yzhu.name/2026/03/17/oauth2-oidc-session-timeout-boundaries/"/>
    <id>https://www.yzhu.name/2026/03/17/oauth2-oidc-session-timeout-boundaries/</id>
    <published>2026-03-17T13:07:47.000Z</published>
    <updated>2026-03-17T13:22:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 语言</strong>: <strong>中文版</strong> | <a href="/2026/03/17/oauth2-oidc-session-timeout-boundaries-en/">English Version</a></p></blockquote><p>在一次排查会话超时问题时，我们遇到了一个很典型的现象：前端已经提示会话失效，但用户重新进入授权流程后，并没有再次看到登录页，而是直接回到了系统。前后端最开始对这个现象的解释完全对不上，后来把链路拆开看，才发现问题不在某一个配置值，而在于大家说的“登录态”本来就不是同一层东西。<span id="more"></span></p><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>这篇文章默认读者已经熟悉 OAuth2&#x2F;OIDC 的基本流程，重点放在生产环境里最容易混淆的超时边界。</p><p>OAuth2&#x2F;OIDC 里的“登录态”通常不是一个时间，而是几类时间共同作用：</p><ul><li><code>authorization code</code> 决定授权码多久失效</li><li><code>access token</code> 决定当前令牌还能调用 API 多久</li><li><code>refresh token</code> 决定应用还能静默续期多久</li><li>SSO 浏览器会话决定再次进入授权流程时是否还需要输入密码</li><li>服务端会话标识决定已签发令牌能否被主动撤销</li></ul><p>排障时，把这几层拆开看，比只围绕某一个 TTL 反复讨论更有效。</p><h2 id="一、问题通常出在哪里"><a href="#一、问题通常出在哪里" class="headerlink" title="一、问题通常出在哪里"></a>一、问题通常出在哪里</h2><p>当时看到的现象很具体：</p><ol><li>用户正在使用前端应用</li><li>一段时间后，前端提示会话过期</li><li>用户点击重新登录</li><li>浏览器跳转到授权服务器后，没有出现登录页</li><li>授权完成后，用户又直接回到了系统</li></ol><p>这个现象通常说明两件事同时成立：</p><ul><li>前端本地持有的 <code>access token</code> 已经过期，或者已经被前端判定为即将过期</li><li>授权服务器的浏览器会话仍然有效，所以授权服务器还认得这个用户</li></ul><p>所以，应用提示的“需要重新登录”，很多时候只是“需要重新获取一组 token”，并不等于“用户必须重新输入账号密码”。</p><p>这个区别如果不先说清楚，后面的排障很容易跑偏。</p><h2 id="二、先分清协议层和工程层"><a href="#二、先分清协议层和工程层" class="headerlink" title="二、先分清协议层和工程层"></a>二、先分清协议层和工程层</h2><p>在标准的 OAuth2&#x2F;OIDC 授权码流程里，核心角色只有四个：</p><ul><li>用户</li><li>客户端应用</li><li>授权服务器</li><li>资源服务器</li></ul><p>一个简化流程如下：</p><pre class="mermaid">sequenceDiagram    participant User as User    participant Client as Client App    participant AS as Authorization Server    participant RS as Resource Server    User->>Client: 打开应用    Client->>AS: 跳转 /authorize    AS->>User: 登录并确认授权    AS-->>Client: 回调 redirect_uri?code=...    Client->>AS: 用 code 换 token    AS-->>Client: 返回 access token / refresh token    Client->>RS: 携带 access token 调用 API    RS-->>Client: 返回业务数据</pre><p>协议层主要定义的是：</p><ul><li>如何发起授权</li><li>如何交换 token</li><li>如何校验 token</li></ul><p>但到了生产环境，系统通常还会叠加一些工程实现：</p><ul><li>API Gateway 统一做 token 校验</li><li>授权服务器用浏览器 session 保存登录记忆</li><li>服务端引入会话标识，支持 logout 后令牌立即失效</li><li>前端为了规避边界抖动，会提前几十秒把 token 判定为过期</li></ul><p>这些机制都很常见，但它们并不属于同一个层次。把协议概念和工程策略混在一起讨论，会直接影响对“登录态”的判断。</p><h2 id="三、最开始我们是怎么误判的"><a href="#三、最开始我们是怎么误判的" class="headerlink" title="三、最开始我们是怎么误判的"></a>三、最开始我们是怎么误判的</h2><p>这类问题在现场里很容易先走到一个错误方向：先去改 <code>access token</code> 的 TTL。</p><p>原因也不复杂。前端提示会话失效，最直觉的理解就是 token 太短了；而且从现象上看，改长一点似乎就能缓解问题。</p><p>但这条思路通常只能解释一部分表象，解释不了下面这些情况：</p><ul><li>为什么 token 过期后，重新授权时不需要再次输入密码</li><li>为什么有些请求会先收到 <code>401</code>，但页面并没有真正退出</li><li>为什么 logout 之后，某些旧 token 还能短时间继续访问</li></ul><p>也就是说，单改 <code>access token</code> TTL 最多只能影响“前端多快感知失效”，并不能覆盖“是否还能静默续期”“是否还要重新输密码”“旧 token 能不能立刻失效”这些问题。</p><p>把这几个边界拆开之后，后面的现象才会变得一致。</p><h2 id="四、一个更实用的心智模型：三层门票"><a href="#四、一个更实用的心智模型：三层门票" class="headerlink" title="四、一个更实用的心智模型：三层门票"></a>四、一个更实用的心智模型：三层门票</h2><p>把登录态理解成三层门票，排障会简单很多。</p><h3 id="1-access-token"><a href="#1-access-token" class="headerlink" title="1. access token"></a>1. <code>access token</code></h3><p>这是访问资源服务器时直接带上的门票。</p><ul><li>生命周期通常较短</li><li>暴露面最大</li><li>过期后最直接的表现是 API 返回 <code>401</code></li></ul><h3 id="2-refresh-token"><a href="#2-refresh-token" class="headerlink" title="2. refresh token"></a>2. <code>refresh token</code></h3><p>这是应用用来换新 <code>access token</code> 的门票。</p><ul><li>生命周期通常比 access token 长</li><li>不应该高频出现在业务请求里</li><li>它决定应用还能不能无感续期</li></ul><h3 id="3-SSO-会话"><a href="#3-SSO-会话" class="headerlink" title="3. SSO 会话"></a>3. SSO 会话</h3><p>这一层通常又分成两部分：</p><ul><li>浏览器侧的登录会话，比如 <code>JSESSIONID</code> 这类 cookie</li><li>服务端侧的会话标识，用于撤销、踢出、单点退出等治理能力</li></ul><p>这两部分经常同时出现，但职责并不相同：</p><ul><li>浏览器会话决定再次进入 <code>/authorize</code> 时是否还需要输入密码</li><li>服务端会话标识决定已签发的 token 是否还能被继续接受</li></ul><h2 id="五、几个最容易混淆的时间参数"><a href="#五、几个最容易混淆的时间参数" class="headerlink" title="五、几个最容易混淆的时间参数"></a>五、几个最容易混淆的时间参数</h2><table><thead><tr><th>参数</th><th>所属层级</th><th>过期后的表现</th><th>常见建议</th></tr></thead><tbody><tr><td><code>authorization_code</code> TTL</td><td>授权码</td><td>用 code 换 token 失败</td><td>短，通常 5 分钟左右</td></tr><tr><td><code>access_token</code> TTL</td><td>API 访问令牌</td><td>调 API 失败或前端判定失效</td><td>短，通常 10 到 15 分钟</td></tr><tr><td><code>refresh_token</code> TTL</td><td>续期令牌</td><td>无法静默续期，需要重新授权</td><td>中长，按应用风险分层</td></tr><tr><td>SSO 浏览器会话超时</td><td>授权服务器登录记忆</td><td>再次授权时重新看到登录页</td><td>与平台会话策略保持一致</td></tr><tr><td>服务端会话标识 TTL</td><td>撤销与登出治理</td><td>旧 token 是否还能被立即拦截</td><td>与会话治理策略保持一致</td></tr><tr><td>前端 <code>skewSeconds</code></td><td>本地提前过期判断</td><td>token 会被提前一点判定失效</td><td>保持很小，例如 30 秒</td></tr></tbody></table><h3 id="5-1-authorization-code"><a href="#5-1-authorization-code" class="headerlink" title="5.1 authorization code"></a>5.1 <code>authorization code</code></h3><p>授权码是一次性的短期凭证，主要用于前端回调后向授权服务器换取 token。它的职责很单一，通常不参与“长会话”讨论。</p><p>把这个时间调长，通常既不能改善用户体验，也会扩大暴露窗口。</p><h3 id="5-2-access-token"><a href="#5-2-access-token" class="headerlink" title="5.2 access token"></a>5.2 <code>access token</code></h3><p>这是用户体感最明显的一层。</p><ul><li>时间太短，前端容易频繁出现未登录、接口重试、边界抖动</li><li>时间太长，泄露后的风险窗口会明显扩大</li></ul><p>对大多数后台系统来说，<code>10-15 分钟</code> 往往比几分钟更平衡。</p><h3 id="5-3-refresh-token"><a href="#5-3-refresh-token" class="headerlink" title="5.3 refresh token"></a>5.3 <code>refresh token</code></h3><p>很多“7 天免登录”“30 天持续可用”的诉求，本质上应该由 <code>refresh token</code> 来承担，而不是把 <code>access token</code> 直接拉长到几天。</p><p>短 access token 配合较长 refresh token，一般比长期 access token 更稳。</p><h3 id="5-4-SSO-浏览器会话"><a href="#5-4-SSO-浏览器会话" class="headerlink" title="5.4 SSO 浏览器会话"></a>5.4 SSO 浏览器会话</h3><p>这一层决定的是：用户再次走授权流程时，授权服务器是否还记得这个浏览器已经登录过。</p><p>所以会出现这样的情况：</p><ul><li>应用本地 token 已经过期</li><li>用户重新发起授权</li><li>授权服务器检查到浏览器会话还有效</li><li>于是直接签发新的授权结果，用户无需再次输入密码</li></ul><p>这不是绕过认证，而是 SSO 的正常体验。</p><h3 id="5-5-服务端会话标识"><a href="#5-5-服务端会话标识" class="headerlink" title="5.5 服务端会话标识"></a>5.5 服务端会话标识</h3><p>很多生产系统会在 token 之外，再维护一个服务端会话标识，比如 <code>sid</code> 之类的 claim 或会话键。它的目的不是替代 token，而是补足“主动撤销”能力。</p><p>典型用途包括：</p><ul><li>用户主动退出后，旧 token 立刻失效</li><li>管理员踢出某个会话</li><li>密码修改后强制旧会话下线</li><li>同浏览器多标签页共享一套失效状态</li></ul><p>没有这层机制时，系统往往只能等 token 自然过期。</p><p>如果只讲概念，这一层还是容易显得有点虚。用一个最小实现示意会更直接：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 签发 token 时，把 sid 一起写入 claim</span></span><br><span class="line"><span class="keyword">const</span> sid = <span class="title function_">randomSessionId</span>();</span><br><span class="line"><span class="keyword">await</span> redis.<span class="title function_">set</span>(<span class="string">`session:sid:<span class="subst">$&#123;sid&#125;</span>`</span>, <span class="string">&quot;1&quot;</span>, &#123; <span class="attr">EX</span>: <span class="number">43200</span> &#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> accessToken = <span class="title function_">signJwt</span>(&#123;</span><br><span class="line">  <span class="attr">sub</span>: userId,</span><br><span class="line">  sid,</span><br><span class="line">  <span class="attr">exp</span>: now + <span class="number">900</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 校验 token 时，同时检查 sid 是否还存在</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">validateAccessToken</span>(<span class="params">token</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> payload = <span class="title function_">verifyJwt</span>(token);</span><br><span class="line">  <span class="keyword">const</span> exists = <span class="keyword">await</span> redis.<span class="title function_">exists</span>(<span class="string">`session:sid:<span class="subst">$&#123;payload.sid&#125;</span>`</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!exists) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;session revoked&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> payload;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// logout 或踢人时，删除 sid</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">revokeSession</span>(<span class="params">sid</span>) &#123;</span><br><span class="line">  <span class="keyword">await</span> redis.<span class="title function_">del</span>(<span class="string">`session:sid:<span class="subst">$&#123;sid&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个做法的重点不在 Redis 本身，而在于给“主动撤销”加了一个服务端抓手。只靠 JWT 的 <code>exp</code>，系统通常只能等它自然过期。</p><h2 id="六、为什么前端和后端经常对不上"><a href="#六、为什么前端和后端经常对不上" class="headerlink" title="六、为什么前端和后端经常对不上"></a>六、为什么前端和后端经常对不上</h2><p>登录态问题之所以容易争论，是因为不同角色观察到的是不同层：</p><ul><li>前端关注本地 token 是否还可用</li><li>网关或资源服务器关注 bearer token 是否能通过校验</li><li>授权服务器关注浏览器会话是否仍然存在</li><li>安全治理关注旧会话是否能被主动撤销</li></ul><p>如果这几层没有分开描述，团队内部就很容易出现两种看起来互相矛盾、但各自都没错的说法：</p><ul><li>“明明过期了，为什么又直接进去了？”</li><li>“明明已经退出了，为什么旧 token 还能再调一次接口？”</li></ul><p>前一句通常在说 SSO 浏览器会话仍有效，后一句通常在说服务端撤销链路没有兜住。</p><h2 id="七、长会话不是只改一个-TTL"><a href="#七、长会话不是只改一个-TTL" class="headerlink" title="七、长会话不是只改一个 TTL"></a>七、长会话不是只改一个 TTL</h2><p>如果某个应用希望做到“几天内持续可用”，通常至少要同时满足下面几件事。</p><h3 id="7-1-前端真的接入了-refresh-token-续期"><a href="#7-1-前端真的接入了-refresh-token-续期" class="headerlink" title="7.1 前端真的接入了 refresh token 续期"></a>7.1 前端真的接入了 refresh token 续期</h3><p>如果 access token 一过期，前端就直接清状态并跳回登录页，那么即使 refresh token 配到了 7 天，也不会改善体验。</p><h3 id="7-2-退出登录时，服务端也能撤销会话"><a href="#7-2-退出登录时，服务端也能撤销会话" class="headerlink" title="7.2 退出登录时，服务端也能撤销会话"></a>7.2 退出登录时，服务端也能撤销会话</h3><p>仅清理前端本地存储并不够。只要旧 token 还在有效期内，就可能继续被网关或资源服务器接受。长会话场景里，这一点尤其重要。</p><h3 id="7-3-敏感事件要支持强制失效"><a href="#7-3-敏感事件要支持强制失效" class="headerlink" title="7.3 敏感事件要支持强制失效"></a>7.3 敏感事件要支持强制失效</h3><p>例如：</p><ul><li>修改密码</li><li>用户被禁用</li><li>权限发生重大变更</li><li>管理员踢出会话</li></ul><p>这些场景不应该单纯依赖自然过期。</p><h3 id="7-4-长期-refresh-token-最好采用轮换策略"><a href="#7-4-长期-refresh-token-最好采用轮换策略" class="headerlink" title="7.4 长期 refresh token 最好采用轮换策略"></a>7.4 长期 refresh token 最好采用轮换策略</h3><p>如果 refresh token 可以反复复用直到自然过期，一旦泄露，风险窗口会很长。</p><p>轮换策略的思路是：每次刷新后，让旧 refresh token 立即失效，并签发新的 refresh token。这样更适合长会话场景。</p><h2 id="八、一个更稳的时间配置思路"><a href="#八、一个更稳的时间配置思路" class="headerlink" title="八、一个更稳的时间配置思路"></a>八、一个更稳的时间配置思路</h2><p>下面是一组比较常见的参考分层：</p><table><thead><tr><th>场景</th><th><code>access token</code></th><th><code>refresh token</code></th><th>浏览器会话</th></tr></thead><tbody><tr><td>普通后台系统</td><td><code>10-15m</code></td><td><code>8-12h</code></td><td>半天或一个工作日</td></tr><tr><td>低风险长会话应用</td><td><code>10-15m</code></td><td><code>7d</code></td><td>按登录策略评估</td></tr><tr><td>极少数白名单应用</td><td><code>10-15m</code></td><td><code>30d</code></td><td>必须配套撤销与风控</td></tr></tbody></table><p>这里有两个重点：</p><ul><li>长体验优先通过 <code>refresh token</code> 实现</li><li><code>access token</code> 仍然保持短期</li></ul><p>如果直接把 <code>access token</code> 拉到数小时、数天，泄露后的影响面会明显变大，而且很多系统本身并没有配套的即时撤销能力。</p><h2 id="九、排障时按什么顺序看"><a href="#九、排障时按什么顺序看" class="headerlink" title="九、排障时按什么顺序看"></a>九、排障时按什么顺序看</h2><p>碰到“重新登录”“自动续期失败”“退出后仍可访问”这类问题时，按下面顺序看通常更高效：</p><ol><li>先看 <code>access token</code> 的 <code>exp</code></li><li>再看前端是否真的执行了 refresh token 续期</li><li>再确认 <code>refresh token</code> 本身是否还有效，是否采用轮换</li><li>再看授权服务器的浏览器会话是否仍然存在</li><li>最后看服务端撤销标识是否仍有效，logout 链路是否真正删除了它</li></ol><p>这个顺序能快速区分：</p><ul><li>是 API 门票过期了</li><li>是续期链路没接好</li><li>还是 SSO 会话还活着</li><li>或者撤销治理没做完整</li></ul><h2 id="十、一个足够实用的判断公式"><a href="#十、一个足够实用的判断公式" class="headerlink" title="十、一个足够实用的判断公式"></a>十、一个足够实用的判断公式</h2><p>如果只保留一句话，可以直接记这组边界：</p><ul><li>是否还能调用 API，看 <code>access token</code></li><li>是否还能静默续期，看 <code>refresh token</code></li><li>是否还需要重新输入密码，看 SSO 浏览器会话</li><li>是否能让旧 token 立刻失效，看服务端会话撤销机制</li></ul><p>这几个边界理顺之后，很多看起来矛盾的现象都会变得非常直接。</p><h2 id="十一、结语"><a href="#十一、结语" class="headerlink" title="十一、结语"></a>十一、结语</h2><p>OAuth2&#x2F;OIDC 本身并不复杂，真正容易混乱的是生产系统把 token、浏览器会话、网关校验、服务端撤销叠在了一起。把这些机制统称为一个“登录态”去讨论，结论很容易跑偏。</p><p>更稳的做法通常是：</p><ul><li>用短期 <code>access token</code> 控制 API 访问风险</li><li>用 <code>refresh token</code> 提供持续体验</li><li>用浏览器会话承载 SSO 登录记忆</li><li>用服务端撤销机制处理 logout、踢人和强制下线</li></ul><p>这几层边界清楚之后，前端、后端和安全侧在讨论超时策略时会轻松很多。</p><h2 id="延伸阅读"><a href="#延伸阅读" class="headerlink" title="延伸阅读"></a>延伸阅读</h2><ul><li><a href="https://datatracker.ietf.org/doc/html/rfc6749">RFC 6749: The OAuth 2.0 Authorization Framework</a></li><li><a href="https://openid.net/specs/openid-connect-core-1_0.html">OpenID Connect Core 1.0</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc9700">RFC 9700: Best Current Practice for OAuth 2.0 Security</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc7636">RFC 7636: Proof Key for Code Exchange by OAuth Public Clients</a></li></ul>]]></content>
    
    
    <summary type="html">梳理 OAuth2/OIDC 中 access token、refresh token、授权码、浏览器会话与服务端会话撤销之间的边界，说明前后端常见超时现象与推荐的时间配置思路。</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="oidc" scheme="https://www.yzhu.name/tags/oidc/"/>
    
    <category term="sso" scheme="https://www.yzhu.name/tags/sso/"/>
    
    <category term="session" scheme="https://www.yzhu.name/tags/session/"/>
    
    <category term="token" scheme="https://www.yzhu.name/tags/token/"/>
    
  </entry>
  
  <entry>
    <title>OAuth2/OIDC Session Timeout Boundaries: Frontend Tokens, Refresh Tokens, and SSO Sessions</title>
    <link href="https://www.yzhu.name/2026/03/17/oauth2-oidc-session-timeout-boundaries-en/"/>
    <id>https://www.yzhu.name/2026/03/17/oauth2-oidc-session-timeout-boundaries-en/</id>
    <published>2026-03-17T13:07:47.000Z</published>
    <updated>2026-03-17T13:22:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/03/17/oauth2-oidc-session-timeout-boundaries/">中文版</a></p></blockquote><p>While troubleshooting a session timeout issue, we encountered a typical phenomenon: the frontend had already indicated session invalidation, but when users re-entered the authorization flow, they didn’t see the login page again and were directly returned to the system. Initially, the frontend and backend explanations were completely misaligned. Later, when we broke down the chain, we realized the problem wasn’t with a single configuration value, but that what everyone called “login state” wasn’t the same layer at all.<span id="more"></span></p><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>This article assumes readers are already familiar with basic OAuth2&#x2F;OIDC flows, focusing on the timeout boundaries most prone to confusion in production environments.</p><p>The “login state” in OAuth2&#x2F;OIDC typically isn’t a single time, but rather several types of times working together:</p><ul><li><code>authorization code</code> determines how long the authorization code remains valid</li><li><code>access token</code> determines how long the current token can still invoke APIs</li><li><code>refresh token</code> determines how long the application can silently renew</li><li>SSO browser session determines whether password re-entry is required when re-entering the authorization flow</li><li>Server-side session identifier determines whether issued tokens can be actively revoked</li></ul><p>When troubleshooting, examining these layers separately is more effective than repeatedly discussing a single TTL.</p><h2 id="I-Where-Problems-Usually-Occur"><a href="#I-Where-Problems-Usually-Occur" class="headerlink" title="I. Where Problems Usually Occur"></a>I. Where Problems Usually Occur</h2><p>The phenomenon we observed was quite specific:</p><ol><li>User is actively using the frontend application</li><li>After some time, the frontend prompts that the session has expired</li><li>User clicks to re-login</li><li>Browser redirects to the authorization server, but the login page doesn’t appear</li><li>After authorization completes, the user is directly returned to the system</li></ol><p>This phenomenon typically indicates that both of the following are true:</p><ul><li>The <code>access token</code> held locally by the frontend has expired, or has been determined by the frontend to be about to expire</li><li>The authorization server’s browser session is still valid, so the authorization server still recognizes this user</li></ul><p>Therefore, the “re-login required” prompt from applications often just means “need to obtain a new set of tokens,” not equivalent to “user must re-enter username and password.”</p><p>If this distinction isn’t clarified first, troubleshooting can easily go astray.</p><h2 id="II-First-Distinguish-Protocol-Layer-from-Engineering-Layer"><a href="#II-First-Distinguish-Protocol-Layer-from-Engineering-Layer" class="headerlink" title="II. First, Distinguish Protocol Layer from Engineering Layer"></a>II. First, Distinguish Protocol Layer from Engineering Layer</h2><p>In the standard OAuth2&#x2F;OIDC authorization code flow, there are only four core roles:</p><ul><li>User</li><li>Client Application</li><li>Authorization Server</li><li>Resource Server</li></ul><p>A simplified flow is as follows:</p><pre class="mermaid">sequenceDiagram    participant User as User    participant Client as Client App    participant AS as Authorization Server    participant RS as Resource Server    User->>Client: Open app    Client->>AS: Redirect to /authorize    AS->>User: Login and authorize    AS-->>Client: Callback redirect_uri?code=...    Client->>AS: Exchange code for token    AS-->>Client: Return access token / refresh token    Client->>RS: Call API with access token    RS-->>Client: Return business data</pre><p>The protocol layer mainly defines:</p><ul><li>How to initiate authorization</li><li>How to exchange tokens</li><li>How to validate tokens</li></ul><p>But in production environments, systems typically overlay some engineering implementations:</p><ul><li>API Gateway uniformly handles token validation</li><li>Authorization servers use browser sessions to maintain login memory</li><li>Server-side introduces session identifiers to support immediate token invalidation after logout</li><li>Frontends avoid boundary jitter by determining tokens as expired tens of seconds early</li></ul><p>These mechanisms are all common, but they don’t belong to the same layer. Mixing protocol concepts with engineering strategies directly affects judgment of “login state.”</p><h2 id="III-How-We-Initially-Misdiagnosed"><a href="#III-How-We-Initially-Misdiagnosed" class="headerlink" title="III. How We Initially Misdiagnosed"></a>III. How We Initially Misdiagnosed</h2><p>Such issues easily lead to a wrong direction first: modifying the <code>access token</code> TTL.</p><p>The reason isn’t complex. When the frontend prompts session invalidation, the most intuitive understanding is that the token is too short; and from the phenomenon, making it longer seems to alleviate the problem.</p><p>But this line of thinking typically only explains part of the表象, not these situations:</p><ul><li>Why no password re-entry is required when re-authorizing after token expiration</li><li>Why some requests first receive <code>401</code>, but the page doesn’t truly exit</li><li>Why after logout, certain old tokens can continue accessing for a short time</li></ul><p>In other words, only modifying <code>access token</code> TTL can at most affect “how quickly the frontend perceives invalidation,” but cannot cover “whether silent renewal is still possible,” “whether password re-entry is still required,” “whether old tokens can be immediately invalidated.”</p><p>After separating these boundaries, subsequent phenomena become consistent.</p><h2 id="IV-A-More-Practical-Mental-Model-Three-Layer-Tickets"><a href="#IV-A-More-Practical-Mental-Model-Three-Layer-Tickets" class="headerlink" title="IV. A More Practical Mental Model: Three-Layer Tickets"></a>IV. A More Practical Mental Model: Three-Layer Tickets</h2><p>Understanding login state as three-layer tickets makes troubleshooting much simpler.</p><h3 id="1-access-token"><a href="#1-access-token" class="headerlink" title="1. access token"></a>1. <code>access token</code></h3><p>This is the ticket directly presented when accessing the resource server.</p><ul><li>Typically has a short lifecycle</li><li>Largest exposure surface</li><li>Direct performance after expiration is API returning <code>401</code></li></ul><h3 id="2-refresh-token"><a href="#2-refresh-token" class="headerlink" title="2. refresh token"></a>2. <code>refresh token</code></h3><p>This is the ticket used by applications to exchange for new <code>access token</code>s.</p><ul><li>Typically has a longer lifecycle than access token</li><li>Should not appear frequently in business requests</li><li>It determines whether the application can still renew silently</li></ul><h3 id="3-SSO-Session"><a href="#3-SSO-Session" class="headerlink" title="3. SSO Session"></a>3. SSO Session</h3><p>This layer is typically divided into two parts:</p><ul><li>Browser-side login session, such as <code>JSESSIONID</code> type cookies</li><li>Server-side session identifier, used for governance capabilities like revocation, kick-out, single sign-out</li></ul><p>These two parts often appear simultaneously, but have different responsibilities:</p><ul><li>Browser session determines whether password re-entry is required when re-entering <code>/authorize</code></li><li>Server-side session identifier determines whether issued tokens can still be accepted</li></ul><h2 id="V-Several-Most-Confusing-Time-Parameters"><a href="#V-Several-Most-Confusing-Time-Parameters" class="headerlink" title="V. Several Most Confusing Time Parameters"></a>V. Several Most Confusing Time Parameters</h2><table><thead><tr><th>Parameter</th><th>Layer</th><th>Performance After Expiration</th><th>Common Recommendations</th></tr></thead><tbody><tr><td><code>authorization_code</code> TTL</td><td>Authorization code</td><td>Fails to exchange code for token</td><td>Short, typically around 5 minutes</td></tr><tr><td><code>access_token</code> TTL</td><td>API access token</td><td>API call failure or frontend determines invalidation</td><td>Short, typically 10 to 15 minutes</td></tr><tr><td><code>refresh_token</code> TTL</td><td>Renewal token</td><td>Cannot renew silently, requires re-authorization</td><td>Medium to long, stratified by application risk</td></tr><tr><td>SSO browser session timeout</td><td>Authorization server login memory</td><td>Login page appears again during re-authorization</td><td>Consistent with platform session policy</td></tr><tr><td>Server-side session identifier TTL</td><td>Revocation and logout governance</td><td>Whether old tokens can be immediately intercepted</td><td>Consistent with session governance policy</td></tr><tr><td>Frontend <code>skewSeconds</code></td><td>Local premature expiration judgment</td><td>Token determined as invalid slightly early</td><td>Keep small, e.g., 30 seconds</td></tr></tbody></table><h3 id="5-1-authorization-code"><a href="#5-1-authorization-code" class="headerlink" title="5.1 authorization code"></a>5.1 <code>authorization code</code></h3><p>The authorization code is a one-time short-term credential, primarily used for the frontend callback to exchange with the authorization server for tokens. Its responsibility is very single and typically doesn’t participate in “long session” discussions.</p><p>Extending this time typically neither improves user experience nor expands the exposure window.</p><h3 id="5-2-access-token"><a href="#5-2-access-token" class="headerlink" title="5.2 access token"></a>5.2 <code>access token</code></h3><p>This is the layer with the most obvious user perception.</p><ul><li>Time too short, frontend easily experiences frequent not-logged-in, API retries, boundary jitter</li><li>Time too long, risk window after leakage significantly expands</li></ul><p>For most backend systems, <code>10-15 minutes</code> is often more balanced than a few minutes.</p><h3 id="5-3-refresh-token"><a href="#5-3-refresh-token" class="headerlink" title="5.3 refresh token"></a>5.3 <code>refresh token</code></h3><p>Many demands like “7 days免登录” or “30 days continuous availability” should essentially be borne by <code>refresh token</code>, rather than directly extending <code>access token</code> to several days.</p><p>Short access token combined with longer refresh token is generally more stable than long-term access token.</p><h3 id="5-4-SSO-Browser-Session"><a href="#5-4-SSO-Browser-Session" class="headerlink" title="5.4 SSO Browser Session"></a>5.4 SSO Browser Session</h3><p>This layer determines: when users go through the authorization flow again, whether the authorization server still remembers that this browser has already logged in.</p><p>Thus this situation occurs:</p><ul><li>Application local token has already expired</li><li>User re-initiates authorization</li><li>Authorization server checks that browser session is still valid</li><li>Thus directly issues new authorization result, user doesn’t need to re-enter password</li></ul><p>This isn’t bypassing authentication, but rather normal SSO experience.</p><h3 id="5-5-Server-side-Session-Identifier"><a href="#5-5-Server-side-Session-Identifier" class="headerlink" title="5.5 Server-side Session Identifier"></a>5.5 Server-side Session Identifier</h3><p>Many production systems maintain a server-side session identifier outside of tokens, such as <code>sid</code> type claims or session keys. Its purpose isn’t to replace tokens, but to supplement “active revocation” capability.</p><p>Typical uses include:</p><ul><li>After user actively exits, old tokens immediately invalidate</li><li>Administrator kicks out a certain session</li><li>After password modification, force old sessions offline</li><li>Same browser multiple tabs share one invalidation state</li></ul><p>Without this layer mechanism, systems typically can only wait for token natural expiration.</p><p>If only discussing concepts, this layer still easily appears somewhat虚. Using a minimal implementation to illustrate is more direct:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// When signing token, write sid into claim together</span></span><br><span class="line"><span class="keyword">const</span> sid = <span class="title function_">randomSessionId</span>();</span><br><span class="line"><span class="keyword">await</span> redis.<span class="title function_">set</span>(<span class="string">`session:sid:<span class="subst">$&#123;sid&#125;</span>`</span>, <span class="string">&quot;1&quot;</span>, &#123; <span class="attr">EX</span>: <span class="number">43200</span> &#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> accessToken = <span class="title function_">signJwt</span>(&#123;</span><br><span class="line">  <span class="attr">sub</span>: userId,</span><br><span class="line">  sid,</span><br><span class="line">  <span class="attr">exp</span>: now + <span class="number">900</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// When validating token, simultaneously check whether sid still exists</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">validateAccessToken</span>(<span class="params">token</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> payload = <span class="title function_">verifyJwt</span>(token);</span><br><span class="line">  <span class="keyword">const</span> exists = <span class="keyword">await</span> redis.<span class="title function_">exists</span>(<span class="string">`session:sid:<span class="subst">$&#123;payload.sid&#125;</span>`</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!exists) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;session revoked&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> payload;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// When logout or kicking people, delete sid</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">revokeSession</span>(<span class="params">sid</span>) &#123;</span><br><span class="line">  <span class="keyword">await</span> redis.<span class="title function_">del</span>(<span class="string">`session:sid:<span class="subst">$&#123;sid&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The focus of this approach isn’t Redis itself, but adding a server-side handle for “active revocation.” Relying only on JWT’s <code>exp</code>, systems typically can only wait for it to naturally expire.</p><h2 id="VI-Why-Frontend-and-Backend-Often-Don’t-Match"><a href="#VI-Why-Frontend-and-Backend-Often-Don’t-Match" class="headerlink" title="VI. Why Frontend and Backend Often Don’t Match"></a>VI. Why Frontend and Backend Often Don’t Match</h2><p>Login state issues are prone to debate because different roles observe different layers:</p><ul><li>Frontend focuses on whether local tokens are still available</li><li>Gateway or resource server focuses on whether bearer token can pass validation</li><li>Authorization server focuses on whether browser session still exists</li><li>Security governance focuses on whether old sessions can be actively revoked</li></ul><p>If these layers aren’t described separately, teams easily develop two seemingly contradictory but individually correct statements:</p><ul><li>“It’s obviously expired, why did it go straight in again?”</li><li>“It’s obviously already exited, why can old tokens still call one more API?”</li></ul><p>The first sentence usually says SSO browser session is still valid, the second sentence usually says server-side revocation chain didn’t catch.</p><h2 id="VII-Long-Sessions-Don’t-Mean-Just-Modifying-One-TTL"><a href="#VII-Long-Sessions-Don’t-Mean-Just-Modifying-One-TTL" class="headerlink" title="VII. Long Sessions Don’t Mean Just Modifying One TTL"></a>VII. Long Sessions Don’t Mean Just Modifying One TTL</h2><p>If an application wants to achieve “continuous availability for several days,” typically at least the following several things need to be simultaneously satisfied.</p><h3 id="7-1-Frontend-Actually-Integrated-Refresh-Token-Renewal"><a href="#7-1-Frontend-Actually-Integrated-Refresh-Token-Renewal" class="headerlink" title="7.1 Frontend Actually Integrated Refresh Token Renewal"></a>7.1 Frontend Actually Integrated Refresh Token Renewal</h3><p>If frontend directly clears state and jumps back to login page once access token expires, then even if refresh token is configured to 7 days, experience won’t improve.</p><h3 id="7-2-When-Logging-Out-Server-side-Can-Also-Revoke-Sessions"><a href="#7-2-When-Logging-Out-Server-side-Can-Also-Revoke-Sessions" class="headerlink" title="7.2 When Logging Out, Server-side Can Also Revoke Sessions"></a>7.2 When Logging Out, Server-side Can Also Revoke Sessions</h3><p>Only clearing frontend local storage isn’t enough. As long as old tokens are within validity period, they may continue to be accepted by gateway or resource server. This is particularly important in long session scenarios.</p><h3 id="7-3-Sensitive-Events-Must-Support-Forced-Invalidation"><a href="#7-3-Sensitive-Events-Must-Support-Forced-Invalidation" class="headerlink" title="7.3 Sensitive Events Must Support Forced Invalidation"></a>7.3 Sensitive Events Must Support Forced Invalidation</h3><p>For example:</p><ul><li>Modify password</li><li>User disabled</li><li>Major permission changes</li><li>Administrator kicks out session</li></ul><p>These scenarios shouldn’t purely rely on natural expiration.</p><h3 id="7-4-Long-term-Refresh-Token-Best-Uses-Rotation-Strategy"><a href="#7-4-Long-term-Refresh-Token-Best-Uses-Rotation-Strategy" class="headerlink" title="7.4 Long-term Refresh Token Best Uses Rotation Strategy"></a>7.4 Long-term Refresh Token Best Uses Rotation Strategy</h3><p>If refresh token can be repeatedly reused until natural expiration, once leaked, risk window will be very long.</p><p>Rotation strategy idea is: after each refresh, immediately invalidate old refresh token, and issue new refresh token. This is more suitable for long session scenarios.</p><h2 id="VIII-A-More-Stable-Time-Configuration-Strategy"><a href="#VIII-A-More-Stable-Time-Configuration-Strategy" class="headerlink" title="VIII. A More Stable Time Configuration Strategy"></a>VIII. A More Stable Time Configuration Strategy</h2><p>Below is a set of common reference stratifications:</p><table><thead><tr><th>Scenario</th><th><code>access token</code></th><th><code>refresh token</code></th><th>Browser Session</th></tr></thead><tbody><tr><td>Ordinary backend system</td><td><code>10-15m</code></td><td><code>8-12h</code></td><td>Half day or one work day</td></tr><tr><td>Low-risk long session application</td><td><code>10-15m</code></td><td><code>7d</code></td><td>Evaluate according to login policy</td></tr><tr><td>Very few whitelist applications</td><td><code>10-15m</code></td><td><code>30d</code></td><td>Must配套 revocation and risk control</td></tr></tbody></table><p>Here are two key points:</p><ul><li>Long experience优先 implemented through <code>refresh token</code></li><li><code>access token</code> still remains short-term</li></ul><p>If directly extending <code>access token</code> to several hours or days, impact surface after leakage significantly expands, and many systems themselves don’t have配套 immediate revocation capability.</p><h2 id="IX-What-Order-to-View-When-Troubleshooting"><a href="#IX-What-Order-to-View-When-Troubleshooting" class="headerlink" title="IX. What Order to View When Troubleshooting"></a>IX. What Order to View When Troubleshooting</h2><p>When encountering problems like “re-login,” “auto-renewal failure,” “still accessible after logout,” viewing in the following order is typically more efficient:</p><ol><li>First look at <code>access token</code>‘s <code>exp</code></li><li>Then look at whether frontend actually executed refresh token renewal</li><li>Then confirm whether <code>refresh token</code> itself is still valid, whether it uses rotation</li><li>Then look at whether authorization server’s browser session still exists</li><li>Finally look at whether server-side revocation identifier is still valid, whether logout chain truly deleted it</li></ol><p>This order can quickly distinguish:</p><ul><li>Is it API ticket expired</li><li>Is it renewal chain not properly connected</li><li>Or is SSO session still alive</li><li>Or revocation governance not complete</li></ul><h2 id="X-A-Sufficiently-Practical-Judgment-Formula"><a href="#X-A-Sufficiently-Practical-Judgment-Formula" class="headerlink" title="X. A Sufficiently Practical Judgment Formula"></a>X. A Sufficiently Practical Judgment Formula</h2><p>If only keeping one sentence, can directly remember this set of boundaries:</p><ul><li>Whether can still call API, look at <code>access token</code></li><li>Whether can still silently renew, look at <code>refresh token</code></li><li>Whether still need to re-enter password, look at SSO browser session</li><li>Whether can make old tokens immediately invalidate, look at server-side session revocation mechanism</li></ul><p>After these boundaries are straightened out, many seemingly contradictory phenomena become very direct.</p><h2 id="XI-Conclusion"><a href="#XI-Conclusion" class="headerlink" title="XI. Conclusion"></a>XI. Conclusion</h2><p>OAuth2&#x2F;OIDC itself isn’t complex, what’s truly confusing is production systems overlaying tokens, browser sessions, gateway validation, server-side revocation. Discussing these mechanisms collectively as one “login state,” conclusions easily go astray.</p><p>A more stable approach is typically:</p><ul><li>Use short-term <code>access token</code> to control API access risk</li><li>Use <code>refresh token</code> to provide continuous experience</li><li>Use browser session to carry SSO login memory</li><li>Use server-side revocation mechanism to handle logout, kick-out, and forced offline</li></ul><p>After these layer boundaries are clear, frontend, backend, and security sides will have much easier discussions when discussing timeout strategies.</p><h2 id="Further-Reading"><a href="#Further-Reading" class="headerlink" title="Further Reading"></a>Further Reading</h2><ul><li><a href="https://datatracker.ietf.org/doc/html/rfc6749">RFC 6749: The OAuth 2.0 Authorization Framework</a></li><li><a href="https://openid.net/specs/openid-connect-core-1_0.html">OpenID Connect Core 1.0</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc9700">RFC 9700: Best Current Practice for OAuth 2.0 Security</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc7636">RFC 7636: Proof Key for Code Exchange by OAuth Public Clients</a></li></ul>]]></content>
    
    
    <summary type="html">Clarifying the boundaries between access tokens, refresh tokens, authorization codes, browser sessions, and server-side session revocation in OAuth2/OIDC, with explanations of common timeout phenomena and recommended time configuration strategies.</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="oidc" scheme="https://www.yzhu.name/tags/oidc/"/>
    
    <category term="sso" scheme="https://www.yzhu.name/tags/sso/"/>
    
    <category term="session" scheme="https://www.yzhu.name/tags/session/"/>
    
    <category term="token" scheme="https://www.yzhu.name/tags/token/"/>
    
  </entry>
  
  <entry>
    <title>APISIX 网关层 OAuth2 重定向端口问题排查</title>
    <link href="https://www.yzhu.name/2026/03/10/apisix-trusted-addresses-oauth2-redirect/"/>
    <id>https://www.yzhu.name/2026/03/10/apisix-trusted-addresses-oauth2-redirect/</id>
    <published>2026-03-10T14:00:00.000Z</published>
    <updated>2026-03-10T14:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 语言</strong>: <strong>中文版</strong> | <a href="/2026/03/10/apisix-trusted-addresses-oauth2-redirect-en/">English Version</a></p></blockquote><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p><strong>问题</strong>: OAuth2 重定向 URL 包含错误端口 <code>http://example.com:9080</code><br><strong>根因</strong>: APISIX <code>trusted_addresses</code> 配置过窄，导致 X-Forwarded-* 头被覆盖<br><strong>解决</strong>: 将前置代理网段加入 <code>trusted_addresses</code>  </p><h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>在一个典型的多层代理架构中：</p><pre class="mermaid">flowchart TD    Browser[浏览器<br>HTTPS:443] --> Nginx[Nginx<br>TLS 终止]    Nginx --> APISIX[APISIX<br>Docker:9080]    APISIX --> OAuth2[Spring OAuth2<br>授权服务器]</pre><p>用户通过 <code>https://public.example.com</code> 访问系统，未登录时访问 OAuth2 授权端点 <code>/oauth2/authorize</code>，理论上应该返回：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">302</span> Found</span><br><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>https://public.example.com/login</span><br></pre></td></tr></table></figure><p>但实际返回的是：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">302</span> Found</span><br><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>http://public.example.com:9080/login</span><br></pre></td></tr></table></figure><p>浏览器访问 <code>https://public.example.com:9080/login</code> 时会报 <code>ERR_CONNECTION_CLOSED</code>，导致用户无法登录。</p><h2 id="问题现象分析"><a href="#问题现象分析" class="headerlink" title="问题现象分析"></a>问题现象分析</h2><h3 id="浏览器网络请求链路"><a href="#浏览器网络请求链路" class="headerlink" title="浏览器网络请求链路"></a>浏览器网络请求链路</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">GET https://public.example.com/oauth2/authorize → 302</span><br><span class="line">GET http://public.example.com:9080/login → 307 Internal Redirect</span><br><span class="line">GET https://public.example.com:9080/login → FAILED</span><br></pre></td></tr></table></figure><h3 id="应用日志关键信息"><a href="#应用日志关键信息" class="headerlink" title="应用日志关键信息"></a>应用日志关键信息</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">requestUrl=http://public.example.com:9080/oauth2/authorize</span><br><span class="line">scheme=http serverName=public.example.com serverPort=9080</span><br><span class="line">Saved request http://public.example.com:9080/oauth2/authorize?...</span><br><span class="line">Redirecting to /login</span><br><span class="line">location=http://public.example.com:9080/login</span><br></pre></td></tr></table></figure><p>从日志可以看出，Spring Security 看到的请求视图已经是错误的：</p><ul><li>scheme 被识别成 <code>http</code></li><li>port 被识别成 <code>9080</code></li><li>SavedRequest 保存的 URL 被污染</li></ul><h2 id="排查方向"><a href="#排查方向" class="headerlink" title="排查方向"></a>排查方向</h2><p>一开始我们怀疑过几个方向：</p><ol><li>Spring Security 的 SavedRequest 逻辑有问题</li><li>APISIX 的 proxy-rewrite 插件配置不正确</li><li>应用没有正确开启 forward-headers-strategy</li><li>入口 Nginx 没把 X-Forwarded-* 头传下来</li></ol><p>这些判断都能解释一部分表象，但都不是根本原因。</p><h3 id="排查时间线"><a href="#排查时间线" class="headerlink" title="排查时间线"></a>排查时间线</h3><table><thead><tr><th>阶段</th><th>假设</th><th>验证方法</th><th>结论</th></tr></thead><tbody><tr><td>1</td><td>Spring Security 问题</td><td>检查 SavedRequest 逻辑</td><td>❌ 排除</td></tr><tr><td>2</td><td>proxy-rewrite 配置</td><td>对比插件配置</td><td>❌ 排除</td></tr><tr><td>3</td><td>forward-headers-strategy</td><td>检查应用配置</td><td>❌ 排除</td></tr><tr><td>4</td><td>前置 Nginx 配置</td><td>检查 X-Forwarded-* 传递</td><td>❌ 排除</td></tr><tr><td>5</td><td>APISIX 日志诊断</td><td>增加诊断日志</td><td>✅ 定位根因</td></tr></tbody></table><h2 id="收敛到-APISIX"><a href="#收敛到-APISIX" class="headerlink" title="收敛到 APISIX"></a>收敛到 APISIX</h2><p>真正的突破来自给 APISIX 增加诊断日志后，发现：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;public.example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;server_port&quot;</span><span class="punctuation">:</span> <span class="string">&quot;9080&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;http_x_forwarded_host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;public.example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;http_x_forwarded_proto&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;http_x_forwarded_port&quot;</span><span class="punctuation">:</span> <span class="string">&quot;9080&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;sent_http_location&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://public.example.com:9080/login&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;upstream_http_location&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://public.example.com:9080/login&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>关键发现：</p><ul><li>错误的 <code>http/9080</code> 已经在 APISIX 里形成了</li><li>不是应用在返回阶段又”改坏了一次”</li><li>应用拿到的错误请求视图，本身就是网关传进去的</li></ul><h2 id="容易混淆的点"><a href="#容易混淆的点" class="headerlink" title="容易混淆的点"></a>容易混淆的点</h2><p>在排查过程中，最容易混淆的是把”用户真实 IP”链路和”公网 origin”链路混在一起。这两件事不是一回事：</p><h3 id="用户真实-IP-链路"><a href="#用户真实-IP-链路" class="headerlink" title="用户真实 IP 链路"></a>用户真实 IP 链路</h3><p>给风控、审计、限流、日志定位使用，通常依赖：</p><ul><li>X-Real-IP</li><li>X-Forwarded-For</li></ul><h3 id="公网-origin-链路"><a href="#公网-origin-链路" class="headerlink" title="公网 origin 链路"></a>公网 origin 链路</h3><p>给 OAuth2 重定向、绝对 URL 生成、回调地址判断使用，通常依赖：</p><ul><li>X-Forwarded-Proto</li><li>X-Forwarded-Host</li><li>X-Forwarded-Port</li></ul><p>这次问题的本质，不是”用户 IP 没透传”，而是 APISIX 在 trust 判断失败后，把第二条链路改坏了。</p><h2 id="APISIX-源码分析"><a href="#APISIX-源码分析" class="headerlink" title="APISIX 源码分析"></a>APISIX 源码分析</h2><p>问题最终收敛到 <code>trusted_addresses</code> 配置。</p><p>APISIX 在处理请求时的逻辑：</p><ol><li>计算上一跳地址（通常通过 X-Real-IP 或 Remote Addr）</li><li>用这个地址去匹配 <code>apisix.trusted_addresses</code></li><li>如果不可信，就覆盖：<ul><li>X-Forwarded-Proto</li><li>X-Forwarded-Host</li><li>X-Forwarded-Port</li></ul></li><li>覆盖值不是外部公网入口，而是 APISIX 自己观察到的：<ul><li>scheme</li><li>host</li><li>server_port</li></ul></li></ol><p>用伪代码表示：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 判断上一跳地址是否在可信列表中</span></span><br><span class="line"><span class="keyword">local</span> addr_is_trusted = trusted_addresses_util.is_trusted(api_ctx.var.realip_remote_addr)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> addr_is_trusted <span class="keyword">then</span></span><br><span class="line">    <span class="comment">-- 不可信时，用网关自己的视图覆盖 X-Forwarded-* 头</span></span><br><span class="line">    <span class="keyword">local</span> proto = api_ctx.var.scheme        <span class="comment">-- 实际值：http</span></span><br><span class="line">    <span class="keyword">local</span> host = api_ctx.var.host            <span class="comment">-- 实际值：public.example.com</span></span><br><span class="line">    <span class="keyword">local</span> port = api_ctx.var.server_port     <span class="comment">-- 实际值：9080</span></span><br><span class="line"></span><br><span class="line">    core.request.set_header(api_ctx, <span class="string">&quot;X-Forwarded-Proto&quot;</span>, proto)</span><br><span class="line">    core.request.set_header(api_ctx, <span class="string">&quot;X-Forwarded-Host&quot;</span>, host)</span><br><span class="line">    core.request.set_header(api_ctx, <span class="string">&quot;X-Forwarded-Port&quot;</span>, port)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>这就解释了为什么应用端不断看到 <code>http</code> 和 <code>9080</code>：</p><ul><li>一旦 APISIX 判定前置代理”不可信”</li><li>就会把外部 origin 信息降级成自己的监听视图</li></ul><p>更多配置细节参考 <a href="https://apisix.apache.org/docs/apisix/conf/config.yml/">APISIX 官方文档 - trusted_addresses</a></p><h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><h3 id="快速验证"><a href="#快速验证" class="headerlink" title="快速验证"></a>快速验证</h3><p>最简单的控制变量实验：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apisix:</span></span><br><span class="line">  <span class="attr">trusted_addresses:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">0.0</span><span class="number">.0</span><span class="number">.0</span><span class="string">/0</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">::/0</span></span><br></pre></td></tr></table></figure><p>重新加载后，验证 OAuth2 授权端点：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>https://public.example.com/login</span><br></pre></td></tr></table></figure><p>问题立即消失。这个实验本身并不是最终配置，但它把根因钉死了：<strong>问题就在 APISIX <code>trusted_addresses</code> 这一层。</strong></p><h3 id="正式配置"><a href="#正式配置" class="headerlink" title="正式配置"></a>正式配置</h3><p>生产环境更合适的做法：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apisix:</span></span><br><span class="line">  <span class="attr">node_listen:</span> <span class="number">9080</span></span><br><span class="line">  <span class="attr">enable_ipv6:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">trusted_addresses:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>           <span class="comment"># 本地回环</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&quot;::1&quot;</span>               <span class="comment"># IPv6 本地回环</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">172.17</span><span class="number">.21</span><span class="number">.0</span><span class="string">/24</span>     <span class="comment"># Docker 网络（如 Docker Desktop）</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">192.168</span><span class="number">.10</span><span class="number">.0</span><span class="string">/24</span>     <span class="comment"># 内网网络（前置代理所在网络）</span></span><br></pre></td></tr></table></figure><p>配合前置 Nginx 配置：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> / &#123;</span><br><span class="line">    <span class="attribute">proxy_pass</span> http://apisix;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-Host <span class="variable">$host</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-Proto <span class="variable">$scheme</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-Port <span class="variable">$server_port</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="为什么应用层补丁不是根解"><a href="#为什么应用层补丁不是根解" class="headerlink" title="为什么应用层补丁不是根解"></a>为什么应用层补丁不是根解</h2><p>排查过程中，一度在应用层做过几种兜底：</p><ul><li>自定义 RequestCache，强行把 SavedRequest 改成公网地址</li><li>自定义登录跳转 URL 生成</li><li>在过滤器里包装 HttpServletRequest，覆盖 scheme&#x2F;host&#x2F;port</li></ul><p>这些手段在短期内确实能缓解症状，但不适合作为正式方案：</p><ol><li>问题不只会出现在一个服务里</li><li>绝对 URL 的生成不只发生在 OAuth2 登录流程</li><li>未来 Swagger、邮件链接、前端 API 基地址、回调地址、下载链接都可能踩到同类问题</li></ol><p>如果公网访问视图在网关层没有统一治理，最终就会变成每个服务各自打补丁。</p><h2 id="开发环境与生产环境的差异"><a href="#开发环境与生产环境的差异" class="headerlink" title="开发环境与生产环境的差异"></a>开发环境与生产环境的差异</h2><p>这次问题还有一个现实背景：开发环境和生产环境的网络拓扑并不一致。</p><p>开发环境常见的链路：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">宿主机 Nginx → Docker Desktop 端口映射 → APISIX 容器</span><br></pre></td></tr></table></figure><p>生产环境更常见的是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Nginx 节点 → 内网 → APISIX 节点</span><br></pre></td></tr></table></figure><p>两种环境里，APISIX 实际看到的”上一跳地址”可能完全不同。</p><p>这会导致一个现象：</p><ul><li>在开发环境中，用很窄的 <code>trusted_addresses</code> 可能根本无法稳定复现生产行为</li><li>因为 APISIX 拿来做 trust 判断的地址对象，可能已经不是预期中的上一跳代理地址</li></ul><p>这也是为什么某些本机环境下，临时把 <code>trusted_addresses</code> 放宽后问题会立刻恢复，而继续猜 CIDR 反而容易陷入死循环。</p><h2 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h2><p>这篇记录适用于类似场景：</p><ul><li>APISIX 部署在反向代理之后</li><li>对外统一域名和 TLS 终止在前置层</li><li>下游应用会基于 scheme&#x2F;host&#x2F;port 生成绝对 URL</li><li>OAuth2 &#x2F; OIDC 登录流程对 URL 视图一致性敏感</li></ul><p>如果链路中已经出现了下面任一现象：</p><ul><li>302 Location 带内部端口</li><li>SavedRequest 里出现内网地址或容器端口</li><li>前端回调或登出地址混入了内网端口</li><li>Mixed Content 指向内网 HTTP 服务</li></ul><p>都值得先检查 APISIX 是否覆盖了 X-Forwarded-*。</p><h2 id="快速检查清单"><a href="#快速检查清单" class="headerlink" title="快速检查清单"></a>快速检查清单</h2><p>遇到类似问题时，按顺序检查：</p><ul><li><input disabled type="checkbox"> APISIX 日志中 <code>server_port</code> 是否为内部端口</li><li><input disabled type="checkbox"> <code>trusted_addresses</code> 是否包含前置代理 IP</li><li><input disabled type="checkbox"> 前置 Nginx 是否正确设置 X-Forwarded-* 头</li><li><input disabled type="checkbox"> 应用是否开启 <code>forward-headers-strategy</code></li><li><input disabled type="checkbox"> OAuth2 重定向 URL 是否使用内部地址</li></ul><p>更多关于 Forwarded Headers 的配置，参考 <a href="https://docs.spring.io/spring-security/reference/servlet/exploits/csrf.html#csrf-foreign-hosts">Spring Security 官方文档</a>。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这个问题本质上不是 OAuth2 协议问题，也不是 Spring Security 的特殊行为。</p><p>它更像是一个典型的网关信任边界问题：</p><ul><li>外部入口是 HTTPS</li><li>网关内部是 HTTP 9080</li><li>如果网关没有稳定保留公网访问视图</li><li>下游服务就会自然把内部端口暴露到绝对 URL 中</li></ul><p>而 APISIX 的 <code>trusted_addresses</code>，正是这条链路里的关键开关。</p><p>最终问题并不是靠单个”神奇配置”解决的，而是靠一轮完整的收敛：</p><ul><li>先承认应用补丁不是根解</li><li>再把问题收敛到网关</li><li>再用源码和控制变量把根因钉死</li><li>最后区分开发环境临时恢复方案和生产环境正式配置原则</li></ul><p>如果运行链路里同时存在前置代理、网关和需要生成绝对 URL 的应用，这类问题值得优先从 trust 边界而不是业务代码开始看。</p><p>这些方案可以帮助你在更复杂的环境中维护 <code>trusted_addresses</code> 的正确性，避免因 IP 变动导致的问题反复出现。</p>]]></content>
    
    
    <summary type="html">记录一次 APISIX 网关在多层代理架构下，OAuth2 重定向 URL 包含错误端口的问题排查过程，以及通过配置 trusted_addresses 解决的完整方案。</summary>
    
    
    
    <category term="API Gateway" scheme="https://www.yzhu.name/categories/API-Gateway/"/>
    
    <category term="OAuth2" scheme="https://www.yzhu.name/categories/API-Gateway/OAuth2/"/>
    
    
    <category term="APISIX" scheme="https://www.yzhu.name/tags/APISIX/"/>
    
    <category term="OAuth2" scheme="https://www.yzhu.name/tags/OAuth2/"/>
    
    <category term="网关，故障排查" scheme="https://www.yzhu.name/tags/%E7%BD%91%E5%85%B3%EF%BC%8C%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    
  </entry>
  
  <entry>
    <title>Troubleshooting APISIX OAuth2 Redirect Port Issue at Gateway Layer</title>
    <link href="https://www.yzhu.name/2026/03/10/apisix-trusted-addresses-oauth2-redirect-en/"/>
    <id>https://www.yzhu.name/2026/03/10/apisix-trusted-addresses-oauth2-redirect-en/</id>
    <published>2026-03-10T14:00:00.000Z</published>
    <updated>2026-03-10T14:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/03/10/apisix-trusted-addresses-oauth2-redirect/">中文版</a></p></blockquote><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p><strong>Problem</strong>: OAuth2 redirect URL contains incorrect port <code>http://example.com:9080</code><br><strong>Root Cause</strong>: APISIX <code>trusted_addresses</code> configuration too restrictive, causing X-Forwarded-* headers to be overwritten<br><strong>Solution</strong>: Add upstream proxy network segments to <code>trusted_addresses</code></p><h2 id="Problem-Background"><a href="#Problem-Background" class="headerlink" title="Problem Background"></a>Problem Background</h2><p>In a typical multi-layer proxy architecture:</p><pre class="mermaid">flowchart TD    Browser[Browser<br>HTTPS:443] --> Nginx[Nginx<br>TLS Termination]    Nginx --> APISIX[APISIX<br>Docker:9080]    APISIX --> OAuth2[Spring OAuth2<br>Authorization Server]</pre><p>Users access the system via <code>https://public.example.com</code>. When not logged in, accessing the OAuth2 authorization endpoint <code>/oauth2/authorize</code> should theoretically return:</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">302</span> Found</span><br><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>https://public.example.com/login</span><br></pre></td></tr></table></figure><p>But the actual response was:</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">302</span> Found</span><br><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>http://public.example.com:9080/login</span><br></pre></td></tr></table></figure><p>When the browser accesses <code>https://public.example.com:9080/login</code>, it throws <code>ERR_CONNECTION_CLOSED</code>, preventing users from logging in.</p><h2 id="Problem-Analysis"><a href="#Problem-Analysis" class="headerlink" title="Problem Analysis"></a>Problem Analysis</h2><h3 id="Browser-Network-Request-Chain"><a href="#Browser-Network-Request-Chain" class="headerlink" title="Browser Network Request Chain"></a>Browser Network Request Chain</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">GET https://public.example.com/oauth2/authorize → 302</span><br><span class="line">GET http://public.example.com:9080/login → 307 Internal Redirect</span><br><span class="line">GET https://public.example.com:9080/login → FAILED</span><br></pre></td></tr></table></figure><h3 id="Key-Application-Log-Information"><a href="#Key-Application-Log-Information" class="headerlink" title="Key Application Log Information"></a>Key Application Log Information</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">requestUrl=http://public.example.com:9080/oauth2/authorize</span><br><span class="line">scheme=http serverName=public.example.com serverPort=9080</span><br><span class="line">Saved request http://public.example.com:9080/oauth2/authorize?...</span><br><span class="line">Redirecting to /login</span><br><span class="line">location=http://public.example.com:9080/login</span><br></pre></td></tr></table></figure><p>From the logs, we can see Spring Security already sees the wrong request view:</p><ul><li>scheme recognized as <code>http</code></li><li>port recognized as <code>9080</code></li><li>SavedRequest URL is corrupted</li></ul><h2 id="Troubleshooting-Directions"><a href="#Troubleshooting-Directions" class="headerlink" title="Troubleshooting Directions"></a>Troubleshooting Directions</h2><p>Initially, we suspected several directions:</p><ol><li>Spring Security’s SavedRequest logic had issues</li><li>APISIX’s proxy-rewrite plugin configuration was incorrect</li><li>Application didn’t properly enable forward-headers-strategy</li><li>Entry Nginx wasn’t passing X-Forwarded-* headers</li></ol><p>These hypotheses could explain some symptoms, but none were the root cause.</p><h3 id="Troubleshooting-Timeline"><a href="#Troubleshooting-Timeline" class="headerlink" title="Troubleshooting Timeline"></a>Troubleshooting Timeline</h3><table><thead><tr><th>Phase</th><th>Hypothesis</th><th>Verification Method</th><th>Conclusion</th></tr></thead><tbody><tr><td>1</td><td>Spring Security issue</td><td>Check SavedRequest logic</td><td>❌ Ruled out</td></tr><tr><td>2</td><td>proxy-rewrite config</td><td>Compare plugin configs</td><td>❌ Ruled out</td></tr><tr><td>3</td><td>forward-headers-strategy</td><td>Check app configuration</td><td>❌ Ruled out</td></tr><tr><td>4</td><td>Upstream Nginx config</td><td>Check X-Forwarded-* passing</td><td>❌ Ruled out</td></tr><tr><td>5</td><td>APISIX log diagnostics</td><td>Add diagnostic logs</td><td>✅ Root cause identified</td></tr></tbody></table><h2 id="Converging-on-APISIX"><a href="#Converging-on-APISIX" class="headerlink" title="Converging on APISIX"></a>Converging on APISIX</h2><p>The real breakthrough came after adding diagnostic logs to APISIX, revealing:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;public.example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;server_port&quot;</span><span class="punctuation">:</span> <span class="string">&quot;9080&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;http_x_forwarded_host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;public.example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;http_x_forwarded_proto&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;http_x_forwarded_port&quot;</span><span class="punctuation">:</span> <span class="string">&quot;9080&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;sent_http_location&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://public.example.com:9080/login&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;upstream_http_location&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://public.example.com:9080/login&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>Key findings:</p><ul><li>The incorrect <code>http/9080</code> was already formed inside APISIX</li><li>It wasn’t the application “breaking it again” during the return phase</li><li>The wrong request view the application received was passed in by the gateway itself</li></ul><h2 id="Easily-Confused-Points"><a href="#Easily-Confused-Points" class="headerlink" title="Easily Confused Points"></a>Easily Confused Points</h2><p>During troubleshooting, the most confusing aspect was mixing “user real IP” chain with “public origin” chain. These are not the same thing:</p><h3 id="User-Real-IP-Chain"><a href="#User-Real-IP-Chain" class="headerlink" title="User Real IP Chain"></a>User Real IP Chain</h3><p>Used for risk control, auditing, rate limiting, and log location, typically relying on:</p><ul><li>X-Real-IP</li><li>X-Forwarded-For</li></ul><h3 id="Public-Origin-Chain"><a href="#Public-Origin-Chain" class="headerlink" title="Public Origin Chain"></a>Public Origin Chain</h3><p>Used for OAuth2 redirects, absolute URL generation, and callback address validation, typically relying on:</p><ul><li>X-Forwarded-Proto</li><li>X-Forwarded-Host</li><li>X-Forwarded-Port</li></ul><p>The essence of this problem wasn’t “user IP not being passed through,” but APISIX corrupting the second chain after trust judgment failed.</p><h2 id="APISIX-Source-Code-Analysis"><a href="#APISIX-Source-Code-Analysis" class="headerlink" title="APISIX Source Code Analysis"></a>APISIX Source Code Analysis</h2><p>The problem ultimately converged on the <code>trusted_addresses</code> configuration.</p><p>APISIX request processing logic:</p><ol><li>Calculate previous hop address (usually via X-Real-IP or Remote Addr)</li><li>Match this address against <code>apisix.trusted_addresses</code></li><li>If not trusted, overwrite:<ul><li>X-Forwarded-Proto</li><li>X-Forwarded-Host</li><li>X-Forwarded-Port</li></ul></li><li>Overwrite values are not the external public entry, but what APISIX observes:<ul><li>scheme</li><li>host</li><li>server_port</li></ul></li></ol><p>Pseudocode representation:</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Check if previous hop address is in trusted list</span></span><br><span class="line"><span class="keyword">local</span> addr_is_trusted = trusted_addresses_util.is_trusted(api_ctx.var.realip_remote_addr)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> addr_is_trusted <span class="keyword">then</span></span><br><span class="line">    <span class="comment">-- When not trusted, overwrite X-Forwarded-* headers with gateway&#x27;s own view</span></span><br><span class="line">    <span class="keyword">local</span> proto = api_ctx.var.scheme        <span class="comment">-- actual value: http</span></span><br><span class="line">    <span class="keyword">local</span> host = api_ctx.var.host            <span class="comment">-- actual value: public.example.com</span></span><br><span class="line">    <span class="keyword">local</span> port = api_ctx.var.server_port     <span class="comment">-- actual value: 9080</span></span><br><span class="line"></span><br><span class="line">    core.request.set_header(api_ctx, <span class="string">&quot;X-Forwarded-Proto&quot;</span>, proto)</span><br><span class="line">    core.request.set_header(api_ctx, <span class="string">&quot;X-Forwarded-Host&quot;</span>, host)</span><br><span class="line">    core.request.set_header(api_ctx, <span class="string">&quot;X-Forwarded-Port&quot;</span>, port)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>This explains why the application kept seeing <code>http</code> and <code>9080</code>:</p><ul><li>Once APISIX judges the upstream proxy as “not trusted”</li><li>It downgrades external origin information to its own listening view</li></ul><p>For more configuration details, refer to <a href="https://apisix.apache.org/docs/apisix/conf/config.yml/">APISIX Official Documentation - trusted_addresses</a></p><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><h3 id="Quick-Verification"><a href="#Quick-Verification" class="headerlink" title="Quick Verification"></a>Quick Verification</h3><p>The simplest control variable experiment:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apisix:</span></span><br><span class="line">  <span class="attr">trusted_addresses:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">0.0</span><span class="number">.0</span><span class="number">.0</span><span class="string">/0</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">::/0</span></span><br></pre></td></tr></table></figure><p>After reloading, verify the OAuth2 authorization endpoint:</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>https://public.example.com/login</span><br></pre></td></tr></table></figure><p>The problem disappears immediately. This experiment itself isn’t the final configuration, but it pins down the root cause: <strong>the problem is at the APISIX <code>trusted_addresses</code> layer.</strong></p><h3 id="Production-Configuration"><a href="#Production-Configuration" class="headerlink" title="Production Configuration"></a>Production Configuration</h3><p>More appropriate approach for production:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apisix:</span></span><br><span class="line">  <span class="attr">node_listen:</span> <span class="number">9080</span></span><br><span class="line">  <span class="attr">enable_ipv6:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">trusted_addresses:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>           <span class="comment"># Local loopback</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&quot;::1&quot;</span>               <span class="comment"># IPv6 loopback</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">172.17</span><span class="number">.21</span><span class="number">.0</span><span class="string">/24</span>     <span class="comment"># Docker network (e.g., Docker Desktop)</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">192.168</span><span class="number">.10</span><span class="number">.0</span><span class="string">/24</span>     <span class="comment"># Internal network (where upstream proxy resides)</span></span><br></pre></td></tr></table></figure><p>With upstream Nginx configuration:</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> / &#123;</span><br><span class="line">    <span class="attribute">proxy_pass</span> http://apisix;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-Host <span class="variable">$host</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-Proto <span class="variable">$scheme</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-Port <span class="variable">$server_port</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Why-Application-Layer-Patches-Aren’t-the-Root-Solution"><a href="#Why-Application-Layer-Patches-Aren’t-the-Root-Solution" class="headerlink" title="Why Application Layer Patches Aren’t the Root Solution"></a>Why Application Layer Patches Aren’t the Root Solution</h2><p>During troubleshooting, several workarounds were attempted at the application layer:</p><ul><li>Custom RequestCache, forcibly changing SavedRequest to public address</li><li>Custom login redirect URL generation</li><li>Wrapping HttpServletRequest in filters to override scheme&#x2F;host&#x2F;port</li></ul><p>These methods can alleviate symptoms in the short term, but aren’t suitable as formal solutions:</p><ol><li>The problem won’t appear in just one service</li><li>Absolute URL generation doesn’t only occur in OAuth2 login flows</li><li>Future Swagger, email links, frontend API base URLs, callback addresses, and download links may encounter similar issues</li></ol><p>If the public access view isn’t uniformly governed at the gateway layer, it will eventually become patchwork for each service.</p><h2 id="Differences-Between-Dev-and-Production-Environments"><a href="#Differences-Between-Dev-and-Production-Environments" class="headerlink" title="Differences Between Dev and Production Environments"></a>Differences Between Dev and Production Environments</h2><p>Another reality of this problem: development and production network topologies differ.</p><p>Common dev environment chain:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Host Nginx → Docker Desktop port mapping → APISIX container</span><br></pre></td></tr></table></figure><p>More common production pattern:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Nginx node → Internal network → APISIX node</span><br></pre></td></tr></table></figure><p>In both environments, the “previous hop address” that APISIX actually sees can be completely different.</p><p>This causes a phenomenon:</p><ul><li>In dev environments, using a very narrow <code>trusted_addresses</code> may not stably reproduce production behavior</li><li>Because the address object APISIX uses for trust judgment may not be the expected upstream proxy address</li></ul><p>This is why in some local environments, temporarily widening <code>trusted_addresses</code> immediately restores functionality, while continuing to guess CIDRs can lead to endless loops.</p><h2 id="Applicable-Scenarios"><a href="#Applicable-Scenarios" class="headerlink" title="Applicable Scenarios"></a>Applicable Scenarios</h2><p>This record applies to similar scenarios:</p><ul><li>APISIX deployed behind a reverse proxy</li><li>Public unified domain and TLS termination at upstream layer</li><li>Downstream applications generate absolute URLs based on scheme&#x2F;host&#x2F;port</li><li>OAuth2 &#x2F; OIDC login flows sensitive to URL view consistency</li></ul><p>If any of the following phenomena appear in the chain:</p><ul><li>302 Location contains internal port</li><li>SavedRequest shows internal addresses or container ports</li><li>Frontend callbacks or logout addresses mix in internal ports</li><li>Mixed Content points to internal HTTP services</li></ul><p>It’s worth checking if APISIX is overwriting X-Forwarded-*.</p><h2 id="Quick-Checklist"><a href="#Quick-Checklist" class="headerlink" title="Quick Checklist"></a>Quick Checklist</h2><p>When encountering similar issues, check in order:</p><ul><li><input disabled type="checkbox"> Is <code>server_port</code> in APISIX logs showing the internal port</li><li><input disabled type="checkbox"> Does <code>trusted_addresses</code> include upstream proxy IPs</li><li><input disabled type="checkbox"> Is upstream Nginx properly setting X-Forwarded-* headers</li><li><input disabled type="checkbox"> Is application enabled <code>forward-headers-strategy</code></li><li><input disabled type="checkbox"> Is OAuth2 redirect URL using internal addresses</li></ul><p>For more on Forwarded Headers configuration, refer to <a href="https://docs.spring.io/spring-security/reference/servlet/exploits/csrf.html#csrf-foreign-hosts">Spring Security Official Documentation</a>.</p><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>This problem isn’t essentially an OAuth2 protocol issue, nor is it Spring Security’s special behavior.</p><p>It’s more like a typical gateway trust boundary problem:</p><ul><li>External entry is HTTPS</li><li>Gateway internal is HTTP 9080</li><li>If the gateway doesn’t stably preserve the public access view</li><li>Downstream services will naturally expose internal ports in absolute URLs</li></ul><p>And APISIX’s <code>trusted_addresses</code> is the key switch in this chain.</p><p>The problem wasn’t solved by a single “magic configuration,” but through a complete convergence:</p><ul><li>First admitting application patches aren’t the root solution</li><li>Then converging the problem to the gateway</li><li>Then using source code and control variables to pin down the root cause</li><li>Finally distinguishing dev environment temporary recovery from production formal configuration principles</li></ul><p>If the runtime chain contains both upstream proxies, gateways, and applications that need to generate absolute URLs, this type of problem is worth prioritizing from the trust boundary rather than business code.</p><p>These solutions can help maintain <code>trusted_addresses</code> correctness in more complex environments, avoiding repeated issues due to IP changes.</p>]]></content>
    
    
    <summary type="html">A detailed walkthrough of troubleshooting OAuth2 redirect URLs with incorrect ports in a multi-layer proxy architecture using APISIX, and the complete solution via configuring trusted_addresses.</summary>
    
    
    
    <category term="API Gateway" scheme="https://www.yzhu.name/categories/API-Gateway/"/>
    
    <category term="OAuth2" scheme="https://www.yzhu.name/categories/API-Gateway/OAuth2/"/>
    
    
    <category term="APISIX" scheme="https://www.yzhu.name/tags/APISIX/"/>
    
    <category term="OAuth2" scheme="https://www.yzhu.name/tags/OAuth2/"/>
    
    <category term="gateway" scheme="https://www.yzhu.name/tags/gateway/"/>
    
    <category term="troubleshooting" scheme="https://www.yzhu.name/tags/troubleshooting/"/>
    
  </entry>
  
  <entry>
    <title>Microservice Gateway Routing: OAuth2 Redirects and Service Decomposition</title>
    <link href="https://www.yzhu.name/2026/02/27/microservice-gateway-oauth2-routing-en/"/>
    <id>https://www.yzhu.name/2026/02/27/microservice-gateway-oauth2-routing-en/</id>
    <published>2026-02-27T14:00:00.000Z</published>
    <updated>2026-02-27T14:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/02/27/microservice-gateway-oauth2-routing/">中文版</a></p></blockquote><p>In microservice architectures, the API Gateway serves as the unified governance layer for all incoming traffic. Its routing design directly impacts system maintainability and evolution capabilities. This article documents a real-world scenario involving OAuth2 redirect issues during gateway integration with Spring Security, and how we achieved seamless client experience and high backend cohesion through Context-Path configuration and BFF (Backend For Frontend) patterns during service decomposition.</p><span id="more"></span><h2 id="The-Problem-Scattered-Routes-Creating-Architectural-Debt"><a href="#The-Problem-Scattered-Routes-Creating-Architectural-Debt" class="headerlink" title="The Problem: Scattered Routes Creating Architectural Debt"></a>The Problem: Scattered Routes Creating Architectural Debt</h2><h3 id="Initial-Scenario"><a href="#Initial-Scenario" class="headerlink" title="Initial Scenario"></a>Initial Scenario</h3><p>When debugging our gateway locally, we discovered that accessing sample applications through the gateway (e.g., <code>http://localhost:18000/java-example</code>) and clicking the “Login with Enterprise IdP” button resulted in a <code>404 Not Found</code> error when redirecting to the gateway’s <code>/login</code> path.</p><h3 id="First-Fix-The-Patch-Approach"><a href="#First-Fix-The-Patch-Approach" class="headerlink" title="First Fix: The Patch Approach"></a>First Fix: The Patch Approach</h3><p>To quickly resolve this, we implemented a “patch” solution by hardcoding scattered routes in the gateway configuration:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Scattered routes configuration in gateway</span></span><br><span class="line"><span class="attr">routes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">paths:</span> [<span class="string">&quot;/login&quot;</span>]</span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">preserve_host:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">paths:</span> [<span class="string">&quot;/oauth2&quot;</span>]</span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">preserve_host:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">paths:</span> [<span class="string">&quot;/css&quot;</span>, <span class="string">&quot;/js&quot;</span>, <span class="string">&quot;/error&quot;</span>]</span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">preserve_host:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure><p><strong>Result</strong>: While the login flow worked, this scattered routing configuration at the gateway’s root level felt fragile. In production operations, such scattered routes would lead to:</p><ul><li><strong>High maintenance costs</strong>: Each new path requires manual configuration, prone to omissions and errors</li><li><strong>Configuration chaos</strong>: As business grows, routing rules quickly become bloated and difficult to manage</li><li><strong>Security risks</strong>: Unified access control becomes challenging, potentially enabling path traversal or unauthorized access</li><li><strong>Poor scalability</strong>: Service decomposition or path adjustments require extensive gateway configuration changes, hindering rapid iteration</li></ul><hr><h2 id="Unified-Prefix-Approach-Redirect-Problem-Analysis"><a href="#Unified-Prefix-Approach-Redirect-Problem-Analysis" class="headerlink" title="Unified Prefix Approach: Redirect Problem Analysis"></a>Unified Prefix Approach: Redirect Problem Analysis</h2><p>Since scattered routes are messy, can we unify <code>/login</code>, <code>/oauth2</code>, and other scattered configurations under a <code>/auth</code> prefix at the gateway level?</p><p><strong>The answer is no—this introduces redirect problems.</strong></p><h3 id="Root-Cause-Analysis"><a href="#Root-Cause-Analysis" class="headerlink" title="Root Cause Analysis"></a>Root Cause Analysis</h3><p>Frameworks like Spring Security, when constructing page URLs or generating absolute <code>Location</code> redirect responses (302 redirects) sent to clients, if unaware they’re behind a gateway’s <code>/auth</code> prefix, will still generate links based on the root path <code>/oauth2/authorization/...</code>.</p><pre class="mermaid">sequenceDiagram    participant Browser as Browser    participant Gateway as API Gateway    participant Service as auth-service (Spring Security)    Browser->>Gateway: GET /auth/login    Gateway->>Service: GET /auth/login (strip_path: false)    Service-->>Gateway: 302 Location: /oauth2/authorization/idp    Gateway-->>Browser: 302 Location: /oauth2/authorization/idp    Browser->>Gateway: GET /oauth2/authorization/idp    Gateway-->>Browser: 404 Not Found (route doesn't exist)</pre><p><strong>Key Conclusion</strong>: Frontend and backend contexts must be strongly aligned! Never hide the prefix on one side while exposing it on the other.</p><hr><h2 id="Context-Path-and-Service-Decomposition-Compatibility"><a href="#Context-Path-and-Service-Decomposition-Compatibility" class="headerlink" title="Context-Path and Service Decomposition Compatibility"></a>Context-Path and Service Decomposition Compatibility</h2><h3 id="The-Canonical-Solution-Server-Side-Context-Path-Configuration"><a href="#The-Canonical-Solution-Server-Side-Context-Path-Configuration" class="headerlink" title="The Canonical Solution: Server-Side Context-Path Configuration"></a>The Canonical Solution: Server-Side Context-Path Configuration</h3><p>To align contexts, the solution is to configure the Java backend service directly:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># application.yml</span></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">context-path:</span> <span class="string">/auth</span> <span class="comment"># Context-Path defines the web application&#x27;s root path.</span></span><br><span class="line">                       <span class="comment"># When set to /auth, all requests to this service start with /auth, e.g., /auth/login.</span></span><br></pre></td></tr></table></figure><h3 id="API-Compatibility-After-Service-Decomposition"><a href="#API-Compatibility-After-Service-Decomposition" class="headerlink" title="API Compatibility After Service Decomposition"></a>API Compatibility After Service Decomposition</h3><p>This raises a core question in microservice evolution: <strong>If the service prefix is hardcoded at the code level, when future business changes require service splitting, do client API URLs need to change as well?</strong></p><p>If clients are forced to upgrade their code because of backend decomposition, doesn’t this defeat the purpose of the gateway’s anti-corruption layer?</p><h3 id="Gateway-Routing-Priority-Design"><a href="#Gateway-Routing-Priority-Design" class="headerlink" title="Gateway Routing Priority Design"></a>Gateway Routing Priority Design</h3><p>Through deep analysis, we arrived at the core API management philosophy for microservices:</p><blockquote><p><strong>Microservices should never care about their external names. The API contract published externally (e.g., <code>http://gateway/auth/users</code>) is entirely the governance asset of the API Gateway.</strong></p></blockquote><p>If we decompose some interfaces to a new service, clients <strong>don’t need to change</strong>—they still access the old <code>/auth/new-feature</code>.</p><p><strong>Backend Development</strong>: Add a high-priority <strong>exact prefix hijacking route</strong> in the gateway:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">routes:</span></span><br><span class="line">  <span class="comment"># Generic route (low priority)</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">route-iam</span></span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/auth</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span> <span class="comment"># Client requests /auth/login, backend receives /auth/login</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># Exact hijacking route (high priority)</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">route-new-feature</span></span><br><span class="line">    <span class="attr">service:</span> <span class="string">new-service</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/auth/new-feature</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">true</span>  <span class="comment"># Client requests /auth/new-feature/api, backend receives /api</span></span><br><span class="line">    <span class="comment"># More specific paths (/auth/new-feature) are matched first</span></span><br></pre></td></tr></table></figure><p><strong>Configuration Strategy</strong>:</p><table><thead><tr><th align="left">Route Type</th><th align="left">Path Config</th><th align="left">strip_path</th><th align="left">Priority</th><th align="left">Description</th></tr></thead><tbody><tr><td align="left">Generic Route</td><td align="left"><code>/auth</code></td><td align="left"><code>false</code></td><td align="left">Low</td><td align="left">Handles most auth-service requests</td></tr><tr><td align="left">Hijacking Route</td><td align="left"><code>/auth/new-feature</code></td><td align="left"><code>true</code></td><td align="left">High</td><td align="left">Exact match, intercepts specific sub-paths and forwards to new service</td></tr></tbody></table><p>With this combination, client code requires no modifications, achieving service decomposition through the gateway.</p><hr><h2 id="BFF-Pattern-and-OAuth2-Security-Best-Practices"><a href="#BFF-Pattern-and-OAuth2-Security-Best-Practices" class="headerlink" title="BFF Pattern and OAuth2 Security Best Practices"></a>BFF Pattern and OAuth2 Security Best Practices</h2><p><strong>BFF (Backend For Frontend)</strong> is an architecture pattern where backends are tailored for specific frontend applications (Web, iOS, Android). It sits between traditional backend services and frontends, responsible for aggregating and transforming data, and handling frontend-specific business logic to optimize user experience and development efficiency.</p><h3 id="Pure-Frontend-Login-Approach-Considerations"><a href="#Pure-Frontend-Login-Approach-Considerations" class="headerlink" title="Pure Frontend Login Approach Considerations"></a>Pure Frontend Login Approach Considerations</h3><p>Since handling Spring Boot page endpoints with redirects is complex, can we separate the login page into a pure frontend microservice (like an independently deployed Vue&#x2F;React container) to avoid backend redirect binding logic?</p><h3 id="Browser-Side-OAuth2-Security-Risks"><a href="#Browser-Side-OAuth2-Security-Risks" class="headerlink" title="Browser-Side OAuth2 Security Risks"></a>Browser-Side OAuth2 Security Risks</h3><p>Completing the OAuth2 loop and storing tokens purely in the browser presents serious security risks:</p><ul><li><strong>Implicit Flow</strong>: Directly obtaining Access Tokens in the browser is vulnerable to XSS attacks and token leakage, and has been deprecated in OAuth 2.1.</li><li><strong>PKCE (Proof Key for Code Exchange)</strong>: An enhancement to the authorization code flow to prevent authorization code interception attacks, <strong>currently the recommended standard practice</strong>, but token storage and usage must still be completed on the backend, not purely in the frontend.</li></ul><p>Under current security standards and best practices, <strong>frontends should not directly hold Access Tokens</strong>.</p><h3 id="BFF-Pattern-Best-Practices"><a href="#BFF-Pattern-Best-Practices" class="headerlink" title="BFF Pattern Best Practices"></a>BFF Pattern Best Practices</h3><p><strong>Core Principle</strong>: OAuth2 authorization code exchange must be completed by the backend; frontends should not hold tokens. The handshake with external IdPs (enterprise identity providers) to exchange authorization codes and Access Tokens <strong>should be handled at the backend BFF layer (i.e., <code>auth-service</code>)</strong>.</p><pre class="mermaid">sequenceDiagram    participant Browser as Browser    participant BFF as auth-service    participant IdP as Enterprise IdP    participant Cache as Redis/Cache    Note right of Browser: 1. User clicks login    Browser->>BFF: GET /auth/login    BFF-->>Browser: Returns login page    Note right of Browser: 2. User clicks Enterprise IdP login    Browser->>BFF: GET /auth/oauth2/authorization/idp    BFF->>IdP: 302 Location: https://idp.example.com/...    Note right of Browser: 3. User completes authentication at IdP    IdP-->>BFF: GET /auth/login/oauth2/code/idp?code=...    BFF->>IdP: POST /token    IdP-->>BFF: access_token, refresh_token    Note right of BFF: 4. Store tokens in distributed cache    BFF->>Cache: SET session_id {tokens}    BFF-->>Browser: Set-Cookie: SESSION_ID</pre><p><strong>Key Security Measures</strong>:</p><ol><li><strong>BFF stores tokens in distributed cache (e.g., Redis) after obtaining them</strong></li><li><strong>Only issue an HttpOnly + Secure Cookie key to the user’s browser externally</strong></li><li><strong>Cookies should not be readable by JS</strong>, preventing XSS theft</li><li><strong>Frontend uses this invisible same-domain cookie key to make all seamless calls</strong></li><li><strong>Production environments recommend using distributed caches like Redis to share Session</strong>, avoiding Session inconsistency in multi-instance deployments</li></ol><hr><h2 id="Context-Path-Configuration-Strategy"><a href="#Context-Path-Configuration-Strategy" class="headerlink" title="Context-Path Configuration Strategy"></a>Context-Path Configuration Strategy</h2><h3 id="Authentication-BFF-Service-Configuration"><a href="#Authentication-BFF-Service-Configuration" class="headerlink" title="Authentication BFF Service Configuration"></a>Authentication BFF Service Configuration</h3><p><strong>1. Add to auth-service’s application.yml:</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">context-path:</span> <span class="string">/auth</span></span><br></pre></td></tr></table></figure><p><strong>2. Clean up redundant authentication routes (<code>/login</code>, etc.) in gateway configuration.</strong></p><p><strong>3. Restore the single <code>/auth</code> route mapping in the gateway, changing <code>strip_path</code> to <code>false</code>:</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">routes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">route-iam</span></span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/auth</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span>  <span class="comment"># Key: preserve /auth prefix</span></span><br></pre></td></tr></table></figure><p><strong>4. Example client targets the single authentication handshake endpoint:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://gateway:18000/auth/login</span><br></pre></td></tr></table></figure><h3 id="Architecture-Extension-Do-Pure-API-Microservices-Need-Context-Path"><a href="#Architecture-Extension-Do-Pure-API-Microservices-Need-Context-Path" class="headerlink" title="Architecture Extension: Do Pure API Microservices Need Context-Path?"></a>Architecture Extension: Do Pure API Microservices Need Context-Path?</h3><p>For <strong>pure API microservices</strong> without authorization pages and redirect logic (no absolute redirect concepts), they <strong>absolutely do not need</strong> Context-Path configuration!</p><p>Develop directly at the default root path <code>/</code>, letting the API Gateway handle all path merging, prefix trimming, and assembly strategies with <code>strip_path: true</code>, keeping services as pure API interface nodes.</p><table><thead><tr><th align="left">Service Type</th><th align="left">Needs Context-Path</th><th align="left">strip_path</th><th align="left">Reason</th></tr></thead><tbody><tr><td align="left"><strong>Authentication BFF</strong></td><td align="left"><strong>Yes</strong></td><td align="left"><code>false</code></td><td align="left">Needs to handle redirects; frontend and backend contexts must align</td></tr><tr><td align="left"><strong>Pure API Microservice</strong></td><td align="left"><strong>No</strong></td><td align="left"><code>true</code></td><td align="left">No redirect logic; gateway handles path trimming, service stays pure</td></tr></tbody></table><hr><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>In microservice gateway routing design, the core principle is: <strong>Find the boundary between gateway control plane and backend logic plane, making the gateway a truly powerful anti-corruption layer.</strong></p><p><strong>Key Decision Tree</strong>:</p><pre class="mermaid">flowchart TD    A[Microservice Routing Design] --> B{Service Type?}    B -->|Authentication BFF| C[Configure Context-Path]    B -->|Pure API Microservice| D[No Context-Path]    C --> E[Gateway strip_path: false]    D --> F[Gateway strip_path: true]    E --> G[Align frontend and backend contexts]    F --> H[Keep services pure]    G --> I{Future decomposition?}    H --> I    I -->|Needed| J[Gateway exact prefix hijacking]    I -->|Not needed| K[Maintain status quo]</pre><p><strong>Core Takeaways</strong>:</p><ol><li><strong>Frontend and backend contexts must be strongly aligned</strong>: Services with redirects must configure Context-Path; gateway doesn’t trim prefix</li><li><strong>Gateway owns the API contract</strong>: Services don’t care about their external names; API contracts are governed by the gateway</li><li><strong>Service decomposition is transparent to clients</strong>: Through gateway exact prefix hijacking, clients don’t need code changes when backends decompose</li><li><strong>BFF pattern handles OAuth2</strong>: Authorization code exchange happens on the backend; frontend doesn’t hold tokens</li></ol><blockquote><p>“In the face of any architectural dilemma, finding the boundary between gateway control plane and backend logic plane is always where the anti-corruption layer delivers its greatest power!”</p></blockquote>]]></content>
    
    
    <summary type="html">A practical guide to designing API gateway routing for OAuth2 authentication flows in microservices. Learn how to handle redirects, configure Context-Path, and implement BFF patterns for seamless service decomposition without breaking client contracts.</summary>
    
    
    
    <category term="Architecture" scheme="https://www.yzhu.name/categories/Architecture/"/>
    
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="microservice" scheme="https://www.yzhu.name/tags/microservice/"/>
    
    <category term="architecture" scheme="https://www.yzhu.name/tags/architecture/"/>
    
    <category term="api-gateway" scheme="https://www.yzhu.name/tags/api-gateway/"/>
    
  </entry>
  
  <entry>
    <title>微服务网关路由设计：OAuth2 重定向与服务拆分实践</title>
    <link href="https://www.yzhu.name/2026/02/27/microservice-gateway-oauth2-routing/"/>
    <id>https://www.yzhu.name/2026/02/27/microservice-gateway-oauth2-routing/</id>
    <published>2026-02-27T14:00:00.000Z</published>
    <updated>2026-02-27T14:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 语言</strong>: <strong>中文版</strong> | <a href="/2026/02/27/microservice-gateway-oauth2-routing-en/">English Version</a></p></blockquote><p>在微服务架构中，API 网关作为流量入口的统一治理层，其路由设计直接影响系统的可维护性和演进能力。记录一次在网关与 Spring Security OAuth2 集成过程中遇到的重定向问题，以及在服务拆分场景下，通过 Context-Path 配置和 BFF（Backend For Frontend）模式，实现客户端无感知、后端高内聚的微服务演进实践。<span id="more"></span></p><h2 id="一、-问题的产生：分散路由引发的架构困境"><a href="#一、-问题的产生：分散路由引发的架构困境" class="headerlink" title="一、 问题的产生：分散路由引发的架构困境"></a>一、 问题的产生：分散路由引发的架构困境</h2><h3 id="1-1-初始场景"><a href="#1-1-初始场景" class="headerlink" title="1.1 初始场景"></a>1.1 初始场景</h3><p>我们在本地环境调试网关时，发现通过网关访问示例应用（如 <code>http://localhost:18000/java-example</code>），点击”使用企业 IdP 进行登录”按钮后跳转到网关的 <code>/login</code> 路径时，出现了 <code>404 Not Found</code> 错误。</p><h3 id="1-2-第一次修复：打补丁的方案"><a href="#1-2-第一次修复：打补丁的方案" class="headerlink" title="1.2 第一次修复：打补丁的方案"></a>1.2 第一次修复：打补丁的方案</h3><p>为了快速修复这个错误，我们采用了”打补丁”的方式：在网关配置文件中硬编码了一套分散路由，包含以下路径：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 网关中的分散路由配置示例</span></span><br><span class="line"><span class="attr">routes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">paths:</span> [<span class="string">&quot;/login&quot;</span>]</span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">preserve_host:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">paths:</span> [<span class="string">&quot;/oauth2&quot;</span>]</span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">preserve_host:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">paths:</span> [<span class="string">&quot;/css&quot;</span>, <span class="string">&quot;/js&quot;</span>, <span class="string">&quot;/error&quot;</span>]</span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">preserve_host:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure><p><strong>结果</strong>：虽然登录流程跑通了，但这堆散乱的路由配置在网关的核心根目录下，显得脆弱。在实际运维中，这种分散路由将导致：</p><ul><li><strong>维护成本高昂</strong>：每个新路径都需要手动配置，容易遗漏和出错。</li><li><strong>配置混乱</strong>：随着业务增长，路由规则会迅速膨胀，难以管理和理解。</li><li><strong>安全风险</strong>：权限控制难以统一，容易出现路径穿越或未授权访问。</li><li><strong>扩展性差</strong>：服务拆分或调整路径时，需要大量修改网关配置，阻碍业务快速迭代。</li></ul><hr><h2 id="二、-统一前缀方案：重定向问题分析"><a href="#二、-统一前缀方案：重定向问题分析" class="headerlink" title="二、 统一前缀方案：重定向问题分析"></a>二、 统一前缀方案：重定向问题分析</h2><p>既然分散路由太乱，那么能否在网关层面把 <code>/login</code>、<code>/oauth2</code> 这些零散配置统一套上一层 <code>/auth</code> 前缀进行转发？</p><p><strong>答案是否定的，这会引发重定向问题。</strong></p><h3 id="2-1-根本原因分析"><a href="#2-1-根本原因分析" class="headerlink" title="2.1 根本原因分析"></a>2.1 根本原因分析</h3><p>Spring Security 这类底层认证框架，在构建页面地址或生成向客户端发送的浏览器绝对 <code>Location</code> 重定向响应（即 302 跳转）时，如果它不知道自己实际躲在网关的 <code>/auth</code> 前缀后面，它生成的链接依然会基于根路径 <code>/oauth2/authorization/...</code>。</p><pre class="mermaid">sequenceDiagram    participant Browser as 浏览器    participant Gateway as API 网关    participant Service as auth-service (Spring Security)    Browser->>Gateway: GET /auth/login    Gateway->>Service: GET /auth/login (strip_path: false)    Service-->>Gateway: 302 Location: /oauth2/authorization/idp    Gateway-->>Browser: 302 Location: /oauth2/authorization/idp    Browser->>Gateway: GET /oauth2/authorization/idp    Gateway-->>Browser: 404 Not Found (路由不存在)</pre><p><strong>关键结论</strong>：前后端上下文必须强对齐！决不可一边藏头，一边露尾。</p><hr><h2 id="三、-Context-Path-与服务拆分的兼容性"><a href="#三、-Context-Path-与服务拆分的兼容性" class="headerlink" title="三、 Context-Path 与服务拆分的兼容性"></a>三、 Context-Path 与服务拆分的兼容性</h2><h3 id="3-1-正统解法：服务端配置-Context-Path"><a href="#3-1-正统解法：服务端配置-Context-Path" class="headerlink" title="3.1 正统解法：服务端配置 Context-Path"></a>3.1 正统解法：服务端配置 Context-Path</h3><p>为了对齐上下文环境，解法是给 Java 底层的服务直接加上：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># application.yml</span></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">context-path:</span> <span class="string">/auth</span> <span class="comment"># Context-Path (上下文路径) 定义了 Web 应用程序的根路径。</span></span><br><span class="line">                       <span class="comment"># 当设置为 /auth 时，所有对该服务的请求都将以 /auth 开头，例如 /auth/login。</span></span><br></pre></td></tr></table></figure><h3 id="3-2-服务拆分后的-API-兼容性"><a href="#3-2-服务拆分后的-API-兼容性" class="headerlink" title="3.2 服务拆分后的 API 兼容性"></a>3.2 服务拆分后的 API 兼容性</h3><p>但这就引出了微服务演进中的核心问题：<strong>如果在代码层写死了服务前缀，未来随着业务变迁导致服务需要切割拆分，客户端的 API URL 是否就得跟着变？</strong></p><p>如果客户端需要因为你的后端拆解而被迫全量改代码升级，这网关的防腐层价值岂不是形同虚设？</p><h3 id="3-3-网关路由优先级设计"><a href="#3-3-网关路由优先级设计" class="headerlink" title="3.3 网关路由优先级设计"></a>3.3 网关路由优先级设计</h3><p>经过深入推导，我们得出了微服务最核心的 API 管理理念：</p><blockquote><p><strong>微服务内部绝不应该关心自己对外的名字叫啥。对外公布的那层 API 契约（如 <code>http://网关/auth/users</code>）全权属于 API 网关的治理资产。</strong></p></blockquote><p>如果我们拆分了部分接口到新服务，客户端 <strong>无需改动</strong>，仍旧访问旧的 <code>/auth/new-feature</code>。</p><p><strong>后端开发</strong>：在网关中增加一条高优先级的<strong>精确前缀劫持路由</strong>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">routes:</span></span><br><span class="line">  <span class="comment"># 通用路由（优先级低）</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">route-iam</span></span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/auth</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span> <span class="comment"># 客户端请求 /auth/login，后端服务收到 /auth/login</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># 精确劫持路由（优先级高）</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">route-new-feature</span></span><br><span class="line">    <span class="attr">service:</span> <span class="string">new-service</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/auth/new-feature</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">true</span>  <span class="comment"># 客户端请求 /auth/new-feature/api，后端服务收到 /api</span></span><br><span class="line">    <span class="comment"># 更具体的路径（/auth/new-feature）会被优先匹配</span></span><br></pre></td></tr></table></figure><p><strong>配置策略</strong>：</p><table><thead><tr><th align="left">路由类型</th><th align="left">路径配置</th><th align="left">strip_path</th><th align="left">优先级</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">通用路由</td><td align="left"><code>/auth</code></td><td align="left"><code>false</code></td><td align="left">低</td><td align="left">处理大部分 auth-service 的请求</td></tr><tr><td align="left">劫持路由</td><td align="left"><code>/auth/new-feature</code></td><td align="left"><code>true</code></td><td align="left">高</td><td align="left">精确匹配，拦截特定子路径并转发到新服务</td></tr></tbody></table><p>用这种组合方式，客户端代码无需修改，通过网关实现服务拆分。</p><hr><h2 id="四、-BFF-模式与-OAuth2-安全实践"><a href="#四、-BFF-模式与-OAuth2-安全实践" class="headerlink" title="四、 BFF 模式与 OAuth2 安全实践"></a>四、 BFF 模式与 OAuth2 安全实践</h2><p><strong>BFF (Backend For Frontend)</strong> 模式，即”服务于前端的后端”，是一种专门为特定前端应用（如 Web、iOS、Android）定制后端 API 的架构模式。它介于传统后端服务和前端之间，负责聚合、转换数据，并处理特定于前端的业务逻辑，以优化前端的用户体验和开发效率。</p><h3 id="4-1-纯前端登录方案考量"><a href="#4-1-纯前端登录方案考量" class="headerlink" title="4.1 纯前端登录方案考量"></a>4.1 纯前端登录方案考量</h3><p>既然处理带有重定向的 Spring Boot 页面端点比较复杂，我们能否把登录页分离成纯前端微服务（如独立部署的 Vue&#x2F;React 容器），从而避免后端的重定向绑定逻辑？</p><h3 id="4-2-浏览器端-OAuth2-的安全风险"><a href="#4-2-浏览器端-OAuth2-的安全风险" class="headerlink" title="4.2 浏览器端 OAuth2 的安全风险"></a>4.2 浏览器端 OAuth2 的安全风险</h3><p>在纯浏览器端完成 OAuth2 闭环获取 Token 并保存存在严重安全风险：</p><ul><li><strong>隐式流（Implicit Flow）</strong>：直接在浏览器中获取 Access Token，由于易遭受 XSS 攻击和 Token 泄露风险，已被 OAuth 2.1 废弃。</li><li><strong>PKCE（Proof Key for Code Exchange）</strong>：是对授权码流的增强，用于防止授权码拦截攻击，<strong>是目前推荐的标准做法</strong>，但 Token 的存储和使用仍需在后端完成，而非纯前端。</li></ul><p>在当前的安全规范和最佳实践下，<strong>前端不应直接持有 Access Token</strong>。</p><h3 id="4-3-BFF-模式的最佳实践"><a href="#4-3-BFF-模式的最佳实践" class="headerlink" title="4.3 BFF 模式的最佳实践"></a>4.3 BFF 模式的最佳实践</h3><p><strong>基本原理</strong>：OAuth2 授权码交换必须由后端完成，前端不应持有 Token。即发起跟外部 IdP（如企业身份提供商）的握手并换取授权码、Access Token 的操作，<strong>应在后端 BFF 层处理（即 <code>auth-service</code>）</strong>。</p><pre class="mermaid">sequenceDiagram    participant Browser as 浏览器    participant BFF as auth-service    participant IdP as 企业 IdP    participant Cache as Redis/缓存    Note right of Browser: 1. 用户点击登录    Browser->>BFF: GET /auth/login    BFF-->>Browser: 返回登录页面    Note right of Browser: 2. 用户点击企业 IdP 登录    Browser->>BFF: GET /auth/oauth2/authorization/idp    BFF->>IdP: 302 Location: https://idp.example.com/...    Note right of Browser: 3. 用户在 IdP 完成认证    IdP-->>BFF: GET /auth/login/oauth2/code/idp?code=...    BFF->>IdP: POST /token    IdP-->>BFF: access_token, refresh_token    Note right of BFF: 4. Token 存入分布式缓存    BFF->>Cache: SET session_id {tokens}    BFF-->>Browser: Set-Cookie: SESSION_ID</pre><p><strong>关键安全措施</strong>：</p><ol><li><strong>BFF 拿到 Token 后存入分布式缓存（如 Redis）</strong></li><li><strong>外层只给用户的浏览器配发一把 HttpOnly + Secure Cookie 钥匙</strong></li><li><strong>Cookie 不应被 JS 读取</strong>，防止 XSS 窃取</li><li><strong>前端拿着这把隐形同域 Cookie 钥匙发起一切无感调用</strong></li><li><strong>生产环境建议使用 Redis 等分布式缓存共享 Session</strong>，避免多实例部署时的 Session 不一致问题</li></ol><hr><h2 id="五、-Context-Path-配置方案"><a href="#五、-Context-Path-配置方案" class="headerlink" title="五、 Context-Path 配置方案"></a>五、 Context-Path 配置方案</h2><h3 id="5-1-认证-BFF-服务配置"><a href="#5-1-认证-BFF-服务配置" class="headerlink" title="5.1 认证 BFF 服务配置"></a>5.1 认证 BFF 服务配置</h3><p><strong>1. auth-service 的 application.yml 增加：</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">context-path:</span> <span class="string">/auth</span></span><br></pre></td></tr></table></figure><p><strong>2. 网关配置文件清理多余的认证路由（<code>/login</code> 等）。</strong></p><p><strong>3. 网关恢复原本唯一的那条针对 <code>/auth</code> 路由进行映射，并将透传代理 <code>strip_path</code> 修改为 <code>false</code>：</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">routes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">route-iam</span></span><br><span class="line">    <span class="attr">service:</span> <span class="string">auth-service</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/auth</span></span><br><span class="line">    <span class="attr">strip_path:</span> <span class="literal">false</span>  <span class="comment"># 关键：保留 /auth 前缀</span></span><br></pre></td></tr></table></figure><p><strong>4. 示例客户端对准唯一的发起端点进行鉴权握手起步：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://网关:18000/auth/login</span><br></pre></td></tr></table></figure><h3 id="5-2-架构延伸：纯-API-微服务是否需要-Context-Path？"><a href="#5-2-架构延伸：纯-API-微服务是否需要-Context-Path？" class="headerlink" title="5.2 架构延伸：纯 API 微服务是否需要 Context-Path？"></a>5.2 架构延伸：纯 API 微服务是否需要 Context-Path？</h3><p>如果是针对其他非授权认证页及跳转逻辑的<strong>纯净接口微服务（没有绝对重定向概念）</strong>，它 <strong>完全不需要</strong> 配置 Context-Path！</p><p>直接使用默认在根路径 <code>/</code> 开发，把所有路径的合并组装、修剪去前缀和合并策略，统统在 API 网关用 <code>strip_path: true</code> 完成，保持服务为纯 API 接口节点。</p><table><thead><tr><th align="left">服务类型</th><th align="left">是否需要 Context-Path</th><th align="left">strip_path</th><th align="left">原因</th></tr></thead><tbody><tr><td align="left"><strong>认证 BFF</strong></td><td align="left"><strong>是</strong></td><td align="left"><code>false</code></td><td align="left">需要处理重定向，前后端上下文必须对齐</td></tr><tr><td align="left"><strong>纯 API 微服务</strong></td><td align="left"><strong>否</strong></td><td align="left"><code>true</code></td><td align="left">无重定向逻辑，网关负责路径修剪，服务保持纯净</td></tr></tbody></table><hr><h2 id="六、-总结"><a href="#六、-总结" class="headerlink" title="六、 总结"></a>六、 总结</h2><p>在微服务网关路由设计中，核心原则是：<strong>找准网关控制面和后台逻辑面的界限，让网关成为真正强大的防腐层</strong>。</p><p><strong>关键决策树</strong>：</p><pre class="mermaid">flowchart TD    A[微服务路由设计] --> B{服务类型?}    B -->|认证 BFF| C[配置 Context-Path]    B -->|纯 API 微服务| D[不配置 Context-Path]    C --> E[网关 strip_path: false]    D --> F[网关 strip_path: true]    E --> G[前后端上下文对齐]    F --> H[服务保持纯净]    G --> I[未来拆分?]    H --> I    I -->|需要拆分| J[网关精确前缀劫持]    I -->|暂不拆分| K[维持现状]</pre><p><strong>核心要点</strong>：</p><ol><li><strong>前后端上下文必须强对齐</strong>：有重定向的服务必须配置 Context-Path，网关不修剪前缀</li><li><strong>网关负责 API 契约</strong>：服务内部不关心对外名字，API 契约由网关治理</li><li><strong>服务拆分客户端无感知</strong>：通过网关精确前缀劫持，客户端无需因后端拆分而修改代码</li><li><strong>BFF 模式处理 OAuth2</strong>：授权码交换在后端完成，前端不持有 Token</li></ol><blockquote><p>“在任何架构困境面前，找准网关控制面和后台逻辑面的界限，永远是防腐层发挥威力最大的秘密！”</p></blockquote>]]></content>
    
    
    <summary type="html">微服务架构中 API 网关 OAuth2 认证流程路由设计的实践指南。学习如何处理重定向、配置 Context-Path，以及实现 BFF 模式，实现客户端无感知的服务拆分。</summary>
    
    
    
    <category term="Architecture" scheme="https://www.yzhu.name/categories/Architecture/"/>
    
    
    <category term="oauth2" scheme="https://www.yzhu.name/tags/oauth2/"/>
    
    <category term="microservice" scheme="https://www.yzhu.name/tags/microservice/"/>
    
    <category term="architecture" scheme="https://www.yzhu.name/tags/architecture/"/>
    
    <category term="api-gateway" scheme="https://www.yzhu.name/tags/api-gateway/"/>
    
  </entry>
  
  <entry>
    <title>微服务架构下的 API 网关选型实践</title>
    <link href="https://www.yzhu.name/2026/02/24/api-gateway-selection-guide/"/>
    <id>https://www.yzhu.name/2026/02/24/api-gateway-selection-guide/</id>
    <published>2026-02-24T11:00:00.000Z</published>
    <updated>2026-02-24T11:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>启动一个新的微服务项目时，API 网关的选型是早期需要做出的关键决策之一。网关作为流量入口，承担了路由、认证、限流、监控等职责，直接影响后续系统的可维护性。</p><h2 id="背景与需求"><a href="#背景与需求" class="headerlink" title="背景与需求"></a>背景与需求</h2><p>我们团队的技术背景：</p><ul><li>主力语言：Java</li><li>技术框架：Spring Boot, Spring Cloud</li><li>部署环境：初期 Docker Compose，生产环境规划 Kubernetes</li><li>服务注册中心：Nacos</li><li>认证协议：OAuth2&#x2F;OIDC（认证中心基于 Spring Authorization Server）</li></ul><p>基于这个背景，我们梳理了三个核心需求：</p><ol><li><strong>动态性</strong>：与 Nacos 深度集成，支持服务自动发现和配置热更新</li><li><strong>安全对接</strong>：无缝对接 OAuth2&#x2F;OIDC 认证体系，处理 Token 验证</li><li><strong>可扩展性</strong>：支持自定义插件，能够实现业务特定的准入控制和权限验证逻辑</li></ol><h2 id="候选方案"><a href="#候选方案" class="headerlink" title="候选方案"></a>候选方案</h2><p>我们筛选了四个主流开源网关进行评估：</p><table><thead><tr><th>方案</th><th>底层技术</th><th>主要特点</th></tr></thead><tbody><tr><td><strong>Kong</strong></td><td>Nginx + Lua</td><td>插件生态丰富，成熟度高</td></tr><tr><td><strong>Spring Cloud Gateway</strong></td><td>Netty + Reactor</td><td>Spring 官方出品，与 Spring 生态深度集成</td></tr><tr><td><strong>Higress</strong></td><td>Envoy</td><td>阿里开源，拥抱 Wasm，适合服务网格场景</td></tr><tr><td><strong>Apache APISIX</strong></td><td>Nginx</td><td>云原生架构（etcd 存储），动态化设计，插件生态完善</td></tr></tbody></table><h2 id="对比分析"><a href="#对比分析" class="headerlink" title="对比分析"></a>对比分析</h2><h3 id="维度一：性能与架构"><a href="#维度一：性能与架构" class="headerlink" title="维度一：性能与架构"></a>维度一：性能与架构</h3><ul><li><strong>Kong &#x2F; APISIX</strong>：基于 Nginx，网络 I&#x2F;O 性能优异，延迟低</li><li><strong>Higress</strong>：基于 Envoy，性能同样出色，与云原生生态结合紧密</li><li><strong>Spring Cloud Gateway</strong>：基于 Netty，性能良好，但在极限 QPS 场景下与 C++ 编写的 Nginx&#x2F;Envoy 存在一定差距</li></ul><p><strong>结论</strong>：对性能要求较高的场景，Nginx 或 Envoy 方案更有优势。</p><h3 id="维度二：服务发现与配置管理"><a href="#维度二：服务发现与配置管理" class="headerlink" title="维度二：服务发现与配置管理"></a>维度二：服务发现与配置管理</h3><ul><li><strong>Spring Cloud Gateway &#x2F; APISIX &#x2F; Higress</strong>：与 Nacos 集成良好，支持动态发现配置热更新</li><li><strong>Kong</strong>：DB-less 模式下，服务发现和配置更新依赖第三方插件，动态性相对弱</li></ul><p><strong>结论</strong>：动态性是我们的刚需，Kong 在这个维度不占优。</p><h3 id="维度三：插件开发与团队匹配度"><a href="#维度三：插件开发与团队匹配度" class="headerlink" title="维度三：插件开发与团队匹配度"></a>维度三：插件开发与团队匹配度</h3><p>这是最关键的维度，直接关系到开发效率和维护成本。</p><p><strong>Spring Cloud Gateway</strong>：</p><ul><li>对 Java 团队最自然的选择</li><li>使用 Spring Filter 模式编写插件，可以复用 Java 生态的所有工具</li><li>权衡：为了开发便利性，需要接受相对 Nginx&#x2F;Envoy 的性能差距</li></ul><p><strong>Kong &#x2F; APISIX</strong>：</p><ul><li>插件使用 Lua 开发</li><li>对纯 Java 团队意味着引入新的语言栈，存在学习成本</li><li>但在 AI 辅助编程工具（如 GitHub Copilot、Cursor 等）的加持下，语言学习成本已大幅降低</li><li>关键是理解插件的生命周期和 API，具体语法实现可以借助 AI 快速完成</li></ul><p><strong>Higress</strong>：</p><ul><li>插件模型基于 Wasm</li><li>技术前景好（多语言、安全沙箱），但工具链和生态还在发展，落地存在不确定性</li></ul><h2 id="决策"><a href="#决策" class="headerlink" title="决策"></a>决策</h2><p>我们最终选择了 <strong>Apache APISIX</strong>。</p><p>理由：</p><ol><li>满足性能要求（Nginx 内核）</li><li>与 Nacos 集成成熟，支持动态配置</li><li>插件生态完善，社区活跃</li><li>在 AI 辅助编程的加持下，Lua 开发成本可接受，不必因此放弃 Nginx 的高性能优势</li></ol><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这次选型的几点体会：</p><ol><li><strong>先明确需求再比较</strong>：定义好自己的”标尺”，避免被技术热度左右</li><li><strong>团队技能栈是重要考量，但不是绝对约束</strong>：在 AI 辅助编程工具普及的今天，跨语言开发的学习成本已大幅降低</li><li><strong>关注”折衷”方案</strong>：像 APISIX 这样兼顾高性能底层和良好扩展性的方案，在特定场景下很实用</li></ol><p>API 网关选型本质上是在多个维度之间做权衡：</p><ul><li><strong>性能</strong> vs <strong>开发效率</strong>：纯 Java 方案（SC Gateway）开发体验好但性能受限；Nginx 方案（Kong&#x2F;APISIX）性能优异但需要学习 Lua，不过 AI 工具可以显著降低语言学习门槛</li><li><strong>稳定性</strong> vs <strong>先进性</strong>：传统方案经过生产验证，新方案（Higress 的 Wasm）可能带来技术红利但也伴随不确定性</li><li><strong>团队现状</strong> vs <strong>技术趋势</strong>：在 AI 辅助编程的加持下，不必过度纠结于技术栈的语言差异，更重要的是选择社区成熟、生态完善的方案</li></ul><p>每个项目的需求和团队背景不同，选型没有标准答案。以上是我们的实际决策路径，供参考。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul><li><a href="https://apisix.apache.org/">Apache APISIX 官方文档</a></li><li><a href="https://spring.io/projects/spring-cloud-gateway">Spring Cloud Gateway 文档</a></li><li><a href="https://docs.konghq.com/">Kong 官方文档</a></li><li><a href="https://higress.io/">Higress 官方文档</a></li></ul>]]></content>
    
    
    <summary type="html">记录一个 Java 技术团队在微服务架构中的网关选型过程，对比 Kong、APISIX、Spring Cloud Gateway、Higress 等主流方案。在 AI 辅助编程的加持下，最终选择基于 Nginx 的 APISIX，兼顾性能与扩展性。</summary>
    
    
    
    <category term="微服务" scheme="https://www.yzhu.name/categories/%E5%BE%AE%E6%9C%8D%E5%8A%A1/"/>
    
    <category term="架构设计" scheme="https://www.yzhu.name/categories/%E5%BE%AE%E6%9C%8D%E5%8A%A1/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    
    
    <category term="APISIX" scheme="https://www.yzhu.name/tags/APISIX/"/>
    
    <category term="API Gateway" scheme="https://www.yzhu.name/tags/API-Gateway/"/>
    
    <category term="技术选型" scheme="https://www.yzhu.name/tags/%E6%8A%80%E6%9C%AF%E9%80%89%E5%9E%8B/"/>
    
    <category term="Java" scheme="https://www.yzhu.name/tags/Java/"/>
    
    <category term="Kong" scheme="https://www.yzhu.name/tags/Kong/"/>
    
    <category term="Spring Cloud Gateway" scheme="https://www.yzhu.name/tags/Spring-Cloud-Gateway/"/>
    
  </entry>
  
  <entry>
    <title>Multi-Identifier Unified Login &amp; Social Binding: Building a Scalable Authentication System</title>
    <link href="https://www.yzhu.name/2026/02/07/unified-login-multi-identifier-social-binding-en/"/>
    <id>https://www.yzhu.name/2026/02/07/unified-login-multi-identifier-social-binding-en/</id>
    <published>2026-02-07T04:00:00.000Z</published>
    <updated>2026-03-01T17:36:49.995Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/02/07/unified-login-multi-identifier-social-binding/">中文版</a></p></blockquote><p>As products evolve, login options multiply: username, email, phone number, plus Google, GitHub, WeChat, and enterprise SSO. Traditional “wide table” user designs face a dilemma: constantly adding fields leads to bloated schemas, or limiting login methods sacrifices user experience. This article explores a domain model that separates <strong>AuthIdentity from User</strong> to build an extensible, unified authentication system.</p><span id="more"></span><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><ul><li><strong>Separate concerns</strong>: Split “who the user is” (User) from “how to prove it” (AuthIdentity)</li><li><strong>Zero schema changes</strong>: Adding new login methods only requires INSERT, not ALTER TABLE</li><li><strong>Flexible progression</strong>: Support “passwordless” users who add credentials over time</li><li><strong>Natural conflict prevention</strong>: Composite unique index <code>(identity_type, identifier)</code> prevents duplicate bindings</li></ul><hr><h2 id="1-From-“Wide-Table”-to-“Separation-Model”"><a href="#1-From-“Wide-Table”-to-“Separation-Model”" class="headerlink" title="1. From “Wide Table” to “Separation Model”"></a>1. From “Wide Table” to “Separation Model”</h2><h3 id="1-1-The-Pain-of-Traditional-Design"><a href="#1-1-The-Pain-of-Traditional-Design" class="headerlink" title="1.1 The Pain of Traditional Design"></a>1.1 The Pain of Traditional Design</h3><p>Early user tables typically looked like this:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> users (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    username <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">UNIQUE</span>,</span><br><span class="line">    email <span class="type">VARCHAR</span>(<span class="number">100</span>) <span class="keyword">UNIQUE</span>,</span><br><span class="line">    mobile <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">UNIQUE</span>,</span><br><span class="line">    password_hash <span class="type">VARCHAR</span>(<span class="number">255</span>),</span><br><span class="line">    google_id <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">    wechat_openid <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">    github_id <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">    <span class="comment">-- Add a new column for every login method</span></span><br><span class="line">    created_at <span class="type">TIMESTAMP</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>The problems are obvious:</p><ul><li><strong>Poor extensibility</strong>: Adding login methods requires schema changes—risky for large production tables</li><li><strong>Tight coupling</strong>: Password field is mandatory for all users, blocking “SMS-only” or “social-only” scenarios</li><li><strong>Chaotic uniqueness</strong>: <code>username</code>, <code>email</code>, <code>mobile</code> each have unique constraints, but social IDs scatter across different fields</li><li><strong>Data redundancy</strong>: Passwordless users have empty <code>password_hash</code>; users without email have empty <code>email</code></li></ul><h3 id="1-2-Core-Idea-of-the-Separation-Model"><a href="#1-2-Core-Idea-of-the-Separation-Model" class="headerlink" title="1.2 Core Idea of the Separation Model"></a>1.2 Core Idea of the Separation Model</h3><p>Completely decouple “who the user is” (User) from “how to prove you’re you” (AuthIdentity):</p><ul><li><strong>User</strong>: Only cares about basic user info, not login methods</li><li><strong>AuthIdentity</strong>: Only cares about authentication credentials, not other user data</li><li><strong>UserPassword</strong>: Password as an independent entity, supporting “passwordless user” scenarios</li></ul><p>The immediate benefit: <strong>Adding a new login method only requires inserting one AuthIdentity record—no schema changes needed</strong>.</p><hr><h2 id="2-Domain-Model-Design"><a href="#2-Domain-Model-Design" class="headerlink" title="2. Domain Model Design"></a>2. Domain Model Design</h2><h3 id="2-1-Core-Entity-Relationships"><a href="#2-1-Core-Entity-Relationships" class="headerlink" title="2.1 Core Entity Relationships"></a>2.1 Core Entity Relationships</h3><pre class="mermaid">classDiagram    class User {        +Long id        +String nickname        +String avatar        +String status        +datetime created_at    }    class AuthIdentity {        +Long id        +Long user_id        +String identity_type        +String identifier        +datetime verified_at    }    class UserPassword {        +Long user_id        +String password_hash    }    User "1" --> "n" AuthIdentity : has    User "1" --> "0..1" UserPassword : has</pre><h3 id="2-2-Entity-Responsibility-Boundaries"><a href="#2-2-Entity-Responsibility-Boundaries" class="headerlink" title="2.2 Entity Responsibility Boundaries"></a>2.2 Entity Responsibility Boundaries</h3><h4 id="User-The-User-Aggregate-Root"><a href="#User-The-User-Aggregate-Root" class="headerlink" title="User: The User Aggregate Root"></a>User: The User Aggregate Root</h4><p>The User entity stores only <strong>business-agnostic basic information</strong>:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line">    <span class="keyword">private</span> String nickname;</span><br><span class="line">    <span class="keyword">private</span> String avatar;</span><br><span class="line">    <span class="keyword">private</span> UserStatus status; <span class="comment">// ACTIVE, SUSPENDED, etc.</span></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime createdAt;</span><br><span class="line">    <span class="comment">// Note: no password, email, or mobile fields</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Design Intent</strong>: The User table should be stable. Regardless of how many login methods are supported later, this table never needs changes. It answers “does this user exist?” not “how does this user log in?”</p><h4 id="AuthIdentity-Authentication-Identity"><a href="#AuthIdentity-Authentication-Identity" class="headerlink" title="AuthIdentity: Authentication Identity"></a>AuthIdentity: Authentication Identity</h4><p>AuthIdentity records <strong>all login credentials</strong> a user possesses—one record per login method:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AuthIdentity</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line">    <span class="keyword">private</span> Long userId;          <span class="comment">// Links to User</span></span><br><span class="line">    <span class="keyword">private</span> String identityType;  <span class="comment">// &quot;email&quot;, &quot;mobile&quot;, &quot;username&quot;, &quot;google&quot;, &quot;wechat&quot;</span></span><br><span class="line">    <span class="keyword">private</span> String identifier;    <span class="comment">// The actual value: email address, phone, username, OpenID</span></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime verifiedAt;     <span class="comment">// Verification time (for email/phone)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Key Design Points</strong>:</p><ul><li><strong>Composite unique index</strong>: <code>(identity_type, identifier)</code> ensures one email can’t bind to two accounts</li><li><strong>Flexible identifier</strong>: <code>13800138000</code> can be a mobile-type identifier or a username-type identifier—no conflict</li><li><strong>Minimal core fields</strong>: AuthIdentity stores only the minimum info needed to locate users; extended data can be stored separately</li></ul><h4 id="UserPassword-Independent-Password-Entity"><a href="#UserPassword-Independent-Password-Entity" class="headerlink" title="UserPassword: Independent Password Entity"></a>UserPassword: Independent Password Entity</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserPassword</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long userId;          <span class="comment">// One-to-one with User</span></span><br><span class="line">    <span class="keyword">private</span> String passwordHash;  <span class="comment">// Argon2id hash</span></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime updatedAt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Why a separate table?</strong></p><ol><li><strong>Support passwordless users</strong>: Users with only SMS or social login have no UserPassword record</li><li><strong>Independent password policy</strong>: Can apply stronger security controls (stricter access control, audit logs)</li><li><strong>Password history</strong>: If you need “can’t reuse last 5 passwords” policy, can extend to one-to-many relationship</li></ol><hr><h2 id="3-Model-in-Action"><a href="#3-Model-in-Action" class="headerlink" title="3. Model in Action"></a>3. Model in Action</h2><h3 id="3-1-Scenario-1-User-Registration"><a href="#3-1-Scenario-1-User-Registration" class="headerlink" title="3.1 Scenario 1: User Registration"></a>3.1 Scenario 1: User Registration</h3><p><strong>Email registration</strong>:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 1. Create User</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> users (id, nickname, status, created_at) </span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;John Doe&#x27;</span>, <span class="string">&#x27;ACTIVE&#x27;</span>, NOW());</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. Create AuthIdentity (email)</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier, verified_at)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;email&#x27;</span>, <span class="string">&#x27;john@example.com&#x27;</span>, NOW());</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 3. Create UserPassword</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_passwords (user_id, password_hash)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;$argon2id$v=19$m=65536,t=3,p=4$...&#x27;</span>);</span><br></pre></td></tr></table></figure><p><strong>WeChat one-click registration</strong> (passwordless user):</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 1. Create User</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> users (id, nickname, avatar, status, created_at) </span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">2</span>, <span class="string">&#x27;WeChat User&#x27;</span>, <span class="string">&#x27;https://wx.qq.com/avatar.jpg&#x27;</span>, <span class="string">&#x27;ACTIVE&#x27;</span>, NOW());</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. Create AuthIdentity (wechat)</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">2</span>, <span class="string">&#x27;wechat&#x27;</span>, <span class="string">&#x27;oABCD1234567890&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- <span class="doctag">Note:</span> no user_passwords record</span></span><br></pre></td></tr></table></figure><h3 id="3-2-Scenario-2-Adding-Login-Methods"><a href="#3-2-Scenario-2-Adding-Login-Methods" class="headerlink" title="3.2 Scenario 2: Adding Login Methods"></a>3.2 Scenario 2: Adding Login Methods</h3><p>User originally logs in with email, now wants to bind mobile:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Just insert one record—no changes to users table</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier, verified_at)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;mobile&#x27;</span>, <span class="string">&#x27;+1234567890&#x27;</span>, NOW());</span><br></pre></td></tr></table></figure><p>User wants to also bind GitHub:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;github&#x27;</span>, <span class="string">&#x27;12345678&#x27;</span>);</span><br></pre></td></tr></table></figure><p><strong>Feel the extensibility</strong>: Each new login method is just one INSERT. In system code, it’s just adding one enum value for <code>identity_type</code>.</p><h3 id="3-3-Scenario-3-Login-Flow-Collaboration"><a href="#3-3-Scenario-3-Login-Flow-Collaboration" class="headerlink" title="3.3 Scenario 3: Login Flow Collaboration"></a>3.3 Scenario 3: Login Flow Collaboration</h3><p>User enters <code>john@example.com</code> + password to log in:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> LoginResult <span class="title function_">login</span><span class="params">(String input, String password)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. Find AuthIdentity</span></span><br><span class="line">    <span class="type">AuthIdentity</span> <span class="variable">identity</span> <span class="operator">=</span> authIdentityRepo</span><br><span class="line">        .findByTypeAndIdentifier(<span class="string">&quot;email&quot;</span>, input)</span><br><span class="line">        .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">IdentityNotFoundException</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. Find associated User</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userRepo.findById(identity.getUserId())</span><br><span class="line">        .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">UserNotFoundException</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. Verify password (from independent UserPassword table)</span></span><br><span class="line">    <span class="type">UserPassword</span> <span class="variable">userPwd</span> <span class="operator">=</span> userPasswordRepo.findByUserId(user.getId())</span><br><span class="line">        .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">PasswordNotSetException</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!passwordEncoder.matches(password, userPwd.getPasswordHash())) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InvalidPasswordException</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 4. Issue token</span></span><br><span class="line">    <span class="keyword">return</span> issueToken(user);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Note the query path</strong>: AuthIdentity → User → UserPassword. Each entity has its own responsibility.</p><h3 id="3-4-Scenario-4-Social-Account-Binding-Conflict"><a href="#3-4-Scenario-4-Social-Account-Binding-Conflict" class="headerlink" title="3.4 Scenario 4: Social Account Binding Conflict"></a>3.4 Scenario 4: Social Account Binding Conflict</h3><p>User A is logged in, tries to bind a WeChat already bound to User B:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">bindSocialAccount</span><span class="params">(Long currentUserId, String wechatOpenid)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. Check if this WeChat is already bound</span></span><br><span class="line">    Optional&lt;AuthIdentity&gt; existing = authIdentityRepo</span><br><span class="line">        .findByTypeAndIdentifier(<span class="string">&quot;wechat&quot;</span>, wechatOpenid);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (existing.isPresent()) &#123;</span><br><span class="line">        <span class="type">Long</span> <span class="variable">boundUserId</span> <span class="operator">=</span> existing.get().getUserId();</span><br><span class="line">        <span class="keyword">if</span> (!boundUserId.equals(currentUserId)) &#123;</span><br><span class="line">            <span class="comment">// Already bound to another user—reject</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IdentityAlreadyBoundException</span>(</span><br><span class="line">                <span class="string">&quot;This WeChat is already bound to user &quot;</span> + boundUserId);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// Already bound to current user—idempotent return</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. Create new AuthIdentity</span></span><br><span class="line">    <span class="type">AuthIdentity</span> <span class="variable">newIdentity</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AuthIdentity</span>();</span><br><span class="line">    newIdentity.setUserId(currentUserId);</span><br><span class="line">    newIdentity.setIdentityType(<span class="string">&quot;wechat&quot;</span>);</span><br><span class="line">    newIdentity.setIdentifier(wechatOpenid);</span><br><span class="line">    authIdentityRepo.save(newIdentity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Key Point</strong>: The <code>(identity_type, identifier)</code> unique constraint naturally prevents duplicate bindings. Conflict handling just needs simple logic.</p><hr><h2 id="4-Architectural-Advantages"><a href="#4-Architectural-Advantages" class="headerlink" title="4. Architectural Advantages"></a>4. Architectural Advantages</h2><h3 id="4-1-Extensibility-Zero-Schema-Changes-for-New-Login-Methods"><a href="#4-1-Extensibility-Zero-Schema-Changes-for-New-Login-Methods" class="headerlink" title="4.1 Extensibility: Zero Schema Changes for New Login Methods"></a>4.1 Extensibility: Zero Schema Changes for New Login Methods</h3><p>Compare with traditional design:</p><table><thead><tr><th>Approach</th><th>Cost of Adding Login Method</th></tr></thead><tbody><tr><td>Wide Table</td><td>ALTER TABLE to add column—high risk for large tables, requires gradual rollout</td></tr><tr><td>Separation Model</td><td>INSERT one record, add enum value in code—pure business logic change</td></tr></tbody></table><h3 id="4-2-Flexibility-Support-“Progressive”-Authentication"><a href="#4-2-Flexibility-Support-“Progressive”-Authentication" class="headerlink" title="4.2 Flexibility: Support “Progressive” Authentication"></a>4.2 Flexibility: Support “Progressive” Authentication</h3><p>Users can start with a “passwordless” account (social login only), then add password and bind mobile later:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Initial state: only WeChat login</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> auth_identities <span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- user_id | identity_type | identifier</span></span><br><span class="line"><span class="comment">-- 1       | wechat        | oABCD123...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- Later bind mobile</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities ... (<span class="string">&#x27;mobile&#x27;</span>, <span class="string">&#x27;+1234567890&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Later set password</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_passwords ... (<span class="number">1</span>, <span class="string">&#x27;password_hash&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Final state: three login options</span></span><br><span class="line"><span class="keyword">SELECT</span> identity_type, identifier <span class="keyword">FROM</span> auth_identities <span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- wechat | oABCD123...</span></span><br><span class="line"><span class="comment">-- mobile | +1234567890</span></span><br></pre></td></tr></table></figure><h3 id="4-3-Data-Consistency-Unbinding-Doesn’t-Lose-User-Data"><a href="#4-3-Data-Consistency-Unbinding-Doesn’t-Lose-User-Data" class="headerlink" title="4.3 Data Consistency: Unbinding Doesn’t Lose User Data"></a>4.3 Data Consistency: Unbinding Doesn’t Lose User Data</h3><p>Unbinding mobile just deletes one AuthIdentity record:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> auth_identities </span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">AND</span> identity_type <span class="operator">=</span> <span class="string">&#x27;mobile&#x27;</span> <span class="keyword">AND</span> identifier <span class="operator">=</span> <span class="string">&#x27;+1234567890&#x27;</span>;</span><br></pre></td></tr></table></figure><p>The User entity is unaffected—user’s orders, posts, comments remain. As long as at least one AuthIdentity remains, the user can still log in.</p><hr><h2 id="5-Implementation-Notes"><a href="#5-Implementation-Notes" class="headerlink" title="5. Implementation Notes"></a>5. Implementation Notes</h2><h3 id="5-1-Index-Design"><a href="#5-1-Index-Design" class="headerlink" title="5.1 Index Design"></a>5.1 Index Design</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Core query: find user by login method</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> INDEX idx_auth_identity_type_identifier </span><br><span class="line"><span class="keyword">ON</span> auth_identities(identity_type, identifier);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Admin scenario: view all user&#x27;s login methods</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_auth_identity_user_id </span><br><span class="line"><span class="keyword">ON</span> auth_identities(user_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Password query</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> INDEX idx_user_password_user_id </span><br><span class="line"><span class="keyword">ON</span> user_passwords(user_id);</span><br></pre></td></tr></table></figure><h3 id="5-2-Transaction-Boundaries"><a href="#5-2-Transaction-Boundaries" class="headerlink" title="5.2 Transaction Boundaries"></a>5.2 Transaction Boundaries</h3><p>Creating a user requires consistency across three tables:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">createUserWithEmail</span><span class="params">(String email, String password)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. Create User</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userRepo.save(<span class="keyword">new</span> <span class="title class_">User</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. Create AuthIdentity</span></span><br><span class="line">    authIdentityRepo.save(<span class="keyword">new</span> <span class="title class_">AuthIdentity</span>(user.getId(), <span class="string">&quot;email&quot;</span>, email));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. Create UserPassword</span></span><br><span class="line">    userPasswordRepo.save(<span class="keyword">new</span> <span class="title class_">UserPassword</span>(user.getId(), hash(password)));</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-3-Security-Considerations"><a href="#5-3-Security-Considerations" class="headerlink" title="5.3 Security Considerations"></a>5.3 Security Considerations</h3><ul><li><strong>Verification codes</strong>: Used to verify phone&#x2F;email ownership; mark <code>verified_at</code> after verification</li><li><strong>Password policy</strong>: Use Argon2id, recommended parameters <code>m=65536, t=3, p=4</code></li><li><strong>Token storage</strong>: Social account AccessTokens should be stored separately and encrypted</li><li><strong>Brute force protection</strong>: Rate limit by <code>identifier</code> + <code>identity_type</code> dimension, not just IP</li></ul><hr><h2 id="6-Comparison-When-to-Use-This-Model"><a href="#6-Comparison-When-to-Use-This-Model" class="headerlink" title="6. Comparison: When to Use This Model"></a>6. Comparison: When to Use This Model</h2><table><thead><tr><th>Scenario</th><th>Wide Table</th><th>Separation Model</th></tr></thead><tbody><tr><td>Only username + password</td><td>✅ Simple</td><td>Overkill</td></tr><tr><td>Username + email + mobile</td><td>⚠️ Manageable</td><td>✅ Better</td></tr><tr><td>Multiple social logins + SSO</td><td>❌ Painful</td><td>✅ Essential</td></tr><tr><td>Need passwordless option</td><td>❌ Hard</td><td>✅ Natural</td></tr><tr><td>Frequent login method changes</td><td>❌ Schema migrations</td><td>✅ Just INSERT</td></tr></tbody></table><hr><h2 id="7-Key-Takeaways"><a href="#7-Key-Takeaways" class="headerlink" title="7. Key Takeaways"></a>7. Key Takeaways</h2><p>The core value of separating AuthIdentity from User: <strong>decouple “identity” from “authentication method”</strong>.</p><p>This design enables systems to:</p><ol><li><strong>Scale flexibly</strong>: Add login methods with just records, no schema changes</li><li><strong>Evolve progressively</strong>: Support users moving from “social login” to “multi-factor authentication”</li><li><strong>Protect data</strong>: Unbinding login methods doesn’t lose user business data</li><li><strong>Maintain clarity</strong>: Each entity has single responsibility, code is easier to maintain</li></ol><p>When your product evolves from “username&#x2F;password only” to “supporting phone, email, WeChat, GitHub, enterprise SSO”, this separation model saves you from painful schema migrations and makes authentication a truly evolvable business capability.</p><hr><h2 id="Related-Resources"><a href="#Related-Resources" class="headerlink" title="Related Resources"></a>Related Resources</h2><ul><li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html">OWASP Authentication Cheat Sheet</a></li><li><a href="https://pages.nist.gov/800-63-3/">NIST Digital Identity Guidelines</a></li><li><a href="https://github.com/P-H-C/phc-winner-argon2">Argon2 Password Hashing</a></li></ul>]]></content>
    
    
    <summary type="html">Traditional &#39;wide table&#39; user designs struggle with diverse login methods. Learn how separating AuthIdentity from User entities enables flexible, scalable authentication supporting email, mobile, social logins, and SSO without schema migrations.</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="identity-management" scheme="https://www.yzhu.name/tags/identity-management/"/>
    
    <category term="authentication" scheme="https://www.yzhu.name/tags/authentication/"/>
    
    <category term="security" scheme="https://www.yzhu.name/tags/security/"/>
    
    <category term="oauth" scheme="https://www.yzhu.name/tags/oauth/"/>
    
  </entry>
  
  <entry>
    <title>多标识符统一登录与社交绑定：构建安全可扩展的认证体系</title>
    <link href="https://www.yzhu.name/2026/02/07/unified-login-multi-identifier-social-binding/"/>
    <id>https://www.yzhu.name/2026/02/07/unified-login-multi-identifier-social-binding/</id>
    <published>2026-02-07T04:00:00.000Z</published>
    <updated>2026-02-07T04:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <a href="/2026/02/07/unified-login-multi-identifier-social-binding-en/">English Version</a> | <strong>中文版</strong></p></blockquote><p>随着业务发展，登录入口逐渐增多：用户名、邮箱、手机号，以及 Google、GitHub、微信等社交账号。传统的”大宽表”用户设计在面对这种多样性时，往往陷入两难：要么不断添加字段导致表结构臃肿，要么限制登录方式失去灵活性。本文将探讨一种通过 <strong>AuthIdentity 与 User 分离</strong> 的领域模型，来构建可扩展的统一认证体系。<span id="more"></span></p><h2 id="一、从”大宽表”到”分离模型”的演进"><a href="#一、从”大宽表”到”分离模型”的演进" class="headerlink" title="一、从”大宽表”到”分离模型”的演进"></a>一、从”大宽表”到”分离模型”的演进</h2><h3 id="1-1-传统设计的困境"><a href="#1-1-传统设计的困境" class="headerlink" title="1.1 传统设计的困境"></a>1.1 传统设计的困境</h3><p>早期的用户表通常是这样的：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> users (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    username <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">UNIQUE</span>,</span><br><span class="line">    email <span class="type">VARCHAR</span>(<span class="number">100</span>) <span class="keyword">UNIQUE</span>,</span><br><span class="line">    mobile <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">UNIQUE</span>,</span><br><span class="line">    password_hash <span class="type">VARCHAR</span>(<span class="number">255</span>),</span><br><span class="line">    google_id <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">    wechat_openid <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">    github_id <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">    <span class="comment">-- 每新增一种登录方式，就要加字段</span></span><br><span class="line">    created_at <span class="type">TIMESTAMP</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>这种设计的痛点显而易见：</p><ul><li><strong>扩展困难</strong>：新增登录方式需要修改表结构，线上大表变更风险高</li><li><strong>逻辑耦合</strong>：密码字段对所有用户都是必填的，无法支持”仅短信登录”或”仅社交登录”的用户</li><li><strong>唯一性混乱</strong>：<code>username</code>、<code>email</code>、<code>mobile</code> 各自有唯一约束，但社交账号的 ID 散落在不同字段，难以统一管理</li><li><strong>数据冗余</strong>：不用密码的用户， <code>password_hash</code> 字段为空；不用邮箱的用户，<code>email</code> 字段为空</li></ul><h3 id="1-2-分离模型的核心思想"><a href="#1-2-分离模型的核心思想" class="headerlink" title="1.2 分离模型的核心思想"></a>1.2 分离模型的核心思想</h3><p>将”用户是谁”（User）与”如何证明你是你”（AuthIdentity）彻底解耦：</p><ul><li><strong>User</strong>：只关心用户的基础信息，不关心怎么登录</li><li><strong>AuthIdentity</strong>：只关心认证凭证，不关心用户其他信息</li><li><strong>UserPassword</strong>：密码作为独立实体，支持”无密码用户”场景</li></ul><p>这种分离带来的直接好处：<strong>新增一种登录方式，只需要插入一条 AuthIdentity 记录，无需修改任何表结构</strong>。</p><hr><h2 id="二、领域模型设计"><a href="#二、领域模型设计" class="headerlink" title="二、领域模型设计"></a>二、领域模型设计</h2><h3 id="2-1-核心实体关系"><a href="#2-1-核心实体关系" class="headerlink" title="2.1 核心实体关系"></a>2.1 核心实体关系</h3><pre class="mermaid">classDiagram    class User {        +Long id        +String nickname        +String avatar        +String status        +datetime created_at    }    class AuthIdentity {        +Long id        +Long user_id        +String identity_type        +String identifier        +datetime verified_at    }    class UserPassword {        +Long user_id        +String password_hash    }    User "1" --> "n" AuthIdentity : has    User "1" --> "0..1" UserPassword : has</pre><h3 id="2-2-实体职责边界"><a href="#2-2-实体职责边界" class="headerlink" title="2.2 实体职责边界"></a>2.2 实体职责边界</h3><h4 id="User：用户聚合根"><a href="#User：用户聚合根" class="headerlink" title="User：用户聚合根"></a>User：用户聚合根</h4><p>User 实体只存储<strong>业务无关的基础信息</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line">    <span class="keyword">private</span> String nickname;</span><br><span class="line">    <span class="keyword">private</span> String avatar;</span><br><span class="line">    <span class="keyword">private</span> UserStatus status; <span class="comment">// ACTIVE, SUSPENDED, etc.</span></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime createdAt;</span><br><span class="line">    <span class="comment">// 注意：没有 password, email, mobile 字段</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>设计意图</strong>：User 表应该足够稳定。无论后面支持多少种登录方式，这张表都不需要改动。它回答的是”这个用户存在吗”，而不是”这个用户怎么登录”。</p><h4 id="AuthIdentity：认证标识"><a href="#AuthIdentity：认证标识" class="headerlink" title="AuthIdentity：认证标识"></a>AuthIdentity：认证标识</h4><p>AuthIdentity 记录用户拥有的<strong>所有登录凭证</strong>，一条记录对应一种登录方式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AuthIdentity</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line">    <span class="keyword">private</span> Long userId;          <span class="comment">// 关联到 User</span></span><br><span class="line">    <span class="keyword">private</span> String identityType;  <span class="comment">// &quot;email&quot;, &quot;mobile&quot;, &quot;username&quot;, &quot;google&quot;, &quot;wechat&quot;</span></span><br><span class="line">    <span class="keyword">private</span> String identifier;    <span class="comment">// 具体的标识值：邮箱地址、手机号、用户名、OpenID</span></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime verifiedAt;     <span class="comment">// 验证时间（邮箱/手机号需要验证）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>关键设计</strong>：</p><ul><li><strong>复合唯一索引</strong>：<code>(identity_type, identifier)</code> 确保同一个邮箱不能绑定两个账号</li><li><strong>灵活的 identifier</strong>：<code>13800138000</code> 可以是手机号类型的标识符，也可以是用户名类型的标识符，互不冲突</li><li><strong>核心字段精简</strong>：AuthIdentity 只存储定位用户所需的最小信息，其他扩展数据可单独存储</li></ul><h4 id="UserPassword：独立的密码实体"><a href="#UserPassword：独立的密码实体" class="headerlink" title="UserPassword：独立的密码实体"></a>UserPassword：独立的密码实体</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserPassword</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long userId;          <span class="comment">// 与 User 一对一</span></span><br><span class="line">    <span class="keyword">private</span> String passwordHash;  <span class="comment">// Argon2id 哈希值</span></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime updatedAt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>为什么要单独一张表？</strong></p><ol><li><strong>支持无密码用户</strong>：只用短信验证码或社交登录的用户，没有 UserPassword 记录</li><li><strong>密码策略独立</strong>：可以单独对密码表做安全加固（如更严格的访问控制、审计日志）</li><li><strong>历史密码管理</strong>：如果需要”不能重复使用最近 5 次密码”的策略，可以扩展为一对多关系</li></ol><hr><h2 id="三、模型在场景中的体现"><a href="#三、模型在场景中的体现" class="headerlink" title="三、模型在场景中的体现"></a>三、模型在场景中的体现</h2><h3 id="3-1-场景一：用户注册"><a href="#3-1-场景一：用户注册" class="headerlink" title="3.1 场景一：用户注册"></a>3.1 场景一：用户注册</h3><p><strong>用邮箱注册</strong>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 1. 创建 User</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> users (id, nickname, status, created_at) </span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;张三&#x27;</span>, <span class="string">&#x27;ACTIVE&#x27;</span>, NOW());</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. 创建 AuthIdentity (email)</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier, verified_at)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;email&#x27;</span>, <span class="string">&#x27;zhangsan@example.com&#x27;</span>, NOW());</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 3. 创建 UserPassword</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_passwords (user_id, password_hash)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;$argon2id$v=19$m=65536,t=3,p=4$...&#x27;</span>);</span><br></pre></td></tr></table></figure><p><strong>用微信一键注册</strong>（无密码用户）：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 1. 创建 User</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> users (id, nickname, avatar, status, created_at) </span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">2</span>, <span class="string">&#x27;微信用户&#x27;</span>, <span class="string">&#x27;https://wx.qq.com/avatar.jpg&#x27;</span>, <span class="string">&#x27;ACTIVE&#x27;</span>, NOW());</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. 创建 AuthIdentity (wechat)</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">2</span>, <span class="string">&#x27;wechat&#x27;</span>, <span class="string">&#x27;oABCD1234567890&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 注意：没有 user_passwords 记录</span></span><br></pre></td></tr></table></figure><h3 id="3-2-场景二：新增登录方式"><a href="#3-2-场景二：新增登录方式" class="headerlink" title="3.2 场景二：新增登录方式"></a>3.2 场景二：新增登录方式</h3><p>用户原本用邮箱登录，现在想绑定手机号：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 只需插入一条记录，无需修改 users 表</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier, verified_at)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;mobile&#x27;</span>, <span class="string">&#x27;13800138000&#x27;</span>, NOW());</span><br></pre></td></tr></table></figure><p>用户想再绑定 GitHub：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> auth_identities (user_id, identity_type, identifier)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="number">1</span>, <span class="string">&#x27;github&#x27;</span>, <span class="string">&#x27;12345678&#x27;</span>);</span><br></pre></td></tr></table></figure><p><strong>体会一下这种扩展性</strong>：每新增一种登录方式，就是一行 INSERT，系统代码里也只是新增一个 identity_type 的枚举值。</p><h3 id="3-3-场景三：登录时的模型协作"><a href="#3-3-场景三：登录时的模型协作" class="headerlink" title="3.3 场景三：登录时的模型协作"></a>3.3 场景三：登录时的模型协作</h3><p>用户输入 <code>zhangsan@example.com</code> + 密码登录：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> LoginResult <span class="title function_">login</span><span class="params">(String input, String password)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 查找 AuthIdentity</span></span><br><span class="line">    <span class="type">AuthIdentity</span> <span class="variable">identity</span> <span class="operator">=</span> authIdentityRepo</span><br><span class="line">        .findByTypeAndIdentifier(<span class="string">&quot;email&quot;</span>, input)</span><br><span class="line">        .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">IdentityNotFoundException</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 找到关联的 User</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userRepo.findById(identity.getUserId())</span><br><span class="line">        .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">UserNotFoundException</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. 验证密码（从独立的 UserPassword 表）</span></span><br><span class="line">    <span class="type">UserPassword</span> <span class="variable">userPwd</span> <span class="operator">=</span> userPasswordRepo.findByUserId(user.getId())</span><br><span class="line">        .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">PasswordNotSetException</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!passwordEncoder.matches(password, userPwd.getPasswordHash())) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InvalidPasswordException</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 4. 颁发 Token</span></span><br><span class="line">    <span class="keyword">return</span> issueToken(user);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意这里的查询路径</strong>：AuthIdentity → User → UserPassword，三个实体各司其职。</p><h3 id="3-4-场景四：社交账号绑定冲突"><a href="#3-4-场景四：社交账号绑定冲突" class="headerlink" title="3.4 场景四：社交账号绑定冲突"></a>3.4 场景四：社交账号绑定冲突</h3><p>用户 A 已登录，尝试绑定一个已被用户 B 占用的微信：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">bindSocialAccount</span><span class="params">(Long currentUserId, String wechatOpenid)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 检查这个微信是否已被绑定</span></span><br><span class="line">    Optional&lt;AuthIdentity&gt; existing = authIdentityRepo</span><br><span class="line">        .findByTypeAndIdentifier(<span class="string">&quot;wechat&quot;</span>, wechatOpenid);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (existing.isPresent()) &#123;</span><br><span class="line">        <span class="type">Long</span> <span class="variable">boundUserId</span> <span class="operator">=</span> existing.get().getUserId();</span><br><span class="line">        <span class="keyword">if</span> (!boundUserId.equals(currentUserId)) &#123;</span><br><span class="line">            <span class="comment">// 已被其他用户绑定，拒绝</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IdentityAlreadyBoundException</span>(</span><br><span class="line">                <span class="string">&quot;该微信已绑定到用户 &quot;</span> + boundUserId);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 已被当前用户绑定，幂等返回</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 创建新的 AuthIdentity</span></span><br><span class="line">    <span class="type">AuthIdentity</span> <span class="variable">newIdentity</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AuthIdentity</span>();</span><br><span class="line">    newIdentity.setUserId(currentUserId);</span><br><span class="line">    newIdentity.setIdentityType(<span class="string">&quot;wechat&quot;</span>);</span><br><span class="line">    newIdentity.setIdentifier(wechatOpenid);</span><br><span class="line">    authIdentityRepo.save(newIdentity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>关键点</strong>：通过 <code>(identity_type, identifier)</code> 的唯一约束，天然防止了重复绑定。冲突处理只需要简单的判断逻辑。</p><hr><h2 id="四、模型带来的架构优势"><a href="#四、模型带来的架构优势" class="headerlink" title="四、模型带来的架构优势"></a>四、模型带来的架构优势</h2><h3 id="4-1-扩展性：新增登录方式零表结构变更"><a href="#4-1-扩展性：新增登录方式零表结构变更" class="headerlink" title="4.1 扩展性：新增登录方式零表结构变更"></a>4.1 扩展性：新增登录方式零表结构变更</h3><p>对比传统设计：</p><table><thead><tr><th>方案</th><th>新增登录方式的成本</th></tr></thead><tbody><tr><td>大宽表</td><td>ALTER TABLE 添加字段，线上大表风险高，需要灰度发布</td></tr><tr><td>分离模型</td><td>INSERT 一条记录，代码新增枚举值，纯业务逻辑变更</td></tr></tbody></table><h3 id="4-2-灵活性：支持”渐进式”认证"><a href="#4-2-灵活性：支持”渐进式”认证" class="headerlink" title="4.2 灵活性：支持”渐进式”认证"></a>4.2 灵活性：支持”渐进式”认证</h3><p>用户可以先注册一个”无密码”账号（仅社交登录），后续再设置密码、绑定手机号：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 初始状态：只有微信登录</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> auth_identities <span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- user_id | identity_type | identifier</span></span><br><span class="line"><span class="comment">-- 1       | wechat        | oABCD123...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 后续绑定手机号</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> auth_identities ... (<span class="string">&#x27;mobile&#x27;</span>, <span class="string">&#x27;13800138000&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 后续设置密码</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_passwords ... (<span class="number">1</span>, <span class="string">&#x27;password_hash&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 最终状态：三种登录方式任选</span></span><br><span class="line"><span class="keyword">SELECT</span> identity_type, identifier <span class="keyword">FROM</span> auth_identities <span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- wechat | oABCD123...</span></span><br><span class="line"><span class="comment">-- mobile | 13800138000</span></span><br></pre></td></tr></table></figure><h3 id="4-3-数据一致性：账号解绑不会丢失用户数据"><a href="#4-3-数据一致性：账号解绑不会丢失用户数据" class="headerlink" title="4.3 数据一致性：账号解绑不会丢失用户数据"></a>4.3 数据一致性：账号解绑不会丢失用户数据</h3><p>解绑手机号只是删除一条 AuthIdentity 记录：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> auth_identities </span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">AND</span> identity_type <span class="operator">=</span> <span class="string">&#x27;mobile&#x27;</span> <span class="keyword">AND</span> identifier <span class="operator">=</span> <span class="string">&#x27;13800138000&#x27;</span>;</span><br></pre></td></tr></table></figure><p>User 实体不受影响，用户的订单、文章、评论等业务数据都还在。只要保留至少一种 AuthIdentity，用户就能继续登录。</p><hr><h2 id="五、实现时的注意事项"><a href="#五、实现时的注意事项" class="headerlink" title="五、实现时的注意事项"></a>五、实现时的注意事项</h2><h3 id="5-1-索引设计"><a href="#5-1-索引设计" class="headerlink" title="5.1 索引设计"></a>5.1 索引设计</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 核心查询：根据登录方式找用户</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> INDEX idx_auth_identity_type_identifier </span><br><span class="line"><span class="keyword">ON</span> auth_identities(identity_type, identifier);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 管理场景：查看用户的所有登录方式</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_auth_identity_user_id </span><br><span class="line"><span class="keyword">ON</span> auth_identities(user_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 密码查询</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> INDEX idx_user_password_user_id </span><br><span class="line"><span class="keyword">ON</span> user_passwords(user_id);</span><br></pre></td></tr></table></figure><h3 id="5-2-事务边界"><a href="#5-2-事务边界" class="headerlink" title="5.2 事务边界"></a>5.2 事务边界</h3><p>创建用户时需要保证三张表的一致性：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">createUserWithEmail</span><span class="params">(String email, String password)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 创建 User</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userRepo.save(<span class="keyword">new</span> <span class="title class_">User</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 创建 AuthIdentity</span></span><br><span class="line">    authIdentityRepo.save(<span class="keyword">new</span> <span class="title class_">AuthIdentity</span>(user.getId(), <span class="string">&quot;email&quot;</span>, email));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. 创建 UserPassword</span></span><br><span class="line">    userPasswordRepo.save(<span class="keyword">new</span> <span class="title class_">UserPassword</span>(user.getId(), hash(password)));</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-3-安全考量（简要）"><a href="#5-3-安全考量（简要）" class="headerlink" title="5.3 安全考量（简要）"></a>5.3 安全考量（简要）</h3><ul><li><strong>验证码</strong>：用于验证手机号&#x2F;邮箱所有权，验证后应标记 <code>verified_at</code></li><li><strong>密码策略</strong>：使用 Argon2id，参数建议 <code>m=65536, t=3, p=4</code></li><li><strong>Token 存储</strong>：社交账号的 AccessToken 应单独存储并加密，防止泄露</li><li><strong>防暴力破解</strong>：对 <code>identifier</code> + <code>identity_type</code> 维度限流，而非仅 IP</li></ul><hr><h2 id="六、总结"><a href="#六、总结" class="headerlink" title="六、总结"></a>六、总结</h2><p>AuthIdentity 与 User 分离的核心价值在于：<strong>将”身份”与”认证方式”解耦</strong>。</p><p>这种设计让系统可以：</p><ol><li><strong>灵活扩展</strong>：新增登录方式只需添加记录，无需改表结构</li><li><strong>渐进演进</strong>：支持用户从”社交登录”逐步完善到”多因子认证”</li><li><strong>数据安全</strong>：解绑登录方式不会丢失用户业务数据</li><li><strong>逻辑清晰</strong>：每个实体职责单一，代码易于维护</li></ol><p>当你的产品从”仅用户名密码”发展到”支持手机号、邮箱、微信、GitHub、企业 SSO”等多种登录方式时，这种分离模型能帮你避免一次次痛苦的表结构迁移，让认证体系真正成为可演进的业务能力。</p>]]></content>
    
    
    <summary type="html">传统的大宽表用户设计难以应对多样化登录方式。了解如何通过分离 AuthIdentity 与 User 实体，构建支持邮箱、手机号、社交登录和 SSO 的灵活可扩展认证体系，无需修改表结构。</summary>
    
    
    
    <category term="Security" scheme="https://www.yzhu.name/categories/Security/"/>
    
    
    <category term="iam" scheme="https://www.yzhu.name/tags/iam/"/>
    
    <category term="identity-management" scheme="https://www.yzhu.name/tags/identity-management/"/>
    
    <category term="authentication" scheme="https://www.yzhu.name/tags/authentication/"/>
    
    <category term="security" scheme="https://www.yzhu.name/tags/security/"/>
    
    <category term="oauth" scheme="https://www.yzhu.name/tags/oauth/"/>
    
  </entry>
  
  <entry>
    <title>Synchronizing Recurring Outlook Meetings with EspoCRM</title>
    <link href="https://www.yzhu.name/2026/01/10/espocrm/08-outlook-recurring-sync-en/"/>
    <id>https://www.yzhu.name/2026/01/10/espocrm/08-outlook-recurring-sync-en/</id>
    <published>2026-01-10T06:00:00.000Z</published>
    <updated>2026-01-11T01:03:18.298Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>🌐 Language</strong>: <strong>English Version</strong> | <a href="/2026/01/05/espocrm/08-outlook-recurring-sync/">中文版</a></p></blockquote><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><p>If you’re building Outlook Calendar integration with EspoCRM, <strong>recurring meetings will break your standard delta sync logic</strong>. This article presents a production-grade solution using:</p><ul><li><strong>Windowed Expansion</strong>: Only sync instances within <code>[Today - 7d, Today + 90d]</code></li><li><strong>Series Rebuild</strong>: When seriesMaster changes, fetch all instances via the Instances API</li><li><strong>Bootstrap State Machine</strong>: Handle cold-start with explicit initialization scan</li><li><strong>Critical Warning</strong>: Never use <code>iCalUId</code> as a unique key for recurring instances</li></ul><hr><h2 id="The-Problem-Why-Delta-Sync-Fails-for-Recurring-Meetings"><a href="#The-Problem-Why-Delta-Sync-Fails-for-Recurring-Meetings" class="headerlink" title="The Problem: Why Delta Sync Fails for Recurring Meetings"></a>The Problem: Why Delta Sync Fails for Recurring Meetings</h2><p>Most developers assume Microsoft Graph’s Delta Sync API will notify them of every calendar change. This works fine for single events. For recurring meetings, it breaks down.</p><h3 id="Microsoft’s-Data-Model"><a href="#Microsoft’s-Data-Model" class="headerlink" title="Microsoft’s Data Model"></a>Microsoft’s Data Model</h3><p>In Microsoft Graph API, a recurring meeting isn’t a single record. It’s split into three types:</p><table><thead><tr><th>Type</th><th>Description</th><th>Stored in Outlook</th></tr></thead><tbody><tr><td><strong>seriesMaster</strong></td><td>The template defining recurrence rules (e.g., “every Thursday at 2 PM”)</td><td>Physical record</td></tr><tr><td><strong>occurrence</strong></td><td>A specific instance (e.g., “Jan 8, 2026 at 2 PM”)</td><td>Virtual, computed from Master</td></tr><tr><td><strong>exception</strong></td><td>A modified instance (e.g., “Just this one, move to 3 PM”)</td><td>Physical record</td></tr></tbody></table><h3 id="The-Delta-Sync-Trap"><a href="#The-Delta-Sync-Trap" class="headerlink" title="The Delta Sync Trap"></a>The Delta Sync Trap</h3><p>When you modify a seriesMaster (e.g., change “weekly Monday” to “weekly Tuesday”), the Delta API returns only the seriesMaster change. <strong>It does NOT return the 20+ occurrence changes</strong> for future instances.</p><p>Your CRM continues showing meetings on Mondays. Users complain. Debugging reveals no sync errors—the delta data simply never contained those changes.</p><h3 id="Common-Failure-Patterns"><a href="#Common-Failure-Patterns" class="headerlink" title="Common Failure Patterns"></a>Common Failure Patterns</h3><table><thead><tr><th>Symptom</th><th>Root Cause</th></tr></thead><tbody><tr><td>Missing instances</td><td>Only processed seriesMaster, never expanded occurrences</td></tr><tr><td>Duplicate instances</td><td>Used <code>iCalUId</code> as dedupe key (same for entire series)</td></tr><tr><td>Orphaned instances</td><td>Deleted series in Outlook, only Master removed from CRM</td></tr><tr><td>Time drift</td><td>Expanded in UTC without proper timezone handling</td></tr></tbody></table><hr><h2 id="The-Solution-Windowed-Expansion-Series-Rebuild"><a href="#The-Solution-Windowed-Expansion-Series-Rebuild" class="headerlink" title="The Solution: Windowed Expansion + Series Rebuild"></a>The Solution: Windowed Expansion + Series Rebuild</h2><h3 id="Core-Principle-The-Window"><a href="#Core-Principle-The-Window" class="headerlink" title="Core Principle: The Window"></a>Core Principle: The Window</h3><p><strong>Never sync infinite future.</strong> Define a “window of interest” such as <code>[Today - 7d, Today + 90d]</code>.</p><p>This makes any recurring series—even “no end date” ones—produce a constant, bounded number of instances.</p><pre class="mermaid">gantt    title Window Sliding Over Time    dateFormat  YYYY-MM-DD    axisFormat  %m/%d    section Window (90d)    Initial window  :a1, 2026-01-01, 90d    Slid window     :a2, 2026-02-01, 90d    section Series    Weekly instances :crit, 2026-01-01, 180d</pre><h3 id="Architecture-Overview"><a href="#Architecture-Overview" class="headerlink" title="Architecture Overview"></a>Architecture Overview</h3><pre class="mermaid">flowchart TD    Start["Start Sync"] --> GetDelta["Graph API: Delta Sync"]    GetDelta --> Loop{"Process Changes"}    Loop -- "Type=seriesMaster" --> MarkResync["Mark for Rebuild"]    Loop -- "Type=occurrence" --> SyncItem["Sync Instance (No iCalUId Fallback)"]    Loop -- "Type=singleInstance" --> SyncNormal["Standard Sync"]    MarkResync --> TriggerResync["Trigger Series Rebuild"]    subgraph SeriesResync ["Series Rebuild"]        Fetch["Graph API: List Instances (Windowed)"]        Upsert["Bulk Upsert Instances"]        Cleanup["Clean Removed Instances"]    end</pre><hr><h2 id="Implementation-Details"><a href="#Implementation-Details" class="headerlink" title="Implementation Details"></a>Implementation Details</h2><h3 id="1-The-Bootstrap-State-Machine"><a href="#1-The-Bootstrap-State-Machine" class="headerlink" title="1. The Bootstrap State Machine"></a>1. The Bootstrap State Machine</h3><p>Delta Sync is “change-driven,” not “presence-driven.” Meetings created years ago with no recent changes won’t appear in delta results. This causes silent missing data on first sync.</p><p><strong>Solution</strong>: Track bootstrap state per user calendar:</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">OutlookCalendarService</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="variable constant_">STATUS_PENDING</span> = <span class="string">&#x27;Pending&#x27;</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="variable constant_">STATUS_COMPLETED</span> = <span class="string">&#x27;Completed&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">syncUserCalendar</span>(<span class="params"><span class="keyword">string</span> <span class="variable">$userId</span></span>): <span class="title">void</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="variable">$calendar</span> = <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">getCalendar</span>(<span class="variable">$userId</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="variable">$calendar</span>-&gt;<span class="title function_ invoke__">getBootstrapStatus</span>() === <span class="built_in">self</span>::<span class="variable constant_">STATUS_PENDING</span>) &#123;</span><br><span class="line">            <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">runBootstrapScan</span>(<span class="variable">$calendar</span>);</span><br><span class="line">            <span class="variable">$calendar</span>-&gt;<span class="title function_ invoke__">setBootstrapStatus</span>(<span class="built_in">self</span>::<span class="variable constant_">STATUS_COMPLETED</span>);</span><br><span class="line">            <span class="variable language_">$this</span>-&gt;em-&gt;<span class="title function_ invoke__">save</span>(<span class="variable">$calendar</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">runDeltaSync</span>(<span class="variable">$calendar</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">runBootstrapScan</span>(<span class="params">OutlookCalendar <span class="variable">$calendar</span></span>): <span class="title">void</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="variable">$windowStart</span> = <span class="keyword">new</span> <span class="title class_">\DateTime</span>(<span class="string">&#x27;-7 days&#x27;</span>);</span><br><span class="line">        <span class="variable">$windowEnd</span> = <span class="keyword">new</span> <span class="title class_">\DateTime</span>(<span class="string">&#x27;+90 days&#x27;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Get ALL seriesMasters in window, not just changed ones</span></span><br><span class="line">        <span class="variable">$masters</span> = <span class="variable language_">$this</span>-&gt;graphClient-&gt;<span class="title function_ invoke__">getCalendarView</span>(</span><br><span class="line">            <span class="variable">$calendar</span>-&gt;<span class="title function_ invoke__">getExternalAccountId</span>(),</span><br><span class="line">            <span class="variable">$windowStart</span>,</span><br><span class="line">            <span class="variable">$windowEnd</span>,</span><br><span class="line">            [<span class="string">&#x27;$filter&#x27;</span> =&gt; <span class="string">&quot;type eq &#x27;seriesMaster&#x27;&quot;</span>]</span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        <span class="keyword">foreach</span> (<span class="variable">$masters</span> <span class="keyword">as</span> <span class="variable">$master</span>) &#123;</span><br><span class="line">            <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">rebuildSeries</span>(<span class="variable">$master</span>[<span class="string">&#x27;id&#x27;</span>], <span class="variable">$windowStart</span>, <span class="variable">$windowEnd</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-Series-Rebuild-Strategy"><a href="#2-Series-Rebuild-Strategy" class="headerlink" title="2. Series Rebuild Strategy"></a>2. Series Rebuild Strategy</h3><p>When seriesMaster changes, trigger instance rebuild:</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">rebuildSeries</span>(<span class="params"><span class="keyword">string</span> <span class="variable">$masterId</span>, \DateTime <span class="variable">$start</span>, \DateTime <span class="variable">$end</span></span>): <span class="title">void</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// Fetch all instances in window</span></span><br><span class="line">    <span class="variable">$instances</span> = <span class="variable language_">$this</span>-&gt;graphClient-&gt;<span class="title function_ invoke__">getInstances</span>(<span class="variable">$masterId</span>, <span class="variable">$start</span>, <span class="variable">$end</span>);</span><br><span class="line"></span><br><span class="line">    <span class="variable">$apiInstanceIds</span> = <span class="title function_ invoke__">array_column</span>(<span class="variable">$instances</span>, <span class="string">&#x27;id&#x27;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Get existing CRM instances for this series</span></span><br><span class="line">    <span class="variable">$existing</span> = <span class="variable language_">$this</span>-&gt;em-&gt;<span class="title function_ invoke__">getRepository</span>(<span class="title class_">OutlookEvent</span>::<span class="variable language_">class</span>)</span><br><span class="line">        -&gt;<span class="title function_ invoke__">findBy</span>([<span class="string">&#x27;seriesMasterId&#x27;</span> =&gt; <span class="variable">$masterId</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="variable">$existingIds</span> = <span class="title function_ invoke__">array_map</span>(fn(<span class="variable">$e</span>) =&gt; <span class="variable">$e</span>-&gt;<span class="title function_ invoke__">getEventId</span>(), <span class="variable">$existing</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Diff: Create new, Update existing, Delete missing</span></span><br><span class="line">    <span class="keyword">foreach</span> (<span class="variable">$instances</span> <span class="keyword">as</span> <span class="variable">$instance</span>) &#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">syncInstance</span>(<span class="variable">$instance</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Delete instances that disappeared from API (but within window)</span></span><br><span class="line">    <span class="keyword">foreach</span> (<span class="variable">$existing</span> <span class="keyword">as</span> <span class="variable">$event</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="title function_ invoke__">in_array</span>(<span class="variable">$event</span>-&gt;<span class="title function_ invoke__">getEventId</span>(), <span class="variable">$apiInstanceIds</span>)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="variable">$event</span>-&gt;<span class="title function_ invoke__">getStart</span>()-&gt;<span class="title function_ invoke__">between</span>(<span class="variable">$start</span>, <span class="variable">$end</span>)) &#123;</span><br><span class="line">                <span class="variable language_">$this</span>-&gt;em-&gt;<span class="title function_ invoke__">remove</span>(<span class="variable">$event</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-The-iCalUId-Trap-CRITICAL"><a href="#3-The-iCalUId-Trap-CRITICAL" class="headerlink" title="3. The iCalUId Trap (CRITICAL!)"></a>3. The iCalUId Trap (CRITICAL!)</h3><p><strong>RFC 5545 specifies that ALL occurrences in a recurring series share the same UID.</strong></p><p>This means <code>iCalUId</code> is <strong>NOT unique</strong> for recurring instances. Using it for deduplication will merge all your weekly meetings into one record.</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="comment">// WRONG: This will break recurring meetings</span></span><br><span class="line"><span class="variable">$meeting</span> = <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">findByEventId</span>(<span class="variable">$eventId</span>)</span><br><span class="line">    ?? <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">findByICalUId</span>(<span class="variable">$iCalUId</span>);  <span class="comment">// ❌ DO NOT DO THIS</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// CORRECT: Never use iCalUId fallback for recurring instances</span></span><br><span class="line"><span class="keyword">if</span> (<span class="variable">$eventType</span> === <span class="string">&#x27;occurrence&#x27;</span> || <span class="variable">$eventType</span> === <span class="string">&#x27;exception&#x27;</span>) &#123;</span><br><span class="line">    <span class="variable">$meeting</span> = <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">findByEventId</span>(<span class="variable">$eventId</span>);  <span class="comment">// ✅ Use eventId only</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="variable">$meeting</span> = <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">findByEventId</span>(<span class="variable">$eventId</span>)</span><br><span class="line">        ?? <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">findByICalUId</span>(<span class="variable">$iCalUId</span>);  <span class="comment">// ✅ OK for single events</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-Complete-Sync-Sequence"><a href="#4-Complete-Sync-Sequence" class="headerlink" title="4. Complete Sync Sequence"></a>4. Complete Sync Sequence</h3><pre class="mermaid">sequenceDiagram  participant S as Scheduler/Job  participant SS as SyncService  participant DB as Database  participant P as Provider(Outlook/Google)  participant W as WindowExpander  S->>SS: Trigger user sync  SS->>DB: Check bootstrapStatus  alt Status is Pending (Cold Start)    SS->>W: Force Windowed Scan (-7d..+90d)    W->>P: List all instances in window    W->>DB: Initialize Series & Instances    SS->>DB: Update bootstrapStatus = Completed  else Status is Completed (Incremental)    SS->>P: Fetch delta (token/page)    alt Series master changed      SS->>W: Mark series for rebuild      W->>P: List instances (window: -7d..+90d)      W->>DB: Upsert created/updated      W->>DB: Delete window-missing    else Occurrence/Exception changed      SS->>DB: Upsert occurrence (no iCalUId fallback)    end  end  SS-->>S: Report metrics</pre><h3 id="5-Quota-Management"><a href="#5-Quota-Management" class="headerlink" title="5. Quota Management"></a>5. Quota Management</h3><p>Recurring series generate massive record counts. One user with 10 weekly meetings &#x3D; ~120 instances per quarter.</p><p><strong>Protect against API throttling:</strong></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SyncLimits</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="variable constant_">MAX_INSTANCES_PER_RUN</span> = <span class="number">200</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="variable constant_">MAX_SERIES_REBUILD_PER_RUN</span> = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="variable">$instancesProcessed</span> = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="variable">$seriesRebuilt</span> = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">shouldProcessMoreInstances</span>(<span class="params"></span>): <span class="title">bool</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">$this</span>-&gt;instancesProcessed &lt; <span class="built_in">self</span>::<span class="variable constant_">MAX_INSTANCES_PER_RUN</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">shouldRebuildMoreSeries</span>(<span class="params"></span>): <span class="title">bool</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">$this</span>-&gt;seriesRebuilt &lt; <span class="built_in">self</span>::<span class="variable constant_">MAX_SERIES_REBUILD_PER_RUN</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">recordInstance</span>(<span class="params"></span>): <span class="title">void</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;instancesProcessed++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">recordSeriesRebuild</span>(<span class="params"></span>): <span class="title">void</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;seriesRebuilt++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="Production-Checklist"><a href="#Production-Checklist" class="headerlink" title="Production Checklist"></a>Production Checklist</h2><p>Before deploying to production, verify:</p><ul><li><input disabled type="checkbox"> <strong>DST Handling</strong>: Create a weekly meeting spanning DST transition (March&#x2F;November). Verify times don’t drift</li><li><input disabled type="checkbox"> <strong>Exception Preservation</strong>: Modify one instance’s time, then change series subject. Verify the modified instance keeps its time</li><li><input disabled type="checkbox"> <strong>Deletion Sync</strong>: Delete one future instance in Outlook. Confirm CRM removes it. Delete entire series. Confirm all instances are removed</li><li><input disabled type="checkbox"> <strong>Long-running Series</strong>: Create a monthly meeting for 2 years. Verify only ~90 days are synced</li><li><input disabled type="checkbox"> <strong>All-day Events</strong>: Create all-day recurring event. Verify it doesn’t span two days due to timezone conversion</li><li><input disabled type="checkbox"> <strong>Token Recovery</strong>: Simulate delta token expiration. Verify system falls back to full sync</li><li><input disabled type="checkbox"> <strong>Logging</strong>: All sync operations log (seriesMasterId, instanceId, operation) for debugging</li></ul><hr><h2 id="Comparison-Outlook-vs-Google-Calendar"><a href="#Comparison-Outlook-vs-Google-Calendar" class="headerlink" title="Comparison: Outlook vs Google Calendar"></a>Comparison: Outlook vs Google Calendar</h2><table><thead><tr><th>Aspect</th><th>Microsoft Graph (Outlook)</th><th>Google Calendar API</th></tr></thead><tbody><tr><td><strong>Recurring Model</strong></td><td>Master + Exception (occurrences are virtual)</td><td>Event + Recurrence (can expand with singleEvents&#x3D;true)</td></tr><tr><td><strong>Delta Behavior</strong></td><td>Master change returns only Master</td><td>Master change can return all affected instances</td></tr><tr><td><strong>ID Reference</strong></td><td><code>seriesMasterId</code></td><td><code>recurringEventId</code></td></tr><tr><td><strong>Deletion</strong></td><td>Physical delete or status change</td><td>Often <code>status: cancelled</code></td></tr><tr><td><strong>Complexity</strong></td><td>⭐⭐⭐⭐⭐</td><td>⭐⭐⭐</td></tr></tbody></table><p>Google’s <code>singleEvents=true</code> parameter makes expansion easier, but the windowed approach remains necessary for performance.</p><hr><h2 id="Key-Takeaways"><a href="#Key-Takeaways" class="headerlink" title="Key Takeaways"></a>Key Takeaways</h2><ol><li><strong>Never trust Delta Sync alone</strong> for recurring meetings. Use delta as a signal, not as truth.</li><li><strong>Windowed expansion is essential</strong>. Infinite series must be bounded to a constant number of instances.</li><li><strong>iCalUId is half-truth</strong>. Same for all occurrences—dangerous as a unique key.</li><li><strong>Bootstrap state matters</strong>. Explicit initialization scan prevents “cold start” data gaps.</li><li><strong>Log everything</strong>. When debugging sync issues, you’ll need the history.</li></ol><hr><h2 id="Data-Model-Reference"><a href="#Data-Model-Reference" class="headerlink" title="Data Model Reference"></a>Data Model Reference</h2><pre class="mermaid">erDiagram    OUTLOOK_CALENDAR_USER ||--o{ OUTLOOK_CALENDAR_SERIES : tracks    OUTLOOK_CALENDAR_USER ||--o{ OUTLOOK_CALENDAR_EVENT : owns    INTERNAL_EVENT ||--o{ OUTLOOK_CALENDAR_EVENT : linked-by    OUTLOOK_CALENDAR_USER {        string id PK        string deltaToken        string bootstrapStatus    }    OUTLOOK_CALENDAR_SERIES {        string id PK        string seriesMasterEventId        boolean resyncRequested    }    OUTLOOK_CALENDAR_EVENT {        string id PK        string eventId "Immutable Outlook ID"        string iCalUId "Shared by Series - NOT unique!"        string eventType "occurrence/master/exception"        string seriesMasterEventId        datetime windowStart        datetime windowEnd    }    INTERNAL_EVENT {        string id PK        string subject        datetime start        datetime end    }</pre><hr><h2 id="Resources"><a href="#Resources" class="headerlink" title="Resources"></a>Resources</h2><ul><li><a href="https://learn.microsoft.com/en-us/graph/delta-query-events">Microsoft Graph Delta Query Documentation</a></li><li><a href="https://learn.microsoft.com/en-us/graph/delta-query-events">Get incremental changes to events in a calendar view</a></li><li><a href="https://learn.microsoft.com/en-us/graph/best-practices-concept">Best practices for working with Microsoft Graph</a></li><li><a href="https://datatracker.ietf.org/doc/html/rfc5545">RFC 5545: iCalendar Specification</a></li></ul><hr>]]></content>
    
    
    <summary type="html">Recurring meetings break standard delta sync logic. Learn how to synchronize Outlook recurring events with EspoCRM using windowed expansion and series rebuild strategies—a production-grade solution for Microsoft Graph API integration.</summary>
    
    
    
    <category term="Integration" scheme="https://www.yzhu.name/categories/Integration/"/>
    
    
    <category term="EspoCRM" scheme="https://www.yzhu.name/tags/EspoCRM/"/>
    
    <category term="Microsoft Graph API" scheme="https://www.yzhu.name/tags/Microsoft-Graph-API/"/>
    
    <category term="Outlook Integration" scheme="https://www.yzhu.name/tags/Outlook-Integration/"/>
    
    <category term="Recurring Events" scheme="https://www.yzhu.name/tags/Recurring-Events/"/>
    
    <category term="Delta Sync" scheme="https://www.yzhu.name/tags/Delta-Sync/"/>
    
  </entry>
  
</feed>
